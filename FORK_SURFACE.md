# Fork patch surface — upstream divergence inventory and merge runbook

English | [中文](FORK_SURFACE.zh.md)

The fork's maintenance contract with upstream: every divergence is either a fork-owned module (zero merge cost), a config flag, a small behavior patch, or a semantic core change. This file inventories each surface, its isolation tier, and the re-apply procedure for upstream syncs. Keep it current with every change that touches an upstream-owned file.

## Why this exists

The 0.1.2-alpha.1 sync (1079 upstream commits) resolved 123 conflicts. The pain concentrated in a handful of upstream-owned files carrying fork semantics. The inventory below classifies every surface so the next sync is a mechanical re-apply checklist instead of an archaeology dig.

## The fork-module convention

Every piece of fork logic that is more than a one-liner lives in a fork-owned module under `<pkg>/src/fork/` (client faces: `<pkg>/src/client/<area>/fork/`). Upstream-owned files carry only a marked injection — one import plus one call — each preceded by a `// Fork patch (FORK_SURFACE.md): ...` comment. Syncing means: copy every `fork/` directory verbatim, then re-apply the registered injections below. Inherent one-liners (constants, single-hook swaps, CSS blocks, config fields) stay inline by policy; extracting them would add indirection without shrinking the merge surface.

## Dual paths

A dual path is one behavior changed on both sides — the same file, the same algorithm, or two implementations of one outcome. Dual paths resolve to upstream: delete the fork copy, adopt upstream's, and record the retirement in `FORK_CHANGES.md`, even where the fork's version measures slightly better. A retained fork copy names the production consumer or reproduced defect upstream's version cannot serve. The inventory below marks each current dual path and its resolution.

### Resolved and retained dual paths

| Dual path | Fork side | Resolution |
| --- | --- | --- |
| TurnProcess fold label | `ui-chat/src/client/chat/fork/turn-process-summary.ts` + the view import | **Retired** — upstream builds the same disclosure label inline; the module and its `message.turnProcess.collapsed` key are deleted and the view restored to upstream's |
| History paging | `session-controller/src/fork/page-boundary.ts` + `history.ts` injections | **Retained** — upstream's `paginate` has no turn-aligned cut, so a page can open mid-turn and render a partial turn head; the fork widens cuts to the owning turn's opening events |
| Cold-open window | `session-controller/src/fork/open-window.ts` + the `history.ts` branch | **Retained** — upstream folds the whole log and trims (seconds of CPU and hundreds of MB on the largest sessions); the fork reads one indexed window through its own seek surface |
| Reflow-stable scroll anchor | `ui-chat/src/client/chat/fork/scroll-anchor.ts` + six ChatView injections + `overflow-anchor: none` | **Retained** — upstream's ResizeObserver only re-follows the pinned tail, its settlement effect clears the anchor, and its ledger reads native-anchoring writes as reader movement; the fork's recorded measurement is an 8,460 px expansion above the reader with zero compensation |
| Mobile frame composition | `ui-layout/src/client/fork/mobile-shell.tsx` + AppFrame and store fields | **Retained** — upstream's frame only collapses to a rail below 1024 px and has no phone regime, so the sidebar cannot be re-opened at phone widths |
| Sandbox composition | `bundle/base/cordis.patch.yml` executor swap + `permission` disable | **Retained** — upstream's confining executors need bwrap or Landlock; this container's kernel predates Landlock and its `bwrap` probe fails, so upstream's composition would fail-close every bash call. Re-verify on a Landlock-capable kernel |
| Activity and rebuild churn | `sessions/fork/coalesced-refresh.ts`, `snapshot-identity.ts`, `ui-workspace/src/client/fork/order-stability.ts`, `session-query-sqlite/src/fork/live-observation-memo.ts`, the no-op identity contract | **Retained** — upstream applies every activity event immediately, mints fresh subagent/job projections and snapshot objects per rebuild, re-sorts the promoted set each sync, and clones plus hashes every attached session per search; each fork module fixes a recorded defect. Partial overlaps (entry/items identity, reconcile/recency helpers) are the next candidates for adopting upstream's helpers |
| Open-file routing | `ui-chat/src/client/chat/fork/open-file-routing.ts` | **Retained** — the routing prefers the installed `dsh-better-sidebar` surface over upstream's right-Sidebar fallback |
| Generic brand `DSH` | locale dictionaries + `apps/web` defaults | **Retained** — the deployment's brand choice; upstream reads `DSH Local Build` |

## Isolation tiers

- **Tier A — fork-owned packages**: files upstream will never have. Zero merge cost.
- **Tier B — config flags**: a constant or a validated `Config` field in an upstream file. Trivial re-apply.
- **Tier C — small behavior patches**: under ~40 lines per upstream file, localized blocks. Small, resolvable conflicts.
- **Tier D — semantic core changes**: fork behavior inside upstream-owned algorithms. This is the merge cost; minimize the injection surface.

## Current inventory (vs upstream/master at the 0.1.2-alpha.1 sync)

| Surface | Tier | Size | Nature |
| --- | --- | --- | --- |
| `packages/web/web-search-{academic,bocha,brave,zhihu}` | A | 20 files | Fork search providers, fully isolated |
| `.agents/notes`, docs, snapshots, test updates | A | — | Follow their sources; re-apply with the source change |
| `session.ts` PAGE_MESSAGES 8 (upstream 50) | B | 8 lines | Client page size |
| `client/connection` browserAuth flag | B | ~50 lines | Optional auth disable behind a config field |
| `ui-chat` TurnProcess fold label + duration | retired (dual path) | — | Upstream 0.1.5-rc.2 builds the same disclosure label inline in `TurnProcessNodeView`; the fork's `src/client/chat/fork/turn-process-summary.ts` (counted categories + wall-clock duration + folded prefix) was deleted and the view restored to upstream's, deleting the fork's `message.turnProcess.collapsed` locale key with it. The duration stays visible in the turn footer's usage details. `TurnProcessNodeView.tsx` and `locale.ts` now match upstream exactly. |
| `ui-chat` ChatView reader-input attribution | retired | — | Upstream's own fix landed (geometric observed-top ledger covers all input devices without listeners); the fork's device-marker patch was dropped at the 0.1.2-rc.1 sync |
| Deployment reduced-motion default | C | 1 shell module + 1 boot call + 1 CSS block | This deployment runs **without motion by default**: every boot marks `<html data-reduce-motion>` and `packages/client/web/src/base.css` completes animations in one step. Transitions keep their authored duration: the right Sidebar's panel machinery needs an observable `transform` transition, and a blanket 1 ms override makes its `transitionrun` probe report "panel transform transition is unavailable" and fails seven `sidebar-right.e2e.ts` cases. `?reduce-motion=0` stores a per-browser opt-out, `?reduce-motion=1` clears it. The marker never changes the browser-wide `prefers-reduced-motion` setting, so other sites keep their motion |
| Deployment theme palette | C | 1 shell module + 1 stylesheet + 1 boot call | This deployment renders the dusk-blue palette by default: every boot marks `<html data-dsh-theme="harbor">` and `packages/client/web/src/fork/themes.css` carries the token overrides — the owner's blue/white recolor of the locally installed "夕港 / Harbor" dsh-market skin plus the MIT Dracula markdown and syntax palette. The palette is declared on the marked root **and on its body**: upstream declares the alias tokens on `body` and `body[data-ds-dark-theme]`, which beat a root-only block inside the app subtree, so a root-only palette left the panels on upstream's colors. `?theme=default` stores a per-browser opt-out, `?theme=harbor` clears it. Upstream's theme plugin owns only the light/dark/system preference and content font size and exposes no palette extension point, so the palette lives in the shell instead of a theme registration; the skin's wallpaper and runtime hooks stay out of the repository |
| Reduced-motion guards for infinite animations | C | 4 CSS media blocks | Upstream ships `@media (prefers-reduced-motion: reduce) { animation: none }` in most animation files but omits it for four infinite ones: `ui-primitives/StateDot.cell` (the running-state dot chase), `ui-conversation/InputBar.pending`, `ui-conversation/TodoPanel.glyphProgress`, and `ui-input-trigger/MenuView.skeletonBar`. The guards only apply when the reader enabled the preference, so every other reader's rendering is unchanged; StateDot keeps a static mid-phase `opacity: 0.6` so the ongoing state stays readable |
| `ui-conversation`/`ui-chat` CSS overflow-anchor + safe-area | C | ~40 lines | Scrollport anchoring; localized rules |
| `ui-conversation` composer keystroke re-render confinement | C | 2 null leaves + 3 call sites | `ConversationRoot` subscribed to the whole input store only to hand the dock its owner props, and `ConversationSession` to the input store plus the stored draft only to seed and mirror the composer draft. Both subscriptions re-rendered the column those components render on every keystroke — the header, the loaded transcript window (`conversation.view`), and the composer stack. The subscriptions now live in `InputDockSlot` and `DraftMirror`, which render no DOM; the dock still receives its live owner props and the seed effect keeps the same mount-only semantics. Measured at 1,519 loaded rows / 30,404 nodes: per-keystroke JS work 3 ms → 1 ms, while total latency stayed at one 16 ms frame either way on the measuring host |
| `api/session-controller/src/history.ts` fast-path injections | C | ~10 lines for `page()`, ~60 lines for `follow` | The paging injection delegates to fork-owned `src/fork/page-boundary.ts` (the boundary walk, the ladder, the fast-path plan): one call in `page()` plus `paginate`'s delegation. The opening injection adds the synchronous service check, the windowed snapshot branch with its observation fallback, and the second constructor callback that activates a windowed Session by id. Both fast paths reach their persistence methods through the `seekSurface` helper, which binds them to the tracker proxy `ctx.get` answers: an extracted, unbound method runs with the wrapper object as `this` and throws inside every provider that reads its own state |
| `api/session-controller/src/fork/open-window.ts` cold-open window | C | ~110 lines + `history.ts` branch | The opening snapshot's windowed read: the read plan over `readIndexedSuffix` (`page-boundary.ts`, which gains the windowed read, the optional plan `throughSeq`/`windowFloor`, and the turn-truncation check), the checkpoint gate, the closed-tail proof, and the fold into the snapshot. Upstream's `follow` folds the whole log and trims, which costs seconds of CPU and hundreds of MB of heap on the largest sessions; upstream has no indexed seek surface to delegate to (the fork's `seekable`/`messageCut`/`readFrom` live on its own persistence backend). The windowed read calls `seekable` first and bails on a false answer before any cut or read. `history.ts` keeps the marked try, its observation fallback, and the id-based activation handoff |
| `session-projection-cache/src/fork/checkpoint-read.ts` checkpoint read + write-back | C | ~90 lines + `index.ts` registration | The windowed open needs the stored checkpoint rows by session id, and the first cold open must install the record the next one folds from. Upstream's public fold face returns viewed values, not seedable rows, and its `hydratePrepared` deliberately wrote nothing; the fork adds a symbol-keyed registration of the service's private lookup (a WeakMap cannot work: cordis hands callers a tracker proxy, not the registered object), the `readCheckpoint` read, and the durable-prefix write-back. `readColdSessionLog` reports `durableEventCount` so the written rows never pass the stored log end, and both upstream-owned files (`session-query/src/cold-read.ts`, `session-query/src/observation.ts`) carry marked injections for it |
| `session-persistence-sqlite` whole package + the seek surface (`seekable`, `messageCut`, `readFrom`) | A (fork-owned since 0.1.2-rc.1) | 8 files | **0.1.5-rc.2 STATUS: adapted** — src/handle.ts (per-session handle, live write-behind, torn-tail truncation, in-process single writer) and src/restore.ts (legacy rows restore through `dsh-session-format-catalog`, v0→v3 migration on read, publish-on-write-open); `preparedSessionCacheSize` dropped (upstream removed the prepare/borrow seam), packed-row decode kept for legacy rows, schema 20 + 19→20 migration + `seed_length` unchanged; 110 tests pass, upstream contract 21/21 memory + 20/20 file-backed. | Upstream removed the package (JSONL-only decision) but kept the seam for out-of-tree providers; the fork keeps it ported to the handle-based `PersistenceBackend`/coordinator at upstream's final SCHEMA_VERSION 20 (`ignorable` column doubling as the packed-row sentinel; one-time in-place 19→20 migration; `seed_length` carries the inherited cut). `seekable(id)` is the fast paths' gate: true only for a current-format row, and it reads metadata only. It exists because `loadStoredFrom`'s historical arm restores the whole log through the format catalog and slices it by a `fromSeq` from the stored physical space, which the re-based restored seqs do not occupy — the deployed store is mostly such rows, and a window plan there paid a full read per attempt and got an empty page |
| `session-query-sqlite` live-observation memo | C | ~13 lines in `index.ts` | The fingerprint memo (`length:tailSeq:tailTime`) in `src/fork/live-observation-memo.ts`; `index.ts` keeps the field plus a 3-line delegation |
| `client/ui-layout` AppFrame mobile drawer | C | import + compose + 2 store fields | **0.1.5-rc.2 STATUS: re-applied** — upstream's frame offers only a narrow rail below its 1024px auto-collapse, so the fork keeps its phone breakpoint (768px, matching the right Sidebar's own auto-fullscreen width): the left sidebar becomes a fixed overlay drawer and the frame renders one in-flow track. The details sheet retired — upstream's right Sidebar covers the viewport in fullscreen below 768px with its own exit affordance. | The regime hook and drawer chrome live in `src/client/fork/mobile-shell.tsx` (its own `MOBILE_VIEWPORT` since upstream's `columns.ts` dropped the constant); AppFrame's diff is the import, one `useMobileRegime` call, the drawer branch, the single-track template, `RightbarColumn`'s `mobile` attr (keeps the column out of that track), and two handle guards; `stores.ts` adds `mobile`/`drawerOpen` plus `setMobile`/`setDrawerOpen` and a mobile branch in `toggleSidebar`; the drawer's label is the `sidebar.open` key the fork added to the `common` dictionary |
| Generic client brand reads plain `DSH` | C | 2 dictionary values + 2 build defaults + test expectations | Upstream's generic brand is `DSH Local Build`; the fork's reads `DSH` (`brand.localBuild` in both locales drives the sidebar brand name and the document-title fallback, `apps/web/index.html` + `vite.config.ts`'s `DEFAULT_CLIENT_TITLE` carry the static default). A deployment title via `DSH_CLIENT_TITLE` still overrides it, and the build-version line under the brand is untouched |
| Conversation header mobile trim (session-log capsule + breadcrumb) | C | ~10 lines | `max-width: 560px` media blocks hide the Session log download capsule (`session-log-export`) and the session-title breadcrumb (`ui-conversation`) at phone widths |
| `api/session-controller` `openWorkspacePath` desktop gate + `ui-chat` open-file routing | C | gate stays inline (5 lines); routing = marker+import+2 calls | The open RPC consults `canOpenPath()` and fails fast (inherent one-liner); the routing decision and refusal copy mapping live in `src/client/chat/fork/open-file-routing.ts` |
| `bundle/base` sandbox capability | C | row `disabled` flags + executor row names | **0.1.5-rc.2 STATUS: partially restored** — upstream's workspace-files and deliverables entries require the `sandboxPolicy` service, so the sandbox and sandbox-policy rows are enabled again with `mode: danger-full-access` (this deployment's only mode); the executor rows stay the local providers and the permission-presets row stays disabled. |
| `client/modules` + `client/web` deferred boot batches | C | ~120 lines across 3 files | `WebBootBatchPhase 'deferred'` + `Config.defer` partition + two-stage boot; upstream-shaped (additive wire field, empty default); the defer list is deployment config, not repo state |
| `ui-workspace` stable promotion head | C | marker+import | `nextSessionOrderAccount`/`reconciledSessionOrder` in `src/client/fork/order-stability.ts`; WorkspaceBrowser calls them plainly |
| `ui-chat` message-actions DOM hook | C | 1 attribute in `MessageIconActions.tsx` | The action strip carries `data-message-actions` so third-party plugins can attach an action beside the native copy/branch controls. Upstream's 0.1.5 removed `data-time-hover-root`, which the fork's `dsh-rewind` plugin anchored to; without a stable hook the plugin silently renders nothing. The attribute is additive and invisible |
| `ui-chat` StatsLine passive measurement | C | 1 line + comment | Ellipsis test in `useEffect` (post-paint) instead of `useLayoutEffect`; no behavior change |
| `skill` registry catalog restrictions | C | 17 lines in `index.ts` | All logic in `src/fork/skill-restrict.ts` (compile, per-scope store, chain filter); `index.ts` keeps the import, one field, and two marked delegating calls; allow/deny mutual exclusion documented in the module's JSDoc |
| `subagent` per-child cwd + skillFilter | C | seams + 2 injections in `child-agent.ts` | Logic in `src/fork/child-scoping.ts` (`stampChildCwd`, `applyChildSkillFilter`); the request-field threading through `childSessionMeta`/continuation/driver is the seam that stays — at the 0.1.5-rc.2 sync the fork re-applied it as the `request.cwd` argument, the descriptor's `cwd`/`skillFilter` fields (version 4), and `skillFilter` in both continuable compositions (first activation from the request, cold resume from the folded descriptor); cwd authority stays the session header on cold resume |
| `ui-chat` ChatView reflow-stable scroll anchor | C | import + 1 callback arm + 3 capture/re-hold edits, 1 removed upstream effect in ChatView | `chat/fork/scroll-anchor.ts` re-asserts the held reader row on every flow-column resize (fold collapse, image loads, disclosures), measuring the hold after the scroll write so consecutive callbacks stay idempotent, with ledger bookkeeping so reader-input attribution stays correct; anchored row hidden falls back to the nearest surviving visible row above. ChatView arms the anchor on every off-bottom scroll sample, re-holds the reader's row after each prepend compensation, captures the restored row on saved-position restore, and keeps the anchor across load-earlier settlement (the upstream clearing effect is removed) |
| `api/session-controller` streaming snapshot cadence | C | 1 line + comment in `session.ts` | A live chunk presentation (`publishAssistantEntry`'s `transient` branch) marks the session snapshot with `markFrameDirty()` instead of `markDirty()`, so one streaming frame publishes at most once per animation frame. The frame scheduler already existed (`Notifier.markFrameDirty`) but had no production caller: every chunk ran a microtask flush and re-rendered every `useSession` consumer (measured 3 chunk frames → 3 notifications; now 3 → 1, and the final state is identical) |
| `api/session-controller` reset recovery for failed session windows | C | 9 lines in `manager.handleConnected` | A carrier reset aborts every logical stream; errored sessions re-open through `resync()` instead of freezing on the last frame until a full page refresh |
| `api/session-controller` ambient activity coalescing | C | import + field + 1 call + no-op identity contract in `manager.ts` | Continuous `api-session/activity` streams from other running sessions buffered per-session (latest timestamp wins) and flushed about once per second by `sessions/fork/coalesced-refresh.ts`; lone activities apply immediately, preserving the synchronous staging contract. `applyMutation` returns the input array reference when a mutation flips nothing, and `recordMutation` skips the dirty flush on that identity, so unchanged status/activity frames never rebuild the list |
| `api/session-controller` client selection notification + snapshot identity | C | ~9 lines in `manager.ts` | Identity/stability logic (entry/items/subagents/jobs caches, equal-content previous-snapshot reuse) in `src/client/sessions/fork/snapshot-identity.ts`; selection still notifies via `markDirty`, `open`/`openSubagent` stage synchronously through `followCurrent` |
| `api/gateway` Remote stream mux permessage-deflate | C | ~35 lines | `RemoteStreamMuxServer` takes a `perMessageDeflate` flag from `Config.websocketPerMessageDeflate` (default off); RFC 7692 negotiation with `threshold: 1024` so journal `opened` window frames compress while live frames stay raw; the mux frame handling itself is untouched
| `ui-workspace` order-store reference stability | C | marker+import+3-line guard | `sessionOrderChanged` in `src/client/fork/order-stability.ts`; the store action keeps the previous array reference when order is unchanged (timestamps still advance) |
| `bundle/base/cordis.patch.yml` local-provider swap + permission disable | C | 5 row deltas + comments | Fork decision 2026-09-05 (danger-full-access-only deployment, FORK_CHANGES.md): executor rows `bash-sandbox`/`pwsh-sandbox`/`fs-sandbox` keep their ids and platform `!!js` gates but mount `@deepseek-ai/dsh-bash-local`/`dsh-pwsh-local`/`dsh-fs-local` instead of the sandboxing packages. Re-apply on sync: revert names/flags per the in-file fork comments; `permission` must track the sandbox rows (its constructor rejects composition over a non-confining executor). The `sandbox` row is upstream's `@deepseek-ai/dsh-sandbox-local` on both sides, and `sandbox-policy` is enabled with `mode` pinned to `danger-full-access` instead of upstream's `DSH_PERMISSION_MODE` default; only `permission` carries `disabled: true`. Adoption is blocked by this container (see the dual-path table): upstream's bash executor fail-closes without bwrap or Landlock |
| root `vitest.config.ts` disabled sandbox suites | C | 2 lists + spreads | `forkDisabledSandboxTests` drops `packages/sandbox/*`, bash-sandbox, pwsh-sandbox, fs-sandbox unit suites from collection; `forkDisabledSandboxCoverageExclusions` drops those sources from the per-file coverage gate. Restore path (re-enable base rows, delete the lists) is in the in-file comments |
| `vitest.e2e.config.ts` sandbox-family + ACP escalation e2e excludes | C | 2 exclude lists | Fork decision 2026-09-05 (FORK_CHANGES.md): the sandbox-family e2e suites (`packages/sandbox/*`, bash-sandbox, pwsh-sandbox, fs-sandbox, `*.e2e.ts`) and the ACP escalation suite (`apps/cli/tests/profiles/acp/tests/escalation.e2e.ts`, denial→escalation flow; keyless-smoke split hint in the comment) probe machinery the fork no longer mounts. Restore path in the in-file comments |
| `vitest.web.config.ts` permission-surface browser excludes | C | 1 exclude list | Fork decision 2026-09-05 (FORK_CHANGES.md): `settings-chrome`, `permission-policy-context`, `access-confirmation`, `seeded-history`, and `approval-composer` browser suites assert the Permission settings row / Access-mode picker / `/permission read-only` chip the fork no longer mounts — approval-composer replays the recorded approval session under `snapshots/web/approval-composer/`, but can only reach its approval-takeover subject by driving the Access chip into Read Only, and the chip renders nothing without the host permission service's `permissions` projection. Goldens stay upstream-shaped; restore path in the in-file comment |
| `vitest.expected.config.ts` sandbox-shaped expected excludes | C | 1 exclude list | Fork decision 2026-09-05 (FORK_CHANGES.md): `subagent-inheritance` and `image-offload` pin the read-only delegation events, sandbox-policy prompt context, denial output, and `Current DSH file policy` sentence the fork no longer produces, and `goal`, `headless`, `semantic-checkpoint`, `subagent-diagnostic`, and `workspace-context-resume` diff whole normalized sessions against goldens recorded on the upstream composition — their `permission/preset`, `sandbox/mode`, `approval/policy` rows and sandbox:policy runtime-context message never appear in fork logs (12 of the lane's 25 tests failed on the fork before this exclusion). Goldens stay upstream-shaped; restore path in the in-file comment |
| `vitest.snapshot.config.ts` ACP snapshot corpus exclusion | C | 1 exclude list | Fork decision 2026-09-05 (FORK_CHANGES.md): `snapshots/acp/acp.snapshot.ts` replays upstream-recorded sandbox denial→escalation→approval corpora (escalation trio plus the cancel/approval and schema-pin cases sharing its sidecars); measured 6/8 replays fail on the fork composition, and suite pin/inventory invariants forbid a scenario-list exclusion, so the whole file is uncollected. Restore path in the in-file comment |
| `apps/web/tests/shipped-composition.e2e.ts` sandbox absence assertions | C | 1 assertion block | Fork decision 2026-09-05 (FORK_CHANGES.md): replaces upstream `sandboxPolicy`/`permissionPresets` mode pins with `ctx.get(...)`-resolves-`undefined` assertions (approval stays `ask`). Restore path in the in-file comment |
| `apps/cli/tests/windows-shell.spec.ts` permission-row assertion | C | 1 assertion block | Fork decision 2026-09-05 (FORK_CHANGES.md): upstream's "permission surface never moves" loop asserted the sandbox/permission rows stay enabled; the fork asserts them `disabled: true` while ui-permission/fs-sandbox/approval stay enabled. Restore path in the in-file comment |
| `apps/cli/tests/github-webhook-real.e2e.ts` permission-preset pin removal | C | 1 conjunct | Fork decision 2026-09-05 (FORK_CHANGES.md): the keyed real test no longer requires the `permission/preset read-only` event (only producer permission-presets is disabled with the sandbox rows); title + webhook-source provenance stay. Restore the conjunct with the sandbox rows |

## Optimization plan (priority order)

1. **DONE — Extract the paging core from `history.ts`** — move `nthMessageCut`, `turnAlignedCut`, `paginateSuffix`, and the pure part of `tryIndexedPage` into a fork-owned module (e.g. `src/page-boundary.ts`, a file upstream will never have). `history.ts` keeps a ~10-line injection: the import, the `tryIndexedPage` call in `page()`, and `paginate` delegating to the shared walk. Upstream refactors of `page()` re-conflict against ten lines, not two hundred.
2. **DONE — Move `messageCut` off the upstream persistence interface** — a fork-owned service (its own package or an extension in `session-query-sqlite`) exposing the indexed cut; `history.ts`'s fast path already discovers it optionally (`SeekablePersistence` duck-typing). The upstream `SessionPersistence` abstract and coordinator revert to pristine; the SQL moves with the fork service.
3. **Write the merge runbook as you change, not at sync time** — every Tier C/D change notes its injection point here; the sync re-apply follows this file top to bottom.
4. **Keep Tier D diffs compact and marked** — the scroll-attribution block carries its rationale comment; upstream fixing the same clamp bug upstream-side shrinks the fork diff to zero (watch for it in upstream releases).
5. **DONE — AppFrame mobile shell** — the largest surface. Realistically stays a patch (layout is upstream's core component), but keep the fork's regimes in `columns.ts`-style leaf modules so the AppFrame diff stays import-and-delegate.
6. **PAGE_MESSAGES** — one line; optionally a build-time env (`DSH_CLIENT_PAGE_MESSAGES`) if it churns.

## Sync procedure (runbook)

1. Read every release note between the fork's base tag and the target tag, and list the protocol, session-format, and API breaks they name; plan each adaptation they imply.
2. `git fetch upstream && git merge upstream/master` in a worktree.
3. **Parity review (AGENTS.md policy)**: walk every inventory row against the new tag; when upstream ships an equivalent, drop the fork row and adopt upstream's (record the retirement in FORK_CHANGES.md); update each retained row's why-upstream-cannot-serve note.
4. Tier A: no action (upstream has no such files); resolve only `pnpm-workspace`/tsconfig aggregates and `cordis.patch.yml`.
5. Tier B: re-apply the flag/constant; expect trivial context conflicts.
6. Tier C: re-apply each localized block per this file's inventory; run the owning package's suite.
7. Tier D: re-apply the injection points (after step 3 above they are the only remaining history.ts/persistence diffs), then copy the fork-owned modules verbatim.
8. Run `pnpm run test:gui`, the session-controller suite, and `DSH_SNAPSHOT=replay pnpm run test:web`; refresh goldens only for intentional output changes.
9. Record the sync in `FORK_CHANGES.md`.
