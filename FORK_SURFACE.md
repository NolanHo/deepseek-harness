# Fork patch surface — upstream divergence inventory and merge runbook

English | [中文](FORK_SURFACE.zh.md)

The fork's maintenance contract with upstream: every divergence is either a fork-owned module (zero merge cost), a config flag, a small behavior patch, or a semantic core change. This file inventories each surface, its isolation tier, and the re-apply procedure for upstream syncs. Keep it current with every change that touches an upstream-owned file.

## Why this exists

The 0.1.2-alpha.1 sync (1079 upstream commits) resolved 123 conflicts. The pain concentrated in a handful of upstream-owned files carrying fork semantics. The inventory below classifies every surface so the next sync is a mechanical re-apply checklist instead of an archaeology dig.

## The fork-module convention

Every piece of fork logic that is more than a one-liner lives in a fork-owned module under `<pkg>/src/fork/` (client faces: `<pkg>/src/client/<area>/fork/`). Upstream-owned files carry only a marked injection — one import plus one call — each preceded by a `// Fork patch (FORK_SURFACE.md): ...` comment. Syncing means: copy every `fork/` directory verbatim, then re-apply the registered injections below. Inherent one-liners (constants, single-hook swaps, CSS blocks, config fields) stay inline by policy; extracting them would add indirection without shrinking the merge surface.

## Dual paths

A dual path is one behavior changed on both sides — the same file, the same algorithm, or two implementations of one outcome. Dual paths resolve to upstream: delete the fork copy, adopt upstream's, and record the retirement in `FORK_CHANGES.md`, even where the fork's version measures slightly better. A retained fork copy names the production consumer or reproduced defect upstream's version cannot serve. The inventory below marks each current dual path and its resolution.

### Resolved and retained dual paths

| Dual path | Fork side | Resolution |
| --- | --- | --- |
| TurnProcess fold label | `ui-chat/src/client/chat/fork/turn-process-summary.ts` + the view import | **Retired** — upstream builds the same disclosure label inline; the module and its `message.turnProcess.collapsed` key are deleted and the view restored to upstream's |
| History paging | `session-controller/src/fork/page-boundary.ts` + `history.ts` injections | **Retained — fork edit inside upstream's `paginate` (2026-09-26)** — upstream widens, with `turnWindow`, to the `turn/start` carrying both minima, but its message stop cuts at the max-th counted message (or that message's origin group head), so a request whose floor crossed a long Turn opened the page at an `assistant/message` that had no `turn/start` above it — reproduced on both `session/page` and the `follow` opening snapshot. The fork edit treats that stop as an anchor and walks on to the `turn/start` owning it, so every cut is a Turn start, or the log head when the walk reaches it before any Turn start (`exhausted`); `maxMessages` becomes a floor rather than a cap. Both read paths still take the cut from `SessionHistoryController.windowCut()`, which binds that `paginate` per request, so an indexed read and the observation path serve one page whether or not a cache record exists. What stays fork-owned beyond the edit is the indexed physical read — `page-boundary.ts`'s `SeekablePersistence`/`readIndexedSuffix`/`readIndexedPage` and the `history.ts` injections |
| Cold-open window | `session-controller/src/fork/open-window.ts` + the `history.ts` branch | **Retained** — upstream folds the whole log and trims (seconds of CPU and hundreds of MB on the largest sessions); the fork reads one indexed window through its own seek surface |
| Reflow-stable scroll anchor | `ui-chat/src/client/chat/fork/scroll-anchor.ts` + six ChatView injections + `overflow-anchor: none` | **Retained** — upstream's ResizeObserver only re-follows the pinned tail, its settlement effect clears the anchor, and its ledger reads native-anchoring writes as reader movement; the fork's recorded measurement is an 8,460 px expansion above the reader with zero compensation |
| Mobile frame composition | `ui-layout/src/client/fork/mobile-shell.tsx` + AppFrame and store fields | **Retained** — upstream's frame only collapses to a rail below 1024 px and has no phone regime, so the sidebar cannot be re-opened at phone widths |
| Sandbox composition | `bundle/base/cordis.patch.yml` executor swap + `permission` disable | **Retained** — upstream's confining executors need bwrap or Landlock; this container's kernel predates Landlock and its `bwrap` probe fails, so upstream's composition would fail-close every bash call. Re-verify on a Landlock-capable kernel |
| Activity and rebuild churn | `sessions/fork/coalesced-refresh.ts`, `client/sessions/projection-store.ts`, `session-query-sqlite/src/fork/live-observation-memo.ts`, the no-op identity contract | **Retained** — upstream applies every activity event immediately, mints fresh subagent/job projections and snapshot objects per rebuild, and clones plus hashes every attached session per search; each remaining fork module fixes a recorded defect. The 0.1.7-rc.1 sync resolved the partial overlaps this row flagged at 0.1.5-rc.2: `snapshot-identity.ts` and `ui-workspace`'s `order-stability.ts` are deleted, because upstream now builds entry identity inline in `manager.buildListSnapshot` and derives the promoted order from `reconcileManualOrder`/`orderByRecency` in `tree.ts`. The 2026-09-25 addition covers the control-frame path, which the activity coalescer never saw: `projection-store.ts` reports whether an accepted frame's published value actually changed (an equal value advances the sequence watermark and keeps the cached value and its reference, without notifying), `manager.handleControlFrame` marks the list dirty only on a real change, and `service.projectList` keeps the published row object per matching row and skips `list.set` entirely when ids, rows, order, phase and projections are unchanged. It also moved `refreshProjections`' in-flight probe from the values identity to the watermark, because a silent landing no longer moves that identity. Measured before it: a phone-class client with no session open spent about 300 s of JavaScript inside a 310 s window (496 long animation frames, 280.9 s blocking, style and layout 13 ms), attributed to the list/tree/status rebuild chain |
| Open-file routing | `ui-chat/src/client/chat/fork/open-file-routing.ts` | **Retained** — the routing prefers the installed `dsh-better-sidebar` surface over upstream's right-Sidebar fallback |
| Generic brand `DSH` | locale dictionaries + `apps/web` defaults | **Retained** — the deployment's brand choice; upstream reads `DSH Local Build` |

## Isolation tiers

- **Tier A — fork-owned packages**: files upstream will never have. Zero merge cost.
- **Tier B — config flags**: a constant or a validated `Config` field in an upstream file. Trivial re-apply.
- **Tier C — small behavior patches**: under ~40 lines per upstream file, localized blocks. Small, resolvable conflicts.
- **Tier D — semantic core changes**: fork behavior inside upstream-owned algorithms. This is the merge cost; minimize the injection surface.

## Current inventory (vs upstream at the fork's base tag, dsh-v0.1.7-rc.1)

| Surface | Tier | Size | Nature |
| --- | --- | --- | --- |
| `packages/web/web-search-{academic,bocha,brave,zhihu}` | A | 20 files | Fork search providers, fully isolated |
| `.agents/notes`, docs, snapshots, test updates | A | — | Follow their sources; re-apply with the source change. `apps/web/tests/preset-migration.snapshot.ts` re-syncs upstream's post-activation fact tail to the fork composition: the `permission` row stays disabled, so activation pins no `permission/preset`, `sandbox/mode`, or `approval/policy` fact. The Web lane's other fork-composition re-syncs: `apps/web/tests/shipped-composition-auto.e2e.ts` is split out of `shipped-composition.e2e.ts` and excluded by `vitest.web.config.ts`, because every case drives the host `permissionPresets` service to switch a Session into Auto and the disabled `permission` row leaves the `auto-review` entry pending, so the producer never activates (registered in `tsconfig.host.json` and `apps/web/tsconfig.json`); `shipped-composition.e2e.ts` asserts the enabled `sandbox-policy` row's `danger-full-access`; `apps/web/tests/support.ts` carries the shared `optOutDeploymentTheme` opt-out, which stores the theme key its query owns because the host's token redirect strips the query before boot; `sidebar-terminal.e2e.ts` and `thinking-markdown.e2e.ts` boot through that opt-out; `trajectory-virtualization.e2e.ts` pages through the fork's 8-message page size; `chat-long-interactions.e2e.ts` re-pins the transcript tail after its fold expansion; `remote-welcome.e2e.ts` asserts the durable ack of a host-published trusted authority; `details-session-lifecycle.e2e.ts` keeps its auto-fullscreen block above the fork's 768 px mobile regime; `present.e2e.ts` withdraws the bare native open while its prose mentions run, so they reach the Sidebar preview while the delivery card keeps its reveal and application opens, and `snapshots/web/present/ui.expected.md` drops the Access chip that row no longer renders; `plan-control-row.e2e.ts` scans below the fork's narrower control row |
| `session.ts` PAGE_MESSAGES 8 (upstream 50) | B | 8 lines + the `apps/web/tests/stats-paged-history.e2e.ts` `loadEarlierUntil` helper that pages the whole log at 8-message pages | Client page size |
| `client/connection` browserAuth flag | B | ~50 lines | Optional auth disable behind a config field |
| `ui-chat` TurnProcess fold label + duration | retired (dual path) | — | Upstream 0.1.5-rc.2 builds the same disclosure label inline in `TurnProcessNodeView`; the fork's `src/client/chat/fork/turn-process-summary.ts` (counted categories + wall-clock duration + folded prefix) was deleted and the view restored to upstream's, deleting the fork's `message.turnProcess.collapsed` locale key with it. The duration stays visible in the turn footer's usage details. `TurnProcessNodeView.tsx` and `locale.ts` now match upstream exactly. |
| `ui-chat` Turn Process fold with partial history | retired | — | **Retired** — upstream 0.1.7-rc.1 puts no `historyIncomplete` gate on `processWindowReady` (it reads the window's `turnStarted`/`turnClosed` flags), so a closed Turn folds as soon as its presentation window is loaded; the fork's dropped prop, its gate edit in `ChatNodeSeat.tsx`, and the two partial-history tests are deleted, and `ChatView.tsx` matches upstream again |
| `ui-chat` TurnProcess disclosure pill | C | ~13 CSS lines in `TurnProcessNodeView.module.css` | The disclosure renders as a rounded pill that hugs its label, with a hover surface and the chevron set off from it; upstream renders a full-width divider row with a trailing chevron. The markup and label are upstream's and the class names are unchanged, so a wholesale upstream stylesheet adoption restores the divider silently |
| `ui-chat` ChatView reader-input attribution | retired | — | Upstream's own fix landed (geometric observed-top ledger covers all input devices without listeners); the fork's device-marker patch was dropped at the 0.1.2-rc.1 sync |
| Deployment reduced-motion default | C | 1 shell module (`packages/client/web/src/fork/reduce-motion.ts`) + 1 boot call in `packages/client/web/src/boot.ts` + 1 CSS block | This deployment runs **without motion by default**: every boot marks `<html data-reduce-motion>` and `packages/client/web/src/base.css` completes animations in one step. Transitions keep their authored duration: the right Sidebar's panel machinery needs an observable `transform` transition, and a blanket 1 ms override makes its `transitionrun` probe report "panel transform transition is unavailable" and fails seven `sidebar-right.e2e.ts` cases. `?reduce-motion=0` stores a per-browser opt-out, `?reduce-motion=1` clears it. The marker never changes the browser-wide `prefers-reduced-motion` setting, so other sites keep their motion |
| Deployment theme palette | C | 1 shell module (`packages/client/web/src/fork/deployment-theme.ts`) + 1 stylesheet (`packages/client/web/src/fork/themes.css`) + 1 boot call in `packages/client/web/src/boot.ts` + 4 element rules | This deployment renders the dusk-blue palette by default: every boot marks `<html data-dsh-theme="harbor">` and `packages/client/web/src/fork/themes.css` carries the token overrides — the owner's blue/white recolor of the locally installed "夕港 / Harbor" dsh-market skin, with the markdown and syntax palette taken from the "tokyo-night" theme in the owner's xbot checkout (code surface `#161a2e`, links cyan `#7dcfff`). The palette is declared on the marked root **and on its body**: upstream declares the alias tokens on `body` and `body[data-ds-dark-theme]`, which beat a root-only block inside the app subtree, so a root-only palette left the panels on upstream's colors. Four rules scoped to `[data-chat-flow]` add the contrast no token carries: two-tier headings (h1/h2 `#7aa2f7`, h3+ `#c8d3f5`), inline-code chips (`#9ece6a` on `#2b3358` with a visible border), and table headers (`#1f2540`). `?theme=default` stores a per-browser opt-out, `?theme=harbor` clears it. Upstream's theme plugin owns only the light/dark/system preference and content font size and exposes no palette extension point, so the palette lives in the shell instead of a theme registration; the skin's wallpaper and runtime hooks stay out of the repository |
| Reduced-motion guards for infinite animations | C | 2 CSS media blocks | Upstream ships `@media (prefers-reduced-motion: reduce) { animation: none }` in most animation files but omits it for two infinite ones: `ui-conversation/InputBar.pending` (`packages/client/ui-conversation/src/client/skeleton/InputBar.module.css`) and `ui-input-trigger/MenuView.skeletonBar` (`packages/client/ui-input-trigger/src/client/MenuView.module.css`). The guards only apply when the reader enabled the preference, so every other reader's rendering is unchanged. The 0.1.7-rc.1 sync retired the other two halves: upstream replaced `ui-primitives`'s StateDot pixel chase with its own spinner keyframes and reduced-motion arc, and `ui-conversation/TodoPanel`'s animated glyph is a `StateDot` now |
| Ongoing Session indicator under the deployment default | C | 1 palette token | The deployment's marker completes animations in one step, and upstream 0.1.7-rc.1 replaced `StateDot`'s pixel chase with its own spinner keyframes and reduced-motion arc, so the fork's `:root[data-reduce-motion]` keep-alive block and the chase's static mid-phase opacity are deleted with the chase. The palette half stays: `packages/client/web/src/fork/themes.css` lifts `--dsw-static-deepseek-450` from `rgb(86 134 254)` to the brand-light `#8fbaff`, the static step the default file-type icon rides through `--dsh-file-type-default-color` |
| `frontend-static` immutable cache header for hash-named assets | C | ~10 lines in `packages/host/frontend-static/src/index.ts` | The dist server answered every file with `content-type` alone, so a reload re-downloaded the shell's content-hashed JavaScript and CSS while the plugin bundles under `/plugins` were already immutable (measured on the running deployment: 438 KiB gzip of shell JavaScript and CSS out of 448 KiB per reload, 1,352 KiB decoded). Hits inside the Vite hash-named output directories (`assets/`, `preview/`; `apps/web/vite.config.ts` fixes `[hash]` for every emitted name) now carry `cache-control: public, max-age=31536000, immutable`; index responses and the stable-named root entries (`favicon.svg`, `manifest.webmanifest`, `preview.html`) keep their previous headers, so a rebuilt bundle or a changed index still reaches the browser. Upstream exposes no cache-header configuration on `frontend-static`, so the rule is a marked patch inside its serving function; the Electron/desktop dist handler (`apps/desktop-host`) keeps upstream's header-less serving, since this deployment serves the Web surface |
| `ui-conversation`/`ui-chat` CSS overflow-anchor + safe-area | C | ~40 lines | Scrollport anchoring; localized rules |
| `ui-conversation` composer keystroke re-render confinement | C | 2 null leaves + 3 call sites in `packages/client/ui-conversation/src/client/skeleton/ConversationContent.tsx` and `packages/client/ui-conversation/src/client/skeleton/DefaultConversationViews.tsx` | `ConversationRoot` subscribed to the whole input store only to hand the dock its owner props, and `ConversationSession` to the input store plus the stored draft only to seed and mirror the composer draft. Both subscriptions re-rendered the column those components render on every keystroke — the header, the loaded transcript window (`conversation.view`), and the composer stack. The subscriptions now live in `InputDockSlot` (in `ConversationContent.tsx`) and `DraftMirror` (in `DefaultConversationViews.tsx`), which render no DOM; the dock still receives its live owner props and the seed effect keeps the same mount-only semantics. Measured at 1,519 loaded rows / 30,404 nodes: per-keystroke JS work 3 ms → 1 ms, while total latency stayed at one 16 ms frame either way on the measuring host |
| Bounded `ui-conversation` settle hide | C | 1 fork module (`packages/client/ui-conversation/src/client/skeleton/settle-hide.ts`) + 1 marked call site in `packages/client/ui-conversation/src/client/skeleton/ConversationMainPanel.tsx` | `.root[data-phase='settling'] .composerSeat { visibility: hidden }` is upstream-owned and unbounded, while both `settling` inputs are asynchronous facts that need not settle: a continuable child's `parentAvailable` is written only by a **successful** `subagents.list` read (both failure branches keep at most the previous value and notify nobody, re-selecting the child refreshes the child's own catalog, and a stalled read holds the manager's single-flight entry), and `Session.doOpen` awaits the first journal frame with no timeout, rethrowing a non-remote rejection with `openState` left `loading`. A page restored into a continuable child then hides the composer for its whole life — reproduced 2026-09-22 on the deployment as `settling` + `visibility: hidden` on the 136px seat with the 133-turn transcript painted, no retry on re-select, released only by a reload. The fork bounds one continuous pending period with `useSettleHide`/`SETTLE_HIDE_LIMIT_MS = 5000`, so the phase falls back to `hero`/`active` and the normal docked composer returns. Consumer: this deployment's desktop GUI. Verify: `pnpm exec vitest run packages/client/ui-conversation` plus the isolated-instance check in the Agent Note. Decision record: [note](.agents/notes/implemented/bug-fix/2026-09-22-bounded-settle-hide.md) |
| `api/session-controller/src/history.ts` fast-path injections | C | ~10 lines for `page()`, ~60 lines for `follow` | The paging injection delegates to fork-owned `src/fork/page-boundary.ts` (the indexed suffix read, its density and end proofs, and the 128/4096 margin ladder), and hands each read plan the request's page rule: `windowCut()` returns upstream's `paginate` (carrying the fork's Turn-aligned cut) bound to the request's `beforeSeq`, `maxMessages`, and `turnWindow`. The opening injection adds the synchronous service check and the windowed snapshot branch with its observation fallback, and yields that snapshot without activating the Session: a read-only open neither reads the whole log nor mounts it, and the first explicit resolution pays both. Both fast paths reach their persistence methods through the `seekSurface` helper, which binds them to the tracker proxy `ctx.get` answers: an extracted, unbound method runs with the wrapper object as `this` and throws inside every provider that reads its own state. A Session that cannot be mounted no longer reports `api-session/error` at the open either: the windowed path surfaces that failure at the first prompt, and only the observation path's `promote` still reports it immediately. Both fast paths take the cut from `windowCut()` instead of computing a page floor of their own, so each serves the page the observation path cuts; `IndexedPagePlan.seedMessages` (`request.turnWindow?.minMessages ?? request.maxMessages ?? DEFAULT_MAX_MESSAGES`) now only seeds the indexed read; `maxMessages` is the message floor the shared `paginate` widens to a Turn start, and an `exhausted` walk (no Turn start below the floor inside the window) marks that window unproven, so the fast path retries at the deep margin or returns `undefined` for the observation path instead of serving the slice |
| `api/session-controller/src/fork/open-window.ts` cold-open window | C | ~110 lines + `history.ts` branch | The opening snapshot's windowed read: the read plan over `readIndexedSuffix` (`page-boundary.ts`, which gains the windowed read, the optional plan fields `seedMessages`/`throughSeq`/`windowFloor`, and the proof-only accept test), cut by the request's own `windowCut()` rule, then the checkpoint gate, the closed-tail proof, and the fold into the snapshot. Upstream's `follow` folds the whole log and trims, which costs seconds of CPU and hundreds of MB of heap on the largest sessions; upstream has no indexed seek surface to delegate to (the fork's `seekable`/`messageCut`/`readFrom` live on its own persistence backend). The windowed read calls `seekable` first and bails on a false answer before any cut or read. `history.ts` keeps the marked try and its observation fallback, and yields the windowed snapshot without activating the Session |
| `session-projection-cache/src/fork/checkpoint-read.ts` checkpoint read + write-back | C | ~90 lines + `index.ts` registration | The windowed open needs the stored checkpoint rows by session id, and the first cold open must install the record the next one folds from. Upstream's public fold face returns viewed values, not seedable rows, and its `hydratePrepared` deliberately wrote nothing; the fork adds a symbol-keyed registration of the service's private lookup (a WeakMap cannot work: cordis hands callers a tracker proxy, not the registered object), the `readCheckpoint` read, and the durable-prefix write-back. `readColdSessionLog` reports `durableEventCount` so the written rows never pass the stored log end, and both upstream-owned files (`session-query/src/cold-read.ts`, `session-query/src/observation.ts`) carry marked injections for it |
| `session-persistence-sqlite` whole package + the seek surface (`seekable`, `messageCut`, `readFrom`) + the rewrite capability (`truncate`) | A (fork-owned since 0.1.2-rc.1) | 9 src files + `delete-event-row.sql` + `select-events-from-through.sql` + `select-max-seq.sql` | **0.1.5-rc.2 STATUS: adapted** — src/handle.ts (per-session handle, live write-behind, torn-tail truncation, in-process single writer) and src/restore.ts (legacy rows restore through `dsh-session-format-catalog`, v0→v3 migration on read, publish-on-write-open); `preparedSessionCacheSize` dropped (upstream removed the prepare/borrow seam), packed-row decode kept for legacy rows, schema 20 + 19→20 migration + `seed_length` unchanged; the package suite passes at 13 files / 176 tests with one skipped, and the upstream contract is 21/21 memory + 20/20 file-backed. | Upstream removed the package (JSONL-only decision) but kept the seam for out-of-tree providers; the fork keeps it ported to the handle-based `PersistenceBackend`/coordinator at upstream's final SCHEMA_VERSION 20 (`ignorable` column doubling as the packed-row sentinel; one-time in-place 19→20 migration; `seed_length` carries the inherited cut). `seekable(id)` is the fast paths' gate: true only for a current-format row, and it reads metadata only. It exists because `loadStoredFrom`'s historical arm restores the whole log through the format catalog and slices it by a `fromSeq` from the stored physical space, which the re-based restored seqs do not occupy — the deployed store is mostly such rows, and a window plan there paid a full read per attempt and got an empty page. The package also implements the seam's optional rewrite capability: `SqliteSessionHandle.truncate`/`SqliteStore.truncateLog` discard every stored event from the cut in one immediate transaction, rewrite the one packed row spanning the cut so only its members below it survive, bump the revision once, and roll back on failure; the handle re-bases its cursor and its shrink guard keys on the stored revision. The page-cache field this row registers is the [page-cache Config field note](.agents/notes/implemented/feature/2026-09-20-sqlite-page-cache-config.md) |
| `session-persistence` seam: optional `SessionHandle.truncate?` rewrite capability | C | ~20 lines in `src/handle.ts` + class JSDoc in `src/index.ts` | `SessionHandleTruncateOptions { signal?, append? }` and the optional `truncate?(toSeq, options?)` member: discard every stored event from `toSeq` on, land `append` (seq order, starting exactly at `toSeq`) in the same durable step, write handle only; rejects `SessionReadOnlyError` on a read handle and `SessionOwnershipLostError` when ownership is gone. A backend that cannot rewrite a committed log omits the method, and a consumer that needs the rewrite refuses loudly (`session/rewrite-unsupported`) instead of admitting the operation unmet — the fork's SQLite backend implements it, upstream's shipped JSONL backend omits it. The `index.ts` class JSDoc drops the absolute never-rewritten claim for this one truncation path. `pnpm run gen-cordis-api` copies that paragraph into the generated `packages/extensions/tool-cordis/src/api-catalog.ts` service description, so the generated file carries the marker text as generated prose and holds no hand patch |
| `api/session-controller` in-place rewrite orchestration | C | fork modules 52 + 55 lines; `commands.ts` +200/-6; `agent.ts` +62/-7; `index.ts` relay guard; `types.ts` 2 wire fields + 2 error codes | The pure cut validation lives in the fork module `src/fork/rewrite-history.ts` (`resolveRewriteCut`: the armed seq must be a stored `user/message`, subagent-owned Sessions refuse, the cut is the seq of the last `turn/start` at or below it extended backwards over that message's OWN admission `agent/inbox/spliced` inserts, refused below `inheritedEventCount`; `planInboxRepair` then folds the retained prefix and returns the one cancellation splice `truncate`'s `append` lands for a message that stayed pending). `commands.ts` carries the marked `createSeeded` injection, the `prompt` rewrite branch, and the two orchestration methods (`rewriteHistory`: queue-only, live-Agent idle-only, inbox-pending refusal, dispose → write-handle truncate + the balancing inbox removal `planInboxRepair` computed → close → discard the Session's projection-cache record; `truncateStoredLog` fails loud when `handle.truncate` is undefined). `agent.ts` carries the marked handle-retention field plus `disposeAgent`/`createSeeded` and the resume/create call-site changes. `types.ts` adds `SessionPromptRequest.rewriteFrom`/`SessionPromptValue.rewrote` and the `session/rewrite-unavailable` (typed reasons) / `session/rewrite-unsupported` codes. The fork module `src/fork/rewrite-hold.ts` holds back the `api-session/removed` announcement that the rewrite's Agent teardown would otherwise publish: `prompt` enters the hold before `rewriteHistory` and releases it once `resolveAgent` settles, `index.ts`'s `session/disposed` relay asks `commands.deferRemoval` first, and a window that ends with no live Agent re-announces the removal — without it every client dropped the row and the selection and rendered the new-Session frame for the rewrite's duration |
| `session-projection-cache` rewrite invalidation + seed water rule | C | `index.ts` +52/-2 (regions + class JSDoc) | `SessionProjectionCache.discard(id)` drops the stored record durably and clears a live Session's pending write-behind first; the rewrite calls it because the cache's identity check cannot see that a log moved backwards. The seed reads (`hydratePrepared`, `coldSnapshot`) additionally refuse a stored row at or past the durable prefix it would be folded into (`rowsWithinDurablePrefix`): the cache may be behind a log, never ahead of it, and a row folded from removed events otherwise restores a removed message as a queued item and replays it as a new turn. The zero-I/O listing read (`cachedSnapshot`) has no log to check against and relies on the writer's discard. No behavior changes for a log that only grows: every row of a normal checkpoint sits below the durable prefix its fold reached. The 0.1.7-rc.1 merge keeps the fork's three-argument `hydratePrepared(session, events, durableEventCount)` write-back and `rowsWithinDurablePrefix`; `cachedSnapshot` and `cachedPredecessorTitle` are upstream's own two-argument and one-argument faces |
| `session-controller` client + `ui-conversation` rewrite threading and window rebuild | C | `sessions/session.ts` ~35 lines; `input/facade.ts` ~30 lines; `hub.ts`/`service.ts` ~15 lines; wire fields in `contract/session.ts` + `contract/input.ts` | `SessionPromptOptions.rewriteFrom` rides the prompt wire request; `ClientSession.prompt` rebuilds the window (`resync()`) before resolving when the Host reports `rewrote === true`, and resolves a visible `session/rewrite-missed` failure when an armed prompt is admitted without `rewrote` (version skew with a not-yet-restarted Host) — never a silent append. The composer input machine (`ui-conversation`) holds `InputState.rewriteFrom`/`InputActions.setRewriteFrom` beside the draft: the armed value rides the default send path, clears with the committed draft, and restores with it on a failed send. The subagent-addressed continuation branch ignores the option |
| `session-query-sqlite` live-observation memo and per-request live-observation budget | C | ~15 lines in `index.ts`, the fork memo, and one allocation in each caller (`api/session-controller` `list.ts`, `tool-session-query` `operations.ts`) | The memo in `src/fork/live-observation-memo.ts` keys on the Session instance plus its O(1) log length (`session.seq`), so an unchanged attached Session is reused without materializing its events; the fork's previous key (`length:tailSeq:tailTime`) could only be computed after `session.snapshotEvents()`, that is after the read. `recompute` is now the only event read; `index.ts` keeps the field, the delegation, and `_chargeLiveObservation`, which charges each observed Session against `exec.liveObservationBudget` under the validated `maxLiveObservedEvents` Config bound (default 100,000; both callers allocate one budget per request) |
| `client/ui-layout` AppFrame mobile drawer | C | import + compose + 2 store fields (`stores.ts`) + the assertions in `packages/client/ui-layout/tests/layout-store.client.spec.ts` and `packages/client/ui-layout/tests/app-frame.client.spec.tsx` | **0.1.5-rc.2 STATUS: re-applied** — upstream's frame offers only a narrow rail below its 1024px auto-collapse, so the fork keeps its phone breakpoint (768px, matching the right Sidebar's own auto-fullscreen width): the left sidebar becomes a fixed overlay drawer and the frame renders one in-flow track. The details sheet retired — upstream's right Sidebar covers the viewport in fullscreen below 768px with its own exit affordance. The drawer's current-Session watch derives the session id from the list rows' retained `mainView` binding (`retainedBy.mainView`), because upstream 0.1.7-rc.1 removed the `SessionListState.current` field the fork's version read. | The regime hook and drawer chrome live in `src/client/fork/mobile-shell.tsx` (its own `MOBILE_VIEWPORT` since upstream's `columns.ts` dropped the constant); AppFrame's diff is the import, one `useMobileRegime` call, the drawer branch, the single-track template, `RightbarColumn`'s `mobile` attr (keeps the column out of that track), and two handle guards; the `sidebar` slot's owner share (`src/client/index.ts`) gains the `mobile` flag the drawer branch sets and the desktop branch clears; `stores.ts` adds `mobile`/`drawerOpen` plus `setMobile`/`setDrawerOpen` and a mobile branch in `toggleSidebar`; the drawer's label is the `sidebar.open` key the fork added to the `common` dictionary; the mobile chrome's CSS (`.rightbarCol[data-mobile]`, `.scrim`, `.mobileDrawer`, `.mobileMenu`) lives in `AppFrame.module.css` under one section marker (upstream's `.overlayLayer` sits inside that span) |
| `ui-sidebar` phone-drawer settings seat | C | `packages/client/ui-sidebar/src/client/SidebarRoot.tsx` (marked conditional) + `packages/client/ui-sidebar/src/client/SidebarRoot.module.css` (marked `.drawerLogoRow` block) + `packages/client/ui-sidebar/src/client/contract/slots.ts` (seat JSDoc) + `packages/client/ui-sidebar/tests/sidebar-root.client.spec.tsx` + `packages/client/ui-sidebar/tests/sidebar-styles.client.spec.ts` + the generated `packages/extensions/cordis-client-runner/src/client/slot-catalog.ts` (embeds the marked owner-props text verbatim) | **Retained** — upstream renders the `sidebar.settings` seat only at the column foot; in the fork's phone drawer that 42px full-width row sits directly under the session list at the thumb's resting spot, where a scroll or a session tap opens Settings. The frame's `mobile` flag moves the seat into the drawer's brand row directly after the brand, as the compact icon (`{ wide: false }`), and the foot keeps only `sidebar.footer.action`. The brand row packs from the leading edge while the toggle keeps the trailing edge, so the blank beside the brand is not a New Session target. The compact seat carries no connection control; the wide foot seat owns that control. |
| Generic client brand reads plain `DSH` | C | 2 dictionary values (`packages/client/locale/src/locales/en.ts`, `packages/client/locale/src/locales/zh.ts`) + 2 build defaults + test expectations | Upstream's generic brand is `DSH Local Build`; the fork's reads `DSH` (`brand.localBuild` in both locales drives the sidebar brand name and the document-title fallback, `apps/web/index.html` + `vite.config.ts`'s `DEFAULT_CLIENT_TITLE` carry the static default). A deployment title via `DSH_CLIENT_TITLE` still overrides it, and the build-version line under the brand is untouched. The same two dictionaries also carry the mobile drawer's `sidebar.open` key and the Think row's two keys, so adopting upstream's dictionary wholesale deletes all three |
| Conversation header mobile trim | C | two `max-width` media blocks (767.98px and 560px) | Below the drawer breakpoint the session header's leading edge clears the frame's fixed drawer opener (`padding-left: 56px` at ≤767.98px) and both blocks reserve the same top safe-area inset the opener's `top` carries (`padding-top: calc(10px + env(safe-area-inset-top))` at ≤767.98px, folded into the 560px `padding` shorthand), so a device with a status-bar inset keeps the button and the title on one line instead of dropping the fixed button a full bar below the title. At phone widths the header padding and tab rhythm tighten, the session-title breadcrumb keeps a `min-width: 140px` floor (raised from 88px in the same change that collapsed the two metadata chips to their counts) and truncates through `.crumb`'s ellipsis, `.headerActions` shrinks (`flex: 0 1 auto`, `min-width: 0`, deliberately without `overflow: hidden`, which would clip the job badge's absolutely positioned menu to nothing), and `.headerCorner` resets its desktop `-16px` right margin — that margin assumes the 28px desktop padding while the phone padding is 12px, which put the corner control 4px past the viewport at 360/412/480/560px. Upstream's module carries no media query at all, so adopting it deletes the phone padding and every rule above at once; `header-phone-styles.client.spec.ts` rejects that. The same block leaves the lineage slot alone on purpose: for a subagent crumb that slot renders the subagent session's own title (`switcher` variant) and it is the only phone path into the subagent catalog, since the drawer tree and session search filter subagent children out. The metadata that leaves the phone header is the agent-preset label instead (`ui-agent-preset` row below). The session-log half retired with 0.1.5-rc.2: upstream moved that control into the header's 28px ellipsis menu, which no longer crowds a phone header |
| `api/session-controller` `openWorkspacePath` desktop gate + `ui-chat` open-file routing and mention gate | C | gate stays inline (5 lines); routing and mention gating = fork-module import + 2 calls (`routeOpenFile`, `nativeGatedMentions`) and 3 marked sites in `packages/client/ui-chat/src/client/apply.ts`, pinned by `packages/client/ui-chat/tests/apply-inject.client.spec.tsx` | The open RPC consults `canOpenPath()` and fails fast (inherent one-liner); the routing decision and the native-gated mention wrapper live in `src/client/chat/fork/open-file-routing.ts` |
| `bundle/base` sandbox capability | C | row `disabled` flags + executor row names | **0.1.5-rc.2 STATUS: partially restored** — upstream's workspace-files and deliverables entries require the `sandboxPolicy` service, so the sandbox and sandbox-policy rows are enabled again with `mode: danger-full-access` (this deployment's only mode); the executor rows stay the local providers and the permission-presets row stays disabled. |
| `client/modules` + `client/web` deferred boot batches | C | `packages/client/modules/src/client/entries.ts` (marked Loader-entry reconciliation) + the boot manifest/resolver partition in `packages/client/modules` + the two-stage shell boot in `packages/client/web/src/boot.ts` | `WebBootBatchPhase 'deferred'` + `Config.defer` partition + two-stage boot; upstream-shaped (additive wire field, empty default); the defer list is deployment config, not repo state. The shell half carries the marked injections (`activateDeferred`, the pre-mount roster filter); the manifest/resolver half carries no fork marker, so the marker census cannot see a revert there — re-apply it from the diff against the fork's base tag |
| `ui-workspace` stable promotion head | retired | — | **Retired** — the fork's `nextSessionOrderAccount`/`reconciledSessionOrder` patch and its `src/client/fork/order-stability.ts` module are deleted: upstream 0.1.7-rc.1 derives the promoted head itself through `reconcileManualOrder`/`orderByRecency` in `src/client/tree.ts`, which `WorkspaceBrowser` and the store call directly |
| `ui-chat` message-actions DOM hook | C | 1 attribute in `MessageIconActions.tsx` | The action strip carries `data-message-actions` so third-party plugins can attach an action beside the native copy/branch controls. Upstream's 0.1.5 removed `data-time-hover-root`, which the fork's `dsh-rewind` plugin anchored to; without a stable hook the plugin silently renders nothing. The attribute is additive and invisible |
| `ui-tool` generic row `bodyContent` + `GenericToolCard` export | C | ~15 lines in 3 upstream files (`ToolRow.tsx`, `GenericToolCard.tsx`, `client/index.ts`) | **Retained** — the fork's out-of-tree `dsh-apollo` plugin keeps the generic collapsed row every tool renders and replaces only its expanded body: `ToolRow` takes an optional `bodyContent: ReactNode`, rendered as the body's first child at block level (outside the Input/Output card), which replaces the default Input (arguments) section — no argument formatting, no Input label, no empty card — and makes the row expandable by itself, while the Output section, the card chain, the summary, and the Inspect pill are unchanged and a row without the prop renders exactly as upstream's. `GenericToolCard` forwards the same prop and the browser entry exports the component and its props type; the value export is the deliberate, user-signed-off exception to the client export-discipline rule. Re-apply: the prop, the `inputBody` substitution for `cardBody`, the body's first child, the `GenericToolCard` forward, and the two entry exports |
| `ui-chat` StatsLine passive measurement | retired | — | Upstream replaced the stats strip with the two icon pills (`StatsPills.tsx`) and deleted `StatsLine.tsx` at 0.1.5-rc.2, so the post-paint ellipsis measurement has no successor to carry it; the row stays as runbook history |
| `skill` registry catalog restrictions | C | 17 lines in `index.ts` | All logic in `src/fork/skill-restrict.ts` (compile, per-scope store, chain filter); `index.ts` keeps the import, one field, and two marked delegating calls; allow/deny mutual exclusion documented in the module's JSDoc |
| `subagent` per-child cwd + skillFilter | C | seams + 5 call sites (`child-agent.ts`, `continuation.ts`, `continuation-activation.ts`, `subagent-in-process-driver`, `tool-subagent`) | Logic in `src/fork/child-scoping.ts` (`stampChildCwd`, `applyChildSkillFilter`); the request-field threading through `childSessionMeta`/continuation/driver is the seam that stays — at the 0.1.5-rc.2 sync the fork re-applied it as the `request.cwd` argument, the descriptor's `cwd`/`skillFilter` fields (version 4), and `skillFilter` in both continuable compositions (first activation from the request, cold resume from the folded descriptor); cwd authority stays the session header on cold resume **Re-apply list (2026-09-16 correction)**: the seam is four call sites, not two — `child-agent.ts` (`childSessionMeta` + `applyChildComposition`), `continuation.ts` (cold-resume meta + both continuable compositions), `packages/subagent/subagent/src/continuation-activation.ts` (the first-activation composition's `skillFilter` field, the third composition site), and `subagent-in-process-driver/src/index.ts` (`setup()` passes `request.skillFilter` to `applyChildComposition`; `agents.create()` passes `request.cwd` to `childSessionMeta`). The driver pair was silently dropped at the 0.1.5-rc.2 sync and missed by the re-apply commit itself, which is why this row now names every call site; `child-isolation.spec.ts` covers all four. The fifth site is the producer: `tool-subagent/src/index.ts` spreads the `cwd`/`skillFilter` its own instance config validated onto the start request it hands the driver (`src/fork/delegation-scope.ts`, registered with that package's alias-face row), and `fork-delegation-alias.spec.ts` covers it on all three start paths. |
| `session-format-v0-to-v1` released-v0 subagent descriptor compat | C | `src/fork/subagent-descriptor-compat.ts` + one marked delegation in each of 5 upstream files | The frozen released-v0 edge admits descriptor version 3 only, while the installed `@deepseek-ai/dsh-subagent` stamps version 4 into new payloads and stored 2/3 payloads predate version 4's composition inputs; the module owns the optional-member set, the current version, and the 2/3→4 upgrade, and `dispositions.ts`, `migration.ts`, `payload-validation.ts`, `validation.ts`, and `tests/relationships.spec.ts` each keep one marked delegation to it |
| `session-format-v3-to-v4` released-v3 descriptor-generation compat | C | `packages/session/session-format-v3-to-v4/src/fork/subagent-descriptor-compat.ts` + one marked export in `src/index.ts` and one marked delegation in `src/facts.ts` | The frozen released-v3 edge interprets descriptor versions 1 through 3 and degrades every other generation to unknown-mode catalog membership, while the installed `@deepseek-ai/dsh-subagent` stamps version 4 into new payloads; the module owns the interpreted generation set (`CURRENT_SUBAGENT_DESCRIPTOR_VERSION = 4`, `isInterpretedDescriptorVersion`) so a parent that already lists the child keeps the mode and label it carries. `src/index.ts` re-exports `childCatalogFact` beside `historicalChildCatalogSource` for a database-backed parent that validates one child's fact itself instead of refusing the whole parent over that child's unusable descriptor, and `src/facts.ts` delegates its version check to the module. Mirrors the released-v0 compat row above; consumer: this deployment's `session-persistence-sqlite` V4 restore, whose child-catalog read imports `childCatalogFact` |
| `subagent` released descriptor identity compat | C | `packages/subagent/subagent/src/fork/released-descriptor-identity.ts` + one marked delegation in `packages/subagent/subagent/src/projection.ts` | The installed descriptor generation is 4 (the per-child `cwd`/`skillFilter` row above), while a restored historical child keeps the generation its writer stamped (2 or 3) — the generation the released-v3 catalog row above already interprets for the parent's membership. The strict fold reads the installed version alone, so such a child served no identity and the session-controller address fence answered `subagent descriptor is corrupt` for a child the parent's own catalog listed. `foldReleasedDescriptorIdentity` validates a copy of a released payload stamped with the installed version through `foldSubagentDescriptor`, so identity comes from the same schema and an undeclared member or a damaged field still establishes nothing; `foldSubagentDescriptor` stays strict for the resume composition, and the `subagent` projection's `stateVersion` moved 2→3 so rows cached as the `null` sentinel refold. Consumer: this deployment's restored released children, covered by the package's released-descriptor-identity spec and the session-controller cold-session spec |
| `ui-chat` ChatView reflow-stable scroll anchor | C | `chat/fork/scroll-anchor.ts` + marked injections in the extracted reader hooks `packages/client/ui-chat/src/client/chat/use-chat-viewport.ts`, `packages/client/ui-chat/src/client/chat/use-chat-reading.ts`, and `packages/client/ui-chat/src/client/chat/use-chat-scroll.ts` | `chat/fork/scroll-anchor.ts` re-asserts the held reader row on every flow-column resize (fold collapse, image loads, disclosures), measuring the hold after the scroll write so consecutive callbacks stay idempotent, with ledger bookkeeping so reader-input attribution stays correct; anchored row hidden falls back to the nearest surviving visible row above. ChatView arms the anchor on every off-bottom scroll sample, re-holds the reader's row after each prepend compensation, captures the restored row on saved-position restore, and keeps the anchor across load-earlier settlement (the upstream clearing effect is removed). The 0.1.7-rc.1 merge moved those injections into the reader hooks upstream extracted: `use-chat-viewport.ts` keeps the reflow re-assert and the callback arm, `use-chat-reading.ts` keeps the off-bottom arm, the prepend re-hold, and the saved-position capture, and `use-chat-scroll.ts` keeps the load-earlier settlement |
| `api/session-controller` streaming snapshot cadence | C | 1 line + comment in `session.ts` | A live chunk presentation (`publishAssistantEntry`'s `transient` branch) marks the session snapshot with `markFrameDirty()` instead of `markDirty()`, so one streaming frame publishes at most once per animation frame. The frame scheduler already existed (`Notifier.markFrameDirty`) but had no production caller: every chunk ran a microtask flush and re-rendered every `useSession` consumer (measured 3 chunk frames → 3 notifications; now 3 → 1, and the final state is identical) |
| `api/session-controller` reset recovery for failed session windows | C | 9 lines in `manager.handleConnected` | A carrier reset aborts every logical stream; errored sessions re-open through `resync()` instead of freezing on the last frame until a full page refresh |
| `api/session-controller` ambient activity coalescing | C | import + field + 1 call + no-op identity contract in `manager.ts` | Continuous `api-session/activity` streams from other running sessions buffered per-session (latest timestamp wins) and flushed about once per second by `sessions/fork/coalesced-refresh.ts`; lone activities apply immediately, preserving the synchronous staging contract. `applyMutation` returns the input array reference when a mutation flips nothing, and `recordMutation` skips the dirty flush on that identity, so unchanged status/activity frames never rebuild the list |
| `api/session-controller` client selection notification + snapshot identity | retired | — | **Retired** — the fork's `src/client/sessions/fork/snapshot-identity.ts` and its spec are deleted: upstream 0.1.7-rc.1 builds entry identity inline in `manager.buildListSnapshot`, and the client session-controller no longer declares the `SubagentCatalogSnapshot` type (the same-named type in `ui-subagent` is that package's own). Selection still notifies via `markDirty`, and `open`/`openSubagent` still stage synchronously through `followCurrent` |
| `api/gateway` Remote stream mux permessage-deflate | C | ~35 lines | `RemoteStreamMuxServer` takes a `perMessageDeflate` flag from `Config.websocketPerMessageDeflate` (default off); RFC 7692 negotiation with `threshold: 1024` so journal `opened` window frames compress while live frames stay raw; the mux frame handling itself is untouched
| `ui-workspace` order-store reference stability | retired | — | **Retired** — `sessionOrderChanged` lived in the deleted `src/client/fork/order-stability.ts`, and its three-line `stores.ts` guard went with the module: the store now assigns the order upstream's `reconcileManualOrder` derives, which is also what `WorkspaceBrowser` calls. The 0.1.7-rc.1 sync adopted upstream's helper rather than re-applying the guard |
| `bundle/base/cordis.patch.yml` local-provider swap + permission disable | C | 5 row deltas + comments | Fork decision 2026-09-05 (danger-full-access-only deployment, FORK_CHANGES.md): executor rows `bash-sandbox`/`pwsh-sandbox`/`fs-sandbox` keep their ids and platform `!!js` gates but mount `@deepseek-ai/dsh-bash-local`/`dsh-pwsh-local`/`dsh-fs-local` instead of the sandboxing packages. Re-apply on sync: revert names/flags per the in-file fork comments; `permission` must track the sandbox rows (its constructor rejects composition over a non-confining executor). The `sandbox` row is upstream's `@deepseek-ai/dsh-sandbox-local` on both sides, and `sandbox-policy` is enabled with `mode` pinned to `danger-full-access` instead of upstream's `DSH_PERMISSION_MODE` default; only `permission` carries `disabled: true`. Adoption is blocked by this container (see the dual-path table): upstream's bash executor fail-closes without bwrap or Landlock |
| root `vitest.config.ts` disabled sandbox suites | C | 2 lists + spreads | `forkDisabledSandboxTests` drops `packages/sandbox/*`, bash-sandbox, pwsh-sandbox, fs-sandbox unit suites from collection; `forkDisabledSandboxCoverageExclusions` drops those sources from the per-file coverage gate. Restore path (re-enable base rows, delete the lists) is in the in-file comments |
| `vitest.e2e.config.ts` sandbox-family + ACP escalation e2e excludes | C | 2 exclude lists | Fork decision 2026-09-05 (FORK_CHANGES.md): the sandbox-family e2e suites (`packages/sandbox/*`, bash-sandbox, pwsh-sandbox, fs-sandbox, `*.e2e.ts`) and the ACP escalation suite (`apps/cli/tests/profiles/acp/tests/escalation.e2e.ts`, denial→escalation flow; keyless-smoke split hint in the comment) probe machinery the fork no longer mounts. Restore path in the in-file comments |
| `vitest.web.config.ts` permission-surface browser excludes | C | 1 exclude list | Fork decision 2026-09-05 (FORK_CHANGES.md): `settings-chrome`, `permission-policy-context`, `access-confirmation`, `seeded-history`, and `approval-composer` browser suites assert the Permission settings row / Access-mode picker / `/permission read-only` chip the fork no longer mounts — approval-composer replays the recorded approval session under `snapshots/web/approval-composer/`, but can only reach its approval-takeover subject by driving the Access chip into Read Only, and the chip renders nothing without the host permission service's `permissions` projection. Goldens stay upstream-shaped; restore path in the in-file comment |
| `vitest.expected.config.ts` sandbox-shaped expected excludes | C | 1 exclude list | Fork decision 2026-09-05 (FORK_CHANGES.md): `subagent-inheritance` and `image-offload` pin the read-only delegation events, sandbox-policy prompt context, denial output, and `Current DSH file policy` sentence the fork no longer produces, and `goal`, `headless`, `semantic-checkpoint`, `subagent-diagnostic`, and `workspace-context-resume` diff whole normalized sessions against goldens recorded on the upstream composition — their `permission/preset`, `sandbox/mode`, `approval/policy` rows and sandbox:policy runtime-context message never appear in fork logs (12 of the lane's 25 tests failed on the fork before this exclusion). Goldens stay upstream-shaped; restore path in the in-file comment |
| `vitest.snapshot.config.ts` ACP snapshot corpus exclusion | C | 1 exclude list | Fork decision 2026-09-05 (FORK_CHANGES.md): `snapshots/acp/acp.snapshot.ts` replays upstream-recorded sandbox denial→escalation→approval corpora (escalation trio plus the cancel/approval and schema-pin cases sharing its sidecars); measured 6/8 replays fail on the fork composition, and suite pin/inventory invariants forbid a scenario-list exclusion, so the whole file is uncollected. Restore path in the in-file comment |
| `apps/web/tests/shipped-composition.e2e.ts` sandbox absence assertions | C | 1 assertion block | Fork decision 2026-09-05 (FORK_CHANGES.md): replaces upstream `sandboxPolicy`/`permissionPresets` mode pins with `ctx.get(...)`-resolves-`undefined` assertions (approval stays `ask`). Restore path in the in-file comment |
| `apps/cli/tests/windows-shell.spec.ts` permission-row assertion | C | 1 assertion block | Fork decision 2026-09-05 (FORK_CHANGES.md): upstream's "permission surface never moves" loop asserted the sandbox/permission rows stay enabled; the fork asserts them `disabled: true` while ui-permission/fs-sandbox/approval stay enabled. Restore path in the in-file comment |
| `apps/cli/tests/github-webhook-real.e2e.ts` permission-preset pin removal | C | 1 conjunct | Fork decision 2026-09-05 (FORK_CHANGES.md): the keyed real test no longer requires the `permission/preset read-only` event (only producer permission-presets is disabled with the sandbox rows); title + webhook-source checks stay. Restore the conjunct with the sandbox rows |
| `ui-jobs` job badge phone width | C | the `.countCompact` default rule + ~21 CSS lines in the marked `@media (max-width: 560px)` block of `JobListAction.module.css` + 1 marked span in `JobListAction.tsx` | The badge label ("9 background jobs") wrapped inside the shrunken phone actions box into a 72px column and grew the header row from 30px to 78px, re-wrapping on every job change; the ellipsis fix that followed left the label truncated to "4 bac…" and spending the width the session title needs (measured at 412×915: title 88px, lineage chip 108px, job badge 65px). The phone block now hides `.count` and renders the component's `.countCompact` — the job count alone, keeping the running dot and the menu chevron — while the trigger's `aria-label` keeps the full localized count. `.trigger` keeps its `max-width: 100%` bound (an inline-flex box inside the shrinking `.root` otherwise resolves shrink-to-fit to its content's min-content and overflows the row instead of narrowing) and `.root`/`.trigger` keep `min-width: 0` without clipping, because `.root` is the containing block of the absolutely positioned `.menu`. A count-only chip needs the number in its own span for CSS to hide the label; the locale-formatted label stays the accessible name |
| `ui-subagent` header lineage chip phone count | C | 1 marked span in `SubagentHeaderLineage.tsx` + the `.countCompact` default rule and the `@media (max-width: 560px)` block in `SubagentHeaderLineage.module.css` | The lineage chip's full count label ("32 subagents") truncated to "32 su…" in the phone header row and took the width the session title needs (same measurement as the `ui-jobs` row). The phone block hides `.count` and renders `.countCompact` — the descendant count alone — while the trigger keeps its full localized `aria-label` and the `switcher` variant (a subagent session's own title in the breadcrumbs) is never touched. Upstream renders the label at every width, so adopting its module restores the crowded row; `subagent-header-lineage-phone-styles.client.spec.ts` rejects that |
| `ui-agent-preset` preset label phone removal | retired | — | **Retired** — upstream 0.1.7-rc.1 ships the same phone outcome in `AgentPresetLabel.module.css`: `@container (max-width: 540px) { .label { display: none } }`, so the fork's marked `@media (max-width: 560px)` block is deleted |
| `ui-settings-general` settings-modal portal + phone presentation | C | new `src/client/fork/portal.tsx` + 1 marked import at the module top and 1 marked wrap at the `SettingsPanel` call site in `SettingsRoot` (the panel's own body is unchanged) + ~70 CSS lines in the marked `@media (max-width: 560px)` block + `packages/client/ui-settings-general/tests/settings-root.client.spec.tsx` pins the mount point | The trigger lives in ui-layout's sidebar, which on phones renders inside the fork's transform-animated drawer; that drawer is the containing block for the modal layer's `position: fixed; inset: 0`, so at 412×905 the layer resolved to the 320px drawer (nav 188 + content 131, controls clipped, mask drawer-scoped) instead of the viewport. The layer mounts under `document.body` through the fork module; `react-dom`/`@types/react-dom` join the package devDependencies as ui-primitives declares them. Below 560px the panel fills the viewport as one column with a horizontal nav rail, a 36px close, and `env(safe-area-inset-*)` padding. Upstream has no portal here and no media rule in this module, so both rules are re-applied patches; the equivalent upstream onboarding modal (`ui-primitives` `Modal`) already portals, and matching it is the first preference if upstream moves the settings dialog onto that primitive. A marked comment-only edit in `ui-sidebar/src/client/SidebarRoot.tsx` records that the panel is no longer a fixed-position descendant of the sidebar column |
| `ui-chat` collapsed Think summary | C | 1 replaced expression in `ReasoningRow.tsx` (upstream's two line helpers deleted) + 2 CSS rules removed + `formatExactCount` added to `token-format.ts` + 2 locale keys + `reasoningSpans` folded from the settled message's timed stream (one `AssistantChatData` field, one Assistant Definition fold, one prop through `packages/client/ui-chat/src/client/chat/AssistantMarkdown.tsx`/`AssistantNodeView`) | **Retained** — the collapsed Think row carries the reasoning character count (`1,234 字符` / `1,234 characters`), plus the block's settled stream duration (`· 12.3s`), instead of upstream's first-line (settled) / streaming-tail preview, keeping the process scan free of reasoning prose; expanded content stays upstream's. The duration comes from the timed stream the settled message already embeds, so it adds no session read; the running block and any block without a recorded span keep the count alone. `formatExactCount` owns the exact-integer digit grouping that `formatExactTokens` delegates to; the two Think-bearing jsdom specs, the definition spec's span case, the lifecycle-chrome pin poll, the opt-in stress assertion, and the 19 comparable `snapshots/web/**` goldens move with it. A wholesale adoption of upstream's `ReasoningRow.tsx` or stylesheet silently restores the preview and its tail pin, and dropping the added export, contract field, or locale key leaves a caller uncompilable |
| `subagent` per-instance model-alias face + per-child scope | C | `src/index.ts` +138/-14 (nine marked sites for the alias face, four upstream expressions replaced in place, plus the guard's three) + fork modules `src/fork/model-alias.ts` (190 lines), `src/fork/delegation-scope.ts` (108 lines), and `src/fork/config-surface.ts` (65 lines) + `tests/fork-delegation-alias.spec.ts` (44 tests) | **Retained** — the official delegation tool now carries the alias face this deployment used to buy from the out-of-tree `dsh-subagent-dispatch` plugin, so its nine tool faces (the generic `subagent` tool plus eight role tools) mount as `tool-subagent` instances. Four config keys, all omission-preserving. `models` is the instance's alias table (`{alias, provider, model, reasoningEffort?}[]`): when present it REPLACES upstream's `provider`/`model`/`reasoning_effort` model-facing parameters with one optional `model` alias, and the table IS that instance's authorization surface — an alias outside it fails loud at call time naming the allowed ones, and the session-level `subagent-model-selection` policy plays no part for an alias instance. `defaultModel` is the alias a call without `model` uses (must be in the table; default: the first entry). `cwd` (child workspace; load-time "non-empty absolute path + existing directory" validation) and `skillFilter` (`allow`/`deny`, exactly one direction, validated in full: object, known keys, string arrays) ride the start request; the in-process driver folds `cwd` into the child's session header. Both alias/scope modules validate at load, so a misconfiguration rejects the mount instead of the first delegation. `models` together with `modelSelectionSettings: true` fails loud at mount (two model-authorization paths with different semantics), an alias instance requires a provider with the `agentOptions` capability (the alias always sets a child route), and the resolved `{provider, model, reasoningEffort}` is preflighted against the live `llm` adapter at call time, so a mount needs no `llm`. With `models` omitted, the upstream face and route resolution stay byte-for-byte: the package's 126 pre-existing upstream tests are untouched and green beside the 44 new ones (170/170 in 6 files). Re-apply: the three `src/fork/` modules verbatim plus the nine marked sites — the two imports, the four config-key declarations, the schema keys with their omission-preserving `default(undefined)` (Schemastery would otherwise materialize `models: []`, read as "aliases configured, none allowed"), the two resolvers and the mutual-exclusion check in `apply()`, the provider-capability guard, the description suffix, the alias parameter arm, the route-resolution/preflight branch, and the two request spreads — plus, for the guard, the import at `src/index.ts:44`, the derived key set at `:200`, and the `apply()` first-line call at `:388`. **2026-09-16 addendum — top-level config-key guard**: `src/fork/config-surface.ts` (65 lines: `allowedConfigKeys(dict)` + `assertKnownConfigKeys(config, allowed)`) closes the silent-passthrough hole this migration opened. Cordis resolves the plugin config through Schemastery's NON-strict object schema (`vendor/schemastery/src/index.ts`: `Schema.resolve(data, schema, options, strict = false)`, and `Schema.extend('object', ...)`'s `if (!strict) merge(result, data)`), so an undeclared top-level key is merged straight into the resolved config; the out-of-tree `dsh-subagent-dispatch` carried its own `assertKnownKeys` and the migration dropped that guarantee, so a preset row spelling `models:` as `modelz:` mounts the upstream face instead — no `model` parameter, an inert `defaultModel`, children inheriting the parent's route — with nothing to see. The guard restores fail-loud as the first statement of `apply()` (`src/index.ts:388`, ahead of the `models`×`modelSelectionSettings` mutual-exclusion check) and its message names the unknown keys against the allowed set. The allowed set is derived, not restated: a Schemastery object schema hangs its declared-key map on the schema object, so `Config.dict` is reachable at runtime and the set is the 13 declared keys — upstream's nine (`provider`, `toolName`, `modelSelectionSettings`, `enableRunInBackground`, `backgroundMode`, `agentOptions`, `persona`, `toolFilter`, `maxDepth`) plus the fork's four (`models`, `defaultModel`, `cwd`, `skillFilter`) — with the module's literal list only the `dict === undefined` fallback, so an upstream key added to `Config` widens the accepted set with no second list to edit. The typo's other half is a lone `defaultModel`: with `models` absent the alias face is gone while the key stays inert, so `src/fork/model-alias.ts:76-86` throws the message "tool-subagent: `defaultModel` is configured without `models` — add the alias table or remove `defaultModel`". Evidence: the guard's five-case block in `tests/fork-delegation-alias.spec.ts` (unknown key rejected at mount naming the allowed set, the same rejection when `apply()` bypasses Schemastery, the derived set proven equal to `Config.dict`'s keys beside a complete literal fallback, an upstream-only config still mounting with no fork field materialized, and `defaultModel` without `models`), which move this row's counts 178→190 lines, 38→44 cases and 164→170 tests in 6 files; `npx tsc -b tsconfig.host.json` exits 0 |
| `tool-agent-team` non-member request render | retired | — | **Retired** — upstream 0.1.7-rc.1 renders the `team:policy` system-prompt section from the static `POLICY` constant and moved role and name into teammate system reminders, so the fork's `tryMembership` render and its regression test in `tests/tool-team.spec.ts` are deleted. `tryMembership` survives only as upstream's `maybeInstall` creation-time gate |
| Fork-surface verifier `scripts/verify-fork-surface.ts` + `scripts/fork-surface-baseline.txt` | A | 1 script + 1 frozen baseline | The fork's own registration checker: a read-only census proving every fork marker under `packages/`, `apps/`, `vendor/` is reachable from a row of this inventory, every file a tier C row names still carries its marker, no row names a path no tracked file matches, and this file and its Chinese twin keep the same heading structure and row count. Findings outside the baseline fail the run (`--json` for machine output, `--update-baseline` to refreeze after review); the baseline may only freeze accepted matcher limits (rows that register a surface by role, package or context instead of by a path the checker recognises), never a real marker or registration gap. Runs as runbook step 3 — before walking the inventory — and as the `fork surface` lefthook pre-push job. Upstream will never have these files, so a sync adopts them verbatim |
| Top-level `deliberations/` roundtable crate | retired | — | The masked-tool-payload deliberation record (19 files, adopted 2026-09-05 as a docs-only Tier-A addition carrying the 0.1.2-alpha.1 absorb's roundtable and worklog) had zero references anywhere in the repository — the private knowledge store keeps the original crate — so the 2026-09-19 consumer audit deleted the copy: the fork tree carries no reference-free additions |
| `ui-conversation` composer chord-only submission | C | 5 marked files + goldens: the Enter guard in `packages/client/ui-conversation/src/client/input/editor/keymap.ts`, the chord arm and the deleted empty-draft steer in `packages/client/ui-conversation/src/client/input/editor/view-binding.ts`, 3 marked edits in `skeleton/InputBar.tsx` (constant `'enter'` gesture, deleted empty-draft steer branch, deleted steer placeholder arm with the `canSteerQueue` derivation), the `settings.enter.description` values and the deleted `placeholder.steerQueue` keys in `client/locales.ts` (zh+en), 3 `describe.skip`s in `apps/web/tests/steering.e2e.ts` (the two chord-inversion scenarios and the whole-queue steer scenario) plus its converted Control+Enter submits, the default-placeholder wait in `apps/web/tests/subagent-interrupt-ui.e2e.ts`, and the `queued-image` / settings-dialog golden copy | **Retained** — upstream's composer submits on plain Enter, inverts the delivery mode on the Cmd/Ctrl chord, and steers the whole queue from an empty-draft chord, with no setting that demotes Enter to a newline; the owner wants accidental sends impossible. Plain Enter and Shift+Enter insert native line breaks; only the chord submits, resolving to the Send button's mode (the busyEnter preference, never inverted), and the empty-draft steer is deleted because a double chord press would steer the just-queued message into the running turn ([note](.agents/notes/implemented/feature/2026-09-19-composer-chord-only-submission.md)). The submission policy, the composer wire contract, the input facade and hub, and the submit machine stay upstream: the `accelerated` gesture keeps its direct unit tests and `steerQueue` keeps `service-orchestration` coverage; QueueDock per-row Steer is the remaining queue-into-turn path. Re-apply: the keymap guard sits after the IME guard and menu arbitration, before `preventDefault()` (null-event synthetic Enters still submit); the InputBar deletions remove the branch, the hint arm, and the derivation together with the two dictionary keys; the three `steering.e2e.ts` describes pin upstream gestures on recorded fixtures and restore (with re-record) only when this row retires |

| `session-persistence-sqlite` session-log thread-pool decompression (`asyncCodec`) | A | `src/compression.ts` `ScannedRows`/`StoredRowScan`/`scanRowsOnThreadPool`/`HYDRATION_WINDOW = 8` (~100 lines) + `src/store.ts` scan selection (1 field, 2 awaited cold-read scans) + `src/index.ts` `Config` field + `tests/compression-async.spec.ts` + `tests/async-codec.spec.ts` + `tests/page-cache-async-composition.spec.ts` | **Retained** — the package is fork-owned, so the switch adds no merge surface outside its own files. A production 61k-event Session costs 36.4 CPU·s to cold-open and the synchronous `zstdDecompressSync`/`zstdCompressSync` calls here dominate it, all of it blocking the event loop. With `asyncCodec: true` the two cold-read scans (`loadStoredLog`, `loadStoredFrom` — the only scans that read outside a transaction) hand each compressed data column to `zlib.zstdDecompress` on the libuv pool in a bounded window and then run the same `scanRows` over the hydrated rows, so the decoded prefix and every classification (generic data, torn tail, committed corruption, malformed row) match the synchronous codec; a column the pool cannot decode keeps its compressed value for the scan's own per-row decode to classify. Decodes a write transaction holds (`physicalSpanFrom`'s append tail, `commitRepair`, `truncateLog`) stay synchronous: an await between `begin` and `commit` on the store's single connection would make every other operation's `BEGIN` fail with the nested-transaction error for the length of the decode. Compression stays `zstdCompressSync` in both states — the asynchronous compressor writes a streamed frame without Frame_Content_Size and cannot reproduce the one-shot `ZSTD_compress2` bytes — so on and off still store byte-identical rows. Default off; the consumer is this deployment's `session-persistence-sqlite` profile row, which carries the switch beside `cacheSizeKib`. Decision record: [async codec note](.agents/notes/implemented/feature/2026-09-20-async-session-codec.md) |

| `subagent` catalog listing cold-read bounds (`coldReadConcurrency`, `coldReadBudget`) | C | `packages/subagent/subagent/src/index.ts` (the validated `Config` with its two schema keys and defaults, the resolved `listingLimits`, and the one listing call site that spends them, `listDescendants`) + `packages/subagent/subagent/src/list-children.ts` (the `SubagentListingLimits` and `ColdReadJob` types, the projection-cache rung hoisted ahead of the budget, `selectColdReads`, `observeColdIdentity`) + `packages/subagent/subagent/tests/list-children.spec.ts` (7 cases in the `SubagentRuntime cold-read bounds` block) | **Retained** — upstream starts one identity read per non-live child behind a hardcoded concurrency of 4, so a cold projection cache over a 422-subagent workspace decodes whole stored logs once per non-live child per pass: measured on the production-isomorphic isolated instance, the first GUI load ran 423 such decodes per pass across 4–5 passes and pinned one core at 100% for about 8 minutes while every RPC and stream stalled and clients timed out and reconnected. A hot cache performs zero reads, so the cost is a tail risk that any cache invalidation (a new `DSH_HOME`, one projection unit's stateVersion change, a rewrite discard, lost storages) turns into the cold case. Upstream has no per-listing read bound and no field to set one — only the hardcoded concurrency constant — so both bounds are the fork's: `coldReadConcurrency` (default 4, the ported constant) caps reads in flight, and `coldReadBudget` (default 64) caps the reads one listing starts. The budget counts real Session reads: a candidate the projection cache can answer is resolved before selection, so it spends neither the budget nor a concurrency slot — which is what makes repeated listings advance monotonically and keeps a newly created child from being permanently `unavailable` behind a cache-served head. Candidates past the budget keep their corpus position and report the existing retryable `{kind:'diagnostic',reason:'unavailable'}`; no new wire vocabulary, and cancellation is unchanged. Verify: `npx vitest run packages/subagent/subagent/tests/list-children.spec.ts` — the block counts observations per listing, holds reads open to pin the concurrency peak, re-lists after a cache fill, and proves the shipped defaults apply to a directly constructed plugin. Its markers sit below the `Config` declaration, because the generated configuration catalog pins that declaration's line. Decision record: [note](.agents/notes/implemented/bug-fix/2026-09-21-subagent-listing-cold-read-bound.md). The 0.1.7-rc.1 merge narrows the bounds to the descendant walk: upstream's `listChildren` answers from the parent catalog projection and performs no cold read, so it passes no limits, and only `listDescendants` spends them |
| `api/gateway` Remote stream carrier lifecycle diagnostics | C | `packages/api/gateway/src/index.ts` (the `diagnosticsSlowMs` `Config` field with its schema bound and default, the stderr sink constant, and the diagnostics argument at the mux construction) + `packages/api/gateway/src/stream-server.ts` (the `RemoteStreamDiagnosticSink` and `RemoteStreamDiagnostics` types, the diagnostic constants, acceptance/heartbeat-causation/opened-stream tracking, and the four report sites) + `packages/api/gateway/tests/stream-server.host.spec.ts` (7 cases) and `packages/api/gateway/tests/gateway-stream.host.spec.ts` (2 cases) | **Retained** — the production browser-side mux WebSocket rebuilt every ~15 seconds for hours while this path logged nothing, so a heartbeat terminate, a delayed ready/first frame (the event loop occupied elsewhere), and a peer or proxy close were indistinguishable; traefik recorded 468 connections on the same route in 24 hours, a median lifetime of 14.5 s, and peaks of 90–135 connections per hour. Upstream's mux has no carrier-lifecycle output and no injectable sink, so the fork adds one optional `RemoteStreamDiagnostics` (sink + `slowMs`) and reports four single-line diagnostics: a first item slower than the threshold (endpoint + elapsedMs), a heartbeat tick the event loop delayed past its interval (driftMs, at most one line per 5 seconds), a heartbeat terminate (missed-Pong count), and one line per socket close (code, reason clipped to 120 characters, socket lifetime, logical streams opened, heartbeat-caused flag). The sink writes `process.stderr` because this tree registers no console exporter — cordis's built-in exporter only appends to a memory ring buffer — so `ctx.logger` lines never reach an operator log, while the deployment's supervisor folds stderr into the service log; the sink stays injectable for a composition that does serve an exporter. Healthy traffic emits only the per-close line, and no message carries stream payloads or Session values. Verify: `npx vitest run packages/api/gateway/tests/stream-server.host.spec.ts packages/api/gateway/tests/gateway-stream.host.spec.ts` — the cases pin every line, the threshold bounds, the 5-second throttle, and the stderr routing through the composed Host. Its markers sit below the `Config` declaration, because the generated configuration catalog pins that declaration's line. Decision record: [note](.agents/notes/implemented/feature/2026-09-21-remote-stream-carrier-diagnostics.md) |

| `session-persistence-sqlite` retained decoded logs (`decodedLogCacheBytes`) | A | `src/index.ts` `Config` field (the `decodedLogCacheBytes` schema key and its JSDoc marker) + `src/store.ts` (`DecodedLogCacheEntry`, the `decodedLogs` LRU map with its byte counter, the revision-checked reuse in `loadStoredLog`, `memoizeDecodedLog`/`forgetDecodedLog`/`commitSessionMutation`, the `materializeHeader` revision bump, the `close()` clear) + `src/compression.ts` (`DecodedTextMeasurement`, `ScannedRows.decodedBytes`, the `Buffer.byteLength` charge in `decodeRow` and `decodeScalarRow`) + `src/restore.ts` (the header freeze the shared header needs: the current-format codec spreads its decoded header into a new unfrozen object, and the header record's primitive fields make the shallow freeze complete) + `tests/decoded-log-cache.spec.ts` (24 cases) + `tests/packed-charge.spec.ts` + `tests/hit-no-event-read.spec.ts` + `tests/resources/sql/insert-schema-19-multibyte-events.sql` + `tests/resources/sql/delete-session-row.sql` + `tests/resources/sql/update-session-cwd.sql` + `tests/decoded-text.ts` | **Retained** — the package is fork-owned, so this adds no merge surface outside its own files. Upstream has no equivalent: every whole-log read decompresses, parses, validates, and freezes the stored rows again, so resuming a Session, reconnecting a client, or re-listing a catalog rebuilds an identical object graph each time. On this deployment's 66,736-event Session that repeat read measured about 3.5 s cold against about 0.30 s once retained while a hit still paid the row scan (about 11×), with the two hits returning the same frozen object (`identitySame: true`); moving the decision ahead of the event-row query makes a hit read one session row and no event rows (80.6 ms → 0.025 ms median, p95 0.342 ms, cold read unchanged on a synthetic 66,000-event Session); the retained log for that Session is charged 252.2 MB of decoded JSON text. The store keeps whole decoded logs per connection behind a check the read already pays for: a hit requires the revision read from the session row in the same `readTransaction` and decides before the event rows are queried, so a hit prepares one `select-session` and no event-row statement, and a write through this provider misses, from any connection or process, instead of returning rows the database no longer holds. Every local write path bumps that revision inside its transaction and drops the session's entry after commit — header materialization included, because it rewrites the row the hit is keyed by — and the ceiling charges each entry the UTF-8 `Buffer.byteLength` of its decoded JSON text, evicts least-recently-used entries, and never retains a log larger than the whole ceiling. Default `0` is the released behavior with nothing retained; a deployment sizes it from its profile row beside `cacheSizeKib` and `asyncCodec`. The limits live in the package README (the charge is a lower bound on the retained graph, and out-of-band SQL damage can be masked until eviction). It composes with the `asyncCodec` row above: that switch moves the decode a miss still pays to the libuv pool, this one skips the decode on a hit. Verify: `npx vitest run packages/session/session-persistence-sqlite/tests/decoded-log-cache.spec.ts` (24 cases), `npx vitest run packages/session/session-persistence-sqlite/tests/packed-charge.spec.ts`, `npx vitest run packages/session/session-persistence-sqlite/tests/hit-no-event-read.spec.ts`, and the package suite (`npx vitest run packages/session/session-persistence-sqlite/tests` → 13 files passed, 175 passed / 1 skipped of 176). Decision record: [note](.agents/notes/implemented/feature/2026-09-21-decoded-log-cache.md) |

| `ui-workspace` session hover card removal | C | ~290 changed lines (231 deleted / 62 added) across 5 marked files: `packages/client/ui-workspace/src/client/rows/Rows.tsx` (the `HoverCard` return, `SessionHoverContent`, `hoverTimeLabel`), `packages/client/ui-workspace/src/client/rows/Rows.module.css` (`.hoverStatus`), `packages/client/ui-workspace/src/client/locales.ts` (`time.ago` in both dictionaries), `packages/client/ui-workspace/tests/rows.client.spec.tsx` (3 card cases and the card halves of 4 row cases), `apps/web/tests/workspace-management.e2e.ts` (the dwell/copy case), plus the package README pair | **Retained** — the owner removed the sidebar's session-row hover card: the row itself carries the title, the compact relative time, and the status dot, so the card only duplicated the row while following the pointer through the session list. Upstream's card (full title, ago-wrapped relative time, one status line per live status, and a whole-card copy of the title) is deleted rather than hidden, because a hidden surface leaves code the per-file coverage gate would keep demanding tests for ([note](.agents/notes/implemented/simplification/2026-09-22-session-hover-card-removal.md)). The Workspace-row card is upstream's and stays, so `HoverCard` and the `.hoverContent`/`.hoverTitle`/`.hoverPath`/`.hoverTime` rules keep their consumer. Re-apply after a sync: `SessionNodeItem` returns its row element at the marked site instead of the `HoverCard` wrapper; `SessionHoverContent` and `hoverTimeLabel` are deleted; the `.hoverStatus` block is deleted; both dictionaries drop `time.ago`; the session-card cases leave the row spec; the dwell/copy case leaves the workspace-management browser spec. Removing the wrapper also removes the `display: block` span around every session row; `.groupSection > * + *` keeps the 2 px row spacing, and the row's own drag markers were already positioned against the row |

| `ui-workspace` session-row `⋯` menu contribution point | retired | — | **Retired** — upstream 0.1.7-rc.1 ships real slots for this seat: `sidebar.workspaces.session.menu.item` and `sidebar.workspaces.session.row.action` are `list` slots every row registers into (this package's own Pin/Rename/Fork/Archive actions included), so the fork's `sessionRowMenu` registry module, its four marked injections, and `tests/session-row-menu.client.spec.ts` are deleted. Consumer migration required, and still open: the out-of-tree `dsh-session-snooze` plugin this deployment mounts injects the deleted `sessionRowMenu` service in its own `packages/session-snooze/src/client/index.tsx` and must re-register its row through `sidebar.workspaces.session.menu.item` |
| `packages/guard/turn-continuation` guard plugin | A | 1 package (`src/index.ts` + `tests/turn-continuation.spec.ts`) + its README pair and Agent Note | **Fork-owned**, added 2026-09-20; upstream ships no such package, so a sync adopts it verbatim. The plugin opens one more turn after a turn closes with a configured reason: `continueOn` (default `['max-tokens']`) and `maxConsecutive` (default `2`) are validated `Config` fields, a `continueOn` entry outside the continuable reasons (`max-tokens`, `error`) or a non-whole `maxConsecutive` fails the load, and `maxConsecutive: 0` disables continuation without unmounting. The budget counts consecutive continuations per human turn in a `WeakMap` keyed by the exact Agent and refills only on an `agent/inbox/claimed` message whose `source.kind` is `user`; every continuation is a durable `user/message` sourced `{kind: 'plugin', plugin: 'turn-continuation'}`, and continuation authority stays process-local. Consumer: this deployment's own profile row enables it — no shipped profile mounts it, so confirm that row (it lives outside this repository) after a sync; the row retires with it. Decision record: [note](.agents/notes/implemented/feature/2026-09-20-turn-continuation-policy.md) |
| `api/gateway` journal frame pacing | C | 1 fork module (`packages/api/gateway/src/client/fork/stream-frame-pacing.ts`, 96 lines) + 1 marked import and 1 marked call in `packages/api/gateway/src/client/journal-stream.ts` (`consume()`) + `packages/api/gateway/tests/stream-frame-pacing.client.spec.ts` (10 cases: 7 unit, 3 integration) + one line in the gateway client compiler face | **Retained (added 2026-09-25)** — measured defect: on a phone-class client (412×915, DPR 2.625, `Emulation.setCPUThrottlingRate(4)`) 228 s of live streaming produced 1352 long animation frames and 164.6 s of blocking, 72% of wall time, worst frame 2927 ms, while style and layout together totalled 61 ms. Mechanism: `StreamInbox` is an unbounded deque whose `next()` resolves on an already-resolved promise while frames are queued, so one WebSocket message task drains the whole downstream chain — frame decode, session fold, conversation assembler, store publish — through microtasks with no return to the event loop, and the renderer never gets its turn. The fork records one consumed frame and, once a bounded batch is spent (`FRAME_PACING_TIME_BUDGET_MS = 4`, `FRAME_PACING_FRAME_BUDGET = 32`), awaits one `MessageChannel` macrotask raced against the stream's `AbortSignal` (no timer fallback: every supported host defines `MessageChannel`): frame order, terminal and error frames, cursor state, `options.failed`, and dispose quiescence are unchanged, and the budget always applies, so a slow one-frame-per-message stream returns to the event loop once per frame while a turn still carries at most one frame's own work plus the cheap frames that fit the remaining budget. Consumer: this deployment's Web GUI. Upstream has no yield point in that loop, so the row retires when upstream bounds its own frame consumption. Re-apply after a sync: copy `src/client/fork/stream-frame-pacing.ts`, then re-add the marked import and the marked `await afterFrame(this.signal)` call at the top of `consume()`. Verify: `npx vitest run packages/api/gateway` (10 files / 379 tests green), `npx vitest run packages/api/session-controller` (46 files / 960 tests green), `tsc -b` on the gateway client compiler face, and the new spec's three integration cases fail with the injected call removed |

| `api/session-controller` + `session-query` + `session-persistence` Session-list row scope | D | ~900 lines across 12 upstream-owned source files plus 2 SQL resources: `packages/api/session-controller/src/index.ts` (the `list` Remote and `listScopeOf`), `packages/api/session-controller/src/{list.ts,types.ts}`, `packages/api/session-controller/src/client/sessions/{manager.ts,service.ts}`, `packages/session-query/session-query/src/{corpus.ts,index.ts,types.ts}`, `packages/session/session-persistence/src/index.ts` (the `SessionPersistenceListSelection` seam and the shared `matchesListSelection` predicate), `packages/session/session-persistence-jsonl/src/index.ts`, `packages/client/ui-session/src/client/index.ts`, `packages/client/ui-conversation/src/client/skeleton/ConversationSession.tsx`; the fork-owned `session-persistence-sqlite` adds `packages/session/session-persistence-sqlite/resources/sql/select-listed-sessions.sql` and `packages/session/session-persistence-sqlite/resources/sql/select-parented-sessions.sql`, routed from `packages/session/session-persistence-sqlite/src/store.ts` and registered in `packages/session/session-persistence-sqlite/src/sql.ts` | **Retained (added 2026-09-25)** — measured defect: the sidebar's first list carried every stored Session, 6,015 rows and 2.58 MB on the wire on the production deployment, because `session/list` had one unpaginated, unfiltered form and the client pulled it whole. Upstream 0.1.7-rc.1 still ships that single form and has no storage-side row selection, so the fork owns the whole scope: the `scope`/`parentSessionId` wire fields and their `SessionListScope` translation, the selection through `session-query` into `SessionPersistence.list`, the SQL row selection, and the client's on-demand children read with its ancestry walk, retention, membership authority, and reconnect re-read. The 2026-09-26 partial-observation sweep added the fences that keep a partial observation from driving state: `readChildren` discards a response from a superseded Host generation (`childReadGeneration`, advanced by `handleConnected`) and keeps out a child whose removal frame is newer than the response (`removedChildIds`, cleared by an add or a fresh generation); `UiSession`'s status reconciliation distinguishes a Host-list member that left from a stored-but-never-listed projection row (the `listBefore` snapshot), so a catalog-derived child keeps its running/unread status through a generation reset; and the `ui-subagent` lineage switcher starts the lazy parent-catalog read when the tree opens, so a restored subagent address no longer shows a notice nothing resolves. Re-apply after a sync, in four layers: (1) keep `listScopeOf` in the session-controller `list` Remote and the `SessionListRequest` fields; (2) keep `listSessions(signal, scope)` threading the selection into `persistence.list(options)` in the query `corpus.ts`, and delete any record-level list filter the corpus regrows — the predicate moved into the seam; (3) keep `SessionPersistenceListSelection`, `matchesListSelection`, and the JSONL header and pending-row filtering, plus the two SQL resources and their `SQL_RESOURCES` entries; (4) on the client keep `refreshList`'s `{ scope: 'listed' }` request, `loadChildren`/`readChildren`/`ancestorParent`/`retainedChildIds`/`retainScopedChildren`/`forgetRetainedChild`/`knownChildParents`, and `UiSession`'s `observedRunning`/`presentBefore` guard, plus the partial-observation fences recorded in the dedicated row below (`childReadGeneration`/`removedChildIds` in the manager, the `listBefore` snapshot, and the switcher's open-time read). Verify: `NODE_ENV=test npx vitest run packages/api/session-controller packages/session-query packages/session/session-persistence-sqlite packages/session/session-persistence-jsonl packages/client/ui-session packages/client/ui-subagent`; the new specs are `packages/session-query/session-query/tests/session-list-scope.spec.ts`, `packages/session/session-persistence-sqlite/tests/session-list-scope.spec.ts`, `packages/session/session-persistence-jsonl/tests/list-scope.spec.ts`, the session-controller manager's `scoped children` block, and the ui-session scoped-status case |
| `api/session-controller` client session-list rebuild gate | C | `client/sessions/projection-store.ts` change-aware writes + the per-key `subscribeAny` channel + the key-gated dirty mark in the manager's store listener + `LIST_IRRELEVANT_PROJECTION_KEYS`/`affectsSessionList`/`reconcileListState` in `client/sessions/fork/coalesced-refresh.ts` + the batched `mergeChildren`/`mergeSummaryRows` children merge in `client/sessions/manager.ts` + one marked call in `client/sessions/service.ts` + the matching specs | **Retained (added 2026-09-25; extended 2026-09-26)** — measured defect: a phone-class client with no session open spent about 300 s of JavaScript inside a 310 s window (496 long animation frames, 280.9 s blocking, style and layout 13 ms) because every control frame of every attached Session marked the list dirty unconditionally, and each rebuild minted a fresh row per session and always called `list.set`. `projection-store.ts` reports whether an accepted frame's published value actually changed (an equal value advances the sequence watermark, keeps the cached value and its reference, and notifies nobody), and `subscribeAny` delivers the changed key per change so the manager can decide per key; the manager's store listener marks the list dirty only when `affectsSessionList(key)` is true — the deny list exempts `sessionStats`, `contextPressure`, `contextBreakdown` (keys whose only readers are session-scoped `useProjection` bindings), an unlisted key still rebuilds, and `tokenUsage` stays out because the subagent lineage reads it from the row map — and `handleControlFrame` no longer self-marks (the store channel owns the decision); `reconcileListState` keeps the published row object per matching row and returns the published state object when ids, rows, order, phase and projections are unchanged, so `list.set` is skipped. The 2026-09-26 extension merges a children read in one pass: `readChildren` hands its whole response to `mergeChildren`, which upserts every row through `mergeSummaryRows` (one O(rows+children) pass, one mutation-log write, one dirty mark, one `updateParentAvailability`) instead of one full-array rewrite per child. Measured: 200 frames carrying only `sessionStats` (20 sessions × 10 changed values) went from 22.1 ms / 10 rebuilds to 0.6 ms / 0 rebuilds, and one 2,244-children read from 89.0 ms to 8.9 ms (production reported 99 ms). Consumer: this deployment's Web GUI. Re-apply after a sync: copy `client/sessions/fork/coalesced-refresh.ts`, then re-add the marked change-aware return in `projection-store.apply`, the per-key `subscribeAny` delivery, the key-gated dirty mark in the manager's store listener, the `handleControlFrame` delegation to that channel, the `refreshProjections` watermark probe, the `mergeChildren`/`mergeSummaryRows` single-pass merge and its `readChildren` call, and the `reconcileListState` call in `service.projectList`. Verify: `npx vitest run packages/api/session-controller`, `npx vitest run packages/client/ui-session packages/client/ui-workspace packages/client/ui-sidebar`, and `tsc -b` on both session-controller faces |
| `settings` legacy-document import resilience | D | ~200 lines in `packages/settings/settings/src/index.ts` (`importLegacyDocument` and its `normalizedLevelKeys`/`valueSchema`/`declaresLevel`, `writeAtomic`, `importedTarget`, `importedSections`, `discardMarker` helpers), plus `packages/settings/settings/tests/legacy-import.spec.ts` and the updates in `packages/settings/settings/tests/{configuration.spec.ts,configuration-fixture.ts}` | **Retained (added 2026-09-25)** — reproduced defect: upstream renames `settings.yaml` before writing a single section, so one section the running composition rejects consumes the document; the stranded production document's `reasoningEfforts` rejected the literal `false` key the YAML parser produces for a bare `off`, and the whole model/provider list behind it stayed in the renamed copy, never retried. The fork writes every section while the document still occupies its path, keeps only the rejected sections there for the next boot, records the completed section names in a `.imported-sections` sidecar before the rename so a failing rename never repeats the writes, moves the consumed document to the first free `.imported[.N]` name instead of overwriting an earlier copy, writes the retry document and the sidecar atomically through a sibling temp file, and retries a schema-rejected section once with its literal `false`/`true` keys rewritten to the level names the entry's config schema declares. Upstream has no equivalent ordering or retry, so the row retires when upstream makes the import transactional. Re-apply after a sync: port `importLegacyDocument` and its helper functions; `packages/settings/settings/tests/legacy-import.spec.ts` is the acceptance spec |
| `ui-chat` bounded mounted transcript window | C | 1 fork module (`packages/client/ui-chat/src/client/chat/fork/mounted-window.ts`) + marked injections in `packages/client/ui-chat/src/client/chat/ChatView.tsx`, `packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx`, `packages/client/ui-chat/src/client/chat/ChatGroupSeat.tsx`, `packages/client/ui-chat/src/client/chat/use-chat-scroll.ts` and `packages/client/ui-chat/src/client/chat/use-chat-reading.ts` plus the reader-gesture event in `packages/client/ui-chat/src/client/chat/use-chat-viewport.ts` (a gesture against the mounted head scrolls nothing, so it emits no scroll event) | **Retained (added 2026-09-25)** — measured: the transcript mounted every loaded history row, so per-frame DOM and render work grew with the conversation (phone-class client: 228 s of streaming produced 1352 long animation frames and 164.6 s of blocking; opening a 231-row session cost 6.87 s of script inside an 8.7 s window). The window mounts `MOUNTED_ROW_LIMIT = 50` resident order keys around the reader, adds `REVEAL_ROW_STEP = 25` per reveal, keeps a resident tail stub while a Turn streams, resolves the saved anchor (plain node keys, `["key","part"]` pairs, and `group:<json>` through its first embedded member key) while leaving an unresolvable one (`call:<id>`) at the mounted floor with the session's scroll memory intact, and re-arms the fork's reflow hold from a jump landing. Known residual: a frozen resolution is cached per entries identity plus head and revalidated against the 50-key slice, so an order rewrite that moves a resident key above the window while that slice stays identical can serve a resolution stored from a complete walk (the probe shape is recorded in `FORK_CHANGES.md`). Head-control mount (added 2026-09-26): the fold hides member rows behind the Turn's `turn-process` control, so a window carrying a Turn's member rows without its control renders none of them; a completed Turn whose last step ends on a Tool call has no finalized answer row, and when its process run outran the 50-key slice the control sat above the head — the transcript showed zero visible rows, silently and with no reveal path (a reader holding no saved row cannot move the head, and paging only prepends older history). `planMountedWindow` takes the ascending resident `controls` indices and mounts the Turn control beside every slice that carries its hidden rows — two conditional extra keys: the head Turn's control when it is resident above the window head, and the tail slice's own control while the stub is mounted (a folded Turn longer than the stub slice otherwise mounts member rows whose control sits above them). Turn-aligned heads (added 2026-09-26): the derived opening head and every reveal step snap to the Turn start owning the requested row (`turnAlignedHead`, pinned with `frozenAt` so `windowAt`'s tail normalization cannot pull it back), because a head inside a Turn mounts that Turn's later rows without its control and its prompt; the reader's row still clamps the step, and a Turn start farther above it than `MOUNTED_ROW_LIMIT` rows makes the step refuse while `pageEarlier()` serves the reader. Consumer: this deployment's Web GUI. Upstream mounts the whole loaded window. Re-apply after a sync: copy the fork module and re-add the marked injections, including the `controls` memo in `ChatView.tsx` that feeds the planner, the reader-gesture event in `use-chat-viewport.ts`, the stub control in `planMountedWindow`, and the Turn-aligned derivation (`turnAlignedHead`/`revealHead`/`frozenAt`). Verify: `NODE_ENV=test npx vitest run packages/client/ui-chat` (44 files / 663 tests green at the merged tip, the anchor-invariant gate at 19 cases and the Turn-aligned spec at 10 inside it), the module at 100% statements/branches/functions/lines, the lanes `chat-scroll-contract`, `chat-long-interactions`, `stats-paged-history` and `preview-boot`, and `complex-history.perf.ts` |

| `api/session-controller` list-plane projection whitelist | D | `LIST_PROJECTION_KEYS` + `listProjectionValues` in `packages/api/session-controller/src/list.ts` (the `hintsOf` projection) + the `SessionProjectionHints` contract JSDoc in `packages/api/session-controller/src/types.ts` + `tests/session-list-projection-selection.host.spec.ts` | **Retained (added 2026-09-26)** — measured defect: the default `session/list` response spent 92.4% of its bytes on projections whose consumers all sit on the Session-open path, because `hintsOf` shipped every registered wire key on every row. The list builder now projects each block down to the keys list surfaces read (`sessionListMetadata`, `title`, `subagentCatalog`, `schedule`, `subagentTiming`, `tokenUsage`, `subagent`, `agentPreset`, `agentTeam`, `modelSelection`); heavy open-plane keys (`turnOutline`, `inbox`, `todos`, `imageLimits`, and the rest) no longer cross the wire for a listing, and a block carrying none of the kept keys omits its `projections` entirely. The trim sits in the list builder because it is the one chokepoint every list row and `api-session/added` frame passes through, cold and live alike, so no future caller can bypass it; the Session-open path (follow snapshot, control baseline, `session/projections` RPC) keeps the complete projection set, and the persistence/cache layers stay full. The kept set is the audited reader set — each key names its list-plane consumer (the workspace tree, the subagent lineage, the sidebar chat tab, the preset chip, the Team panel), and `tokenUsage`/`subagentTiming` stay because cold children's lineage metrics read them from the row map. Corrected 2026-09-26: `modelSelection` belongs to that reader set — the Team panel's member rows read it off `projectionsBySession[member.id]?.values`, and the client's rebuild gate already classifies it as list-relevant — so the key was restored here, and the selection spec now derives its key set from the repository's own list-plane readers instead of a hand-maintained list. Measured: the largest single row saves ≥37,786 B (the whole `turnOutline` value disappears); each row also drops the repeated `imageLimits` constant (~200 B × 223 rows ≈ 44 KB). Consumer: this deployment's Web GUI. Re-apply after a sync: keep `LIST_PROJECTION_KEYS`/`listProjectionValues` and the `hintsOf` projection in `list.ts`, and the contract JSDoc in `types.ts`. Verify: `npx vitest run packages/api/session-controller` (the selection spec pins the kept set, the heavy-key leak, the open-path completeness, and the reader set derived from the sources) |

| `api/session-controller` + `ui-session` + `ui-subagent` partial-observation fences | D | ~70 lines across three upstream-owned client files: the `childReadGeneration` counter and `removedChildIds` set in `packages/api/session-controller/src/client/sessions/manager.ts` (the fence in `readChildren`, the removal filter in `mergeChildren`, the marks in `handleSessionAdded`/`handleSessionRemoved`, and the advance in `handleConnected`), the `listBefore` snapshot in `packages/client/ui-session/src/client/index.ts` (the status reconciliation's stored-vs-listed distinction), and the open-time projection read in `packages/client/ui-subagent/src/client/SubagentHeaderLineage.tsx` (`CatalogDropdown`) | **Retained (added 2026-09-26)** — measured defect class: a scoped Session client observes children and projections in partial pieces, so three paths let a stale or absent piece drive state newer information had already corrected. A pre-reconnect `readChildren` response merged its stale rows over the fresh read and its failure cleared the fresh read's memo (the generation fence now discards both); a removal frame racing an in-flight read was overwritten by the response computed before it (`removedChildIds` keeps that row out); a catalog-derived child lost its observed running/`completionUnread` status whenever a generation reset cleared the projection stores (`listBefore` separates a Host-list member that left from a stored-but-never-listed projection row); and a restored subagent address left the lineage switcher's absent parent catalog loading forever (opening the tree now reads it). Upstream has none of these fences: it reconciles status from the store snapshot alone and starts child reads without a generation. Re-apply after a sync: restore `childReadGeneration`/`removedChildIds` and their check points in the manager, the `listBefore` snapshot beside `presentBefore` in `UiSession`, and the switcher's open-time `refreshProjection` call. Verify: `NODE_ENV=test npx vitest run packages/api/session-controller packages/client/ui-session packages/client/ui-subagent` — 53 files / 1094 tests at the branch tip; the regression cases are the manager's generation/removal block, the status-controller catalog-only case, and the switcher's absent-parent case |

| `ui-workflow-run` workflow panel member resolution | C | ~35 lines inline in `packages/client/ui-workflow-run/src/client/WorkflowRunPanel.tsx` (the `catalogIdIndexes` `WeakMap`, `catalogChildIds`, and the rewritten `navigableMembers` membership test, carrying a `Fork patch (FORK_SURFACE.md)` marker) + the counting-Proxy case in the matching spec | **Retained (added 2026-09-26)** — measured defect: the panel's member selector re-ran on every Session-store notification and resolved each member with its own scan of the parent `subagentCatalog`, so one invocation cost O(members × catalog size) — 4.4 ms per frame across 56 mounted panels with a 2,244-entry catalog, and 11,301 catalog element reads for 200 entries × 30 members over two render passes. The fork indexes each catalog identity once (`catalogIdIndexes`, a `WeakMap` keyed on the catalog array identity) and tests membership with `childIds.has(member.childId)`, so one catalog pass covers the whole selector invocation; the count/status semantics are unchanged (the status lookup keys on `member.childId`, value-equal to the replaced `child.id`). Consumer: this deployment's workflow run panel in the Web GUI. Upstream rescans the catalog per member. Re-apply after a sync: restore `catalogIdIndexes`/`catalogChildIds` and the `navigableMembers` membership test (the marker sits above the index). Verify: the spec's counting case fails on the scan (11,301 reads) and passes on the index (200); `NODE_ENV=test npx vitest run packages/client/ui-workflow-run` — 2 files / 37 tests green at the merged tip |

| `session-query-sqlite` ranked-document budget | B + C | one `Config` field (`maxRankedDocuments`, default `SESSION_QUERY_SQLITE_DEFAULT_MAX_RANKED_DOCUMENTS = 5000`) + the `_assertRankableBreadth` postings-only probe and its two call sites in `packages/session-query/session-query-sqlite/src/index.ts` (~25 lines), plus the `SESSION_QUERY_SEARCH_TOO_BROAD` code in `packages/session-query/session-query/src/config.ts`, its entry in `packages/session-query/tool-session-query/src/service-boundary.ts`, and the `gateway/bad-request` mapping in `packages/api/session-controller/src/list.ts` (~8 lines) | Upstream's ranking CTE materializes `highlight()` and a `replace()`-based occurrence count for **every** match before `LIMIT`/`OFFSET` apply, so one search call's work is O(match set x document text). Measured 2026-09-26 against a copy of this deployment's store (318,335 documents; the terms `agent` and `the` match 35,872 and 92,802 documents): one `session/search` request on the then-deployed build read **3.08 GB in 300 s (660 CPU-s) and never returned**; the wedged production process read 1.44 TB of scattered 4 KiB pages over `session-query.sqlite` and its WAL with no writes, and a request that blocks the single thread this way stops answering signals and the inspector. The probe streams at most `maxRankedDocuments + 1` rowids - postings only, no document text - before reconciliation and before the ranking query, and refuses a wider match set with `SESSION_QUERY_SEARCH_TOO_BROAD` (mapped to `gateway/bad-request`, so the client shows a bounded refusal instead of a server fault). Re-apply: the `Config` field and its validation, the probe next to `_reconcile`, one `assertNotAborted` plus probe call in `searchSessions` and `searchEvents` before `_reconcile`, the taxonomy member, the tool mapping entry, and the controller mapping branch. Consumer: this deployment's Web search box and the `session_query` tool; the measured effect is 3.08 GB / 660 CPU-s never completing to **288 KB / 0.41 CPU-s** with the refusal. Regression: `packages/session-query/session-query-sqlite/tests/sqlite.spec.ts` reads `/proc/self/io` around two searches over a ~3.8 MiB corpus and asserts under 1 MiB (16 MB+ on the unpatched engine). **2026-09-26 follow-up, the per-request bound:** the probe above bounds matched documents per ranking CALL, but one caller request re-ran the whole ranking once per page - `packages/api/session-controller/src/list.ts` passed the cursor as an OFFSET up to `SEARCH_PROVIDER_CALL_LIMIT` (100) times - so a match set under the cap was still re-ranked once per page. Measured on an isolated instance at the master tip over a copy of this deployment's store and index (318,335 documents, 1,438 persisted Sessions): `session/search {query:'搜索'}` (2,271 matching documents, 27.5 MB of stored text, 1,264 Sessions) read **114.8 MB in 1.19 s**, and a 20-page cursor walk of the same query read **1,276.2 MB** (57-79 MB per page, 0.52 s per page). The engine now ranks once per normalized request plus corpus generation, caches the ranked rows as identity columns without document text, and serves a continuation cursor by slicing that ranking; only the page's own rows re-read stored text, and `SESSION_QUERY_SEARCH_BUDGET_EXHAUSTED` refuses a re-rank that would push the caller request past `maxRankedDocuments` documents in total (one budget per request, shared by every page). Measured after: **58.4 MB / 0.78 s** for the request and **63.1 MB / about 3.2 s** for the 20-page walk (first page 57.8 MB, later pages about 0.26 MB each); the over-cap refusal stays 205 KB. Re-apply additions: the `_rankedSessions`/`_rankedEvents` ranking cache, `_slicePage` plus `_markedTextOf` (the FTS5 `highlight()` call needs the MATCH constraint in its own query to mark anything), `_chargeRankedDocuments`, the `exec.rankedDocumentBudget` field in `packages/session-query/session-query/src/types.ts`, the charge at each ranking in `searchSessions`/`searchEvents`, one budget per request in the controller and in the `session_query` tool, and the second taxonomy member with its controller and tool mappings. Regression: two new `sqlite.spec.ts` cases (a six-page walk over a ~3.8 MiB corpus charges one ranking and stays under a 16 MiB ceiling; a live-event generation change makes the retry refuse with the budget code and stay under 1 MiB) and a controller case asserting both pages carry the same budget - all fail on master (`budget.spent` 0 against 480 and 60) |
## Optimization plan (priority order)

1. **DONE — Extract the paging core from `history.ts`** — the indexed read plan (the seekable suffix read, its density and end proofs, the margin ladder) lives in the fork-owned `src/fork/page-boundary.ts`, a file upstream will never have. `history.ts` keeps a small injection: the import, the `tryIndexedPage`/`tryWindowedOpen` calls, and `windowCut()` binding upstream's `paginate` per request. Upstream refactors of `page()` re-conflict against that injection, not two hundred lines.
2. **DONE — Move `messageCut` off the upstream persistence interface** — a fork-owned service (its own package or an extension in `session-query-sqlite`) exposing the indexed cut; `history.ts`'s fast path already discovers it optionally (`SeekablePersistence` duck-typing). The upstream `SessionPersistence` abstract and coordinator revert to pristine; the SQL moves with the fork service.
3. **Write the merge runbook as you change, not at sync time** — every Tier C/D change notes its injection point here; the sync re-apply follows this file top to bottom.
4. **Keep Tier D diffs compact and marked** — the scroll-attribution block carries its rationale comment; upstream fixing the same clamp bug upstream-side shrinks the fork diff to zero (watch for it in upstream releases).
5. **DONE — AppFrame mobile shell** — the largest surface. Realistically stays a patch (layout is upstream's core component), but keep the fork's regimes in `columns.ts`-style leaf modules so the AppFrame diff stays import-and-delegate.
6. **PAGE_MESSAGES** — one line; optionally a build-time env (`DSH_CLIENT_PAGE_MESSAGES`) if it churns.

## Sync procedure (runbook)

1. Read every release note between the fork's base tag and the target tag, and list the protocol, session-format, and API breaks they name; plan each adaptation they imply.
2. `git fetch upstream && git merge upstream/master` in a worktree.
3. Run `npx tsx scripts/verify-fork-surface.ts` (read-only, ~0.5 s) and clear every failure before walking the inventory. It names the marked fork patch that no row registers, the tier C row whose file lost its marker, and any English/Chinese structure drift; a new finding is a real sync signal — register the surface (or restore the marker), never widen `scripts/fork-surface-baseline.txt` to silence it.
4. **Parity review (AGENTS.md policy)**: walk every inventory row against the new tag; when upstream ships an equivalent, drop the fork row and adopt upstream's (record the retirement in FORK_CHANGES.md); update each retained row's why-upstream-cannot-serve note.
5. Tier A: no action (upstream has no such files); resolve only `pnpm-workspace`/tsconfig aggregates and `cordis.patch.yml`.
6. Tier B: re-apply the flag/constant; expect trivial context conflicts.
7. Tier C: re-apply each localized block per this file's inventory; run the owning package's suite.
8. Tier D: re-apply the injection points (after step 4 above they are the only remaining history.ts/persistence diffs), then copy the fork-owned modules verbatim.
9. Run `pnpm run test:gui`, the session-controller suite, and `DSH_SNAPSHOT=replay pnpm run test:web`; refresh goldens only for intentional output changes.
10. Record the sync in `FORK_CHANGES.md`.
