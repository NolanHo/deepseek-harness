# Fork patch surface — 上游差异面清单与合并 runbook

[English](FORK_SURFACE.md) | 中文

fork 与上游的维护契约：每处差异要么是 fork 自有模块（零合并成本）、配置开关、小型行为补丁，要么是语义级核心修改。本文件盘点每个差异面、其隔离层级、以及上游同步时的重放流程。任何触及上游自有文件的改动都要同步更新本清单。

## 为什么需要这份文件

0.1.2-alpha.1 同步（上游 1079 提交）解决了 123 个冲突。痛点集中在上游自有文件中的 fork 语义。下面的清单把每个差异面分类，让下一次同步变成机械的重放清单，而不是考古挖掘。

## fork 模块约定

凡超过一行的 fork 逻辑都住在 `<pkg>/src/fork/`（client 面：`<pkg>/src/client/<area>/fork/`）的 fork 自有模块里。上游文件只带一个标记注入点——一个 import 加一个调用——前面配 `// Fork patch (FORK_SURFACE.md): ...` 注释。同步操作 = 原样搬移所有 `fork/` 目录 + 重放下方登记的注入点。内在一行式改动（常量、单 hook 互换、CSS 块、配置字段）按政策保持内联；抽取它们只会增加间接层而不会缩小合并面。

## 双路径

双路径指同一行为两边都改了——同一文件、同一算法，或同一结果的两套实现。双路径一律归上游：删除 fork 版本、采用上游实现，并在 `FORK_CHANGES.md` 记录退役，即使 fork 版本略优。保留的 fork 版本必须写明上游无法服务的生产消费者或未修复的已复现缺陷。

### 已解决与保留的双路径

| 双路径 | fork 侧 | 结论 |
| --- | --- | --- |
| TurnProcess 折叠标签 | `ui-chat/src/client/chat/fork/turn-process-summary.ts` + 视图 import | **已退役** —— 上游在同一视图内联构建同样的折叠标签；模块及其 `message.turnProcess.collapsed` 键删除，视图恢复上游实现 |
| 历史分页 | `session-controller/src/fork/page-boundary.ts` + `history.ts` 注入 | **保留** —— 上游 `paginate` 没有轮次对齐切点，页窗口可能从轮次中间打开并渲染半个轮次头；fork 把切点扩到所属轮次的开场事件 |
| 冷开窗口 | `session-controller/src/fork/open-window.ts` + `history.ts` 分支 | **保留** —— 上游折叠整段日志再裁剪（最大会话要数秒 CPU 与数百 MB 堆）；fork 通过自有 seek 面只读一个索引窗口 |
| 回流稳定滚动锚点 | `ui-chat/src/client/chat/fork/scroll-anchor.ts` + 6 处 ChatView 注入 + `overflow-anchor: none` | **保留** —— 上游的 ResizeObserver 只重跟尾部、结算 effect 清空锚点，且其台账把原生锚定的写入误判为读者移动；fork 实测阅读行上方展开 8,460 px 而补偿为零 |
| 移动端 frame 组合 | `ui-layout/src/client/fork/mobile-shell.tsx` + AppFrame 与 store 字段 | **保留** —— 上游 frame 在 1024 px 以下只折叠成窄轨且没有手机形态，手机宽度无法重新展开左栏 |
| 沙箱组合 | `bundle/base/cordis.patch.yml` 执行器替换 + `permission` 禁用 | **保留** —— 上游的约束型执行器需要 bwrap 或 Landlock；本容器内核早于 Landlock 且 `bwrap` 探测失败，采用上游组合会让每条 bash 调用 fail-close。在支持 Landlock 的内核上需重新评估 |
| 活动与重建抖动 | `sessions/fork/coalesced-refresh.ts`、`snapshot-identity.ts`、`ui-workspace/src/client/fork/order-stability.ts`、`session-query-sqlite/src/fork/live-observation-memo.ts`、空转 identity 契约 | **保留** —— 上游对每个活动事件立即应用、每次重建都新造 subagent/job 投影与快照对象、每次同步重排提升集合、每次搜索克隆并哈希全部挂载会话；每个 fork 模块都修复了已复现缺陷。局部重叠（entry/items identity、reconcile/recency 辅助函数）是下一步采用上游辅助函数的方向 |
| 打开文件路由 | `ui-chat/src/client/chat/fork/open-file-routing.ts` | **保留** —— 路由优先已安装的 `dsh-better-sidebar`，再回落上游右侧栏 |
| 通用品牌 `DSH` | 语言字典 + `apps/web` 默认值 | **保留** —— 本部署的品牌选择；上游为 `DSH Local Build` |

## 隔离层级

- **A 层 — fork 自有包**：上游永远不会有的文件。零合并成本。
- **B 层 — 配置开关**：上游文件中的一个常量或经校验的 `Config` 字段。重放分钟级。
- **C 层 — 小型行为补丁**：每个上游文件约 40 行以内、局部块。冲突小、可解。
- **D 层 — 语义级核心修改**：上游自有算法内的 fork 行为。这是合并成本所在；尽量压缩注入面。

## 当前清单（相对 fork 基线 tag dsh-v0.1.5-rc.2 的上游）

| 差异面 | 层级 | 规模 | 性质 |
| --- | --- | --- | --- |
| `packages/web/web-search-{academic,bocha,brave,zhihu}` | A | 20 文件 | fork 搜索提供方，完全隔离 |
| `.agents/notes`、文档、快照、测试更新 | A | — | 随源更新；与对应源一起重放 |
| `session.ts` PAGE_MESSAGES 8（上游 50） | B | 8 行 | 客户端页大小 |
| `client/connection` browserAuth 开关 | B | ~50 行 | 配置字段后的可选认证关闭 |
| `ui-chat` TurnProcess 折叠标签 + 时长 | 已退役（双路径） | — | 上游 0.1.5-rc.2 在 `TurnProcessNodeView` 内联构建同一折叠标签；fork 的 `src/client/chat/fork/turn-process-summary.ts`（分类计数 + 墙钟时长 + 折叠前缀）已删除，视图恢复为上游实现，fork 自有的 `message.turnProcess.collapsed` 字典键随之移除。时长仍在轮次页脚的用量详情中可见。`TurnProcessNodeView.tsx` 与 `locale.ts` 现与上游完全一致。 |
| `ui-chat` 部分历史下的 Turn Process 折叠 | C | `ChatNodeSeat.tsx` 删 3 行 + `ChatView.tsx` 删 1 处调用点 | **保留** —— 上游把 `processWindowReady` 门控在 `!historyIncomplete` 上，已关闭回合只在全部页载完后才折叠；`PAGE_MESSAGES = 8` 让真实会话的 `hasMore` 恒为真，折叠从不出现。fork 删除该 prop 与该门，仍有更早历史时即折叠；注入点带 `// Fork patch (FORK_SURFACE.md)` 标记。0.1.5-rc.2 同步曾静默回退这条未登记的表面；2026-09-12 重新打上并恢复两个部分历史折叠测试 |
| `ui-chat` TurnProcess 折叠控件胶囊样式 | C | `TurnProcessNodeView.module.css` 内约 13 行 CSS | 控件渲染为贴合标签的圆角胶囊，带 hover 面，chevron 与标签留出间距；上游渲染的是通栏分隔行、chevron 跟在末尾。标记与文案都是上游的、类名不变，因此整体采用上游样式表会静默恢复成通栏分隔线 |
| `ui-chat` ChatView 读者输入归因 | 已退役 | — | 上游自己的修复已落地（observed-top 几何台账覆盖全部输入设备，无需监听器）；fork 的设备标记补丁在 0.1.2-rc.1 同步时移除 |
| 部署级 reduced-motion 默认 | C | 1 个 shell 模块（`packages/client/web/src/fork/reduce-motion.ts`）+ `packages/client/web/src/boot.ts` 中 1 处启动调用 + 1 段 CSS | 本部署**默认无动效**：每次启动给 `<html data-reduce-motion>` 打标，`packages/client/web/src/base.css` 把动画压成一步完成。过渡保持作者时长：右侧栏面板机制需要可观测的 `transform` 过渡，一刀切 1 ms 会让它的 `transitionrun` 探针报 "panel transform transition is unavailable"，并让 `sidebar-right.e2e.ts` 的 7 个用例失败。`?reduce-motion=0` 写入按浏览器的退出选择，`?reduce-motion=1` 清除它。该标记不改变浏览器全局的 `prefers-reduced-motion`，其他站点保持各自动效 |
| 部署主题配色 | C | 1 个 shell 模块（`packages/client/web/src/fork/deployment-theme.ts`）+ 1 个样式表（`packages/client/web/src/fork/themes.css`）+ `packages/client/web/src/boot.ts` 中 1 处启动调用 + 4 条元素规则 | 本部署默认渲染暮蓝配色：每次启动给 `<html data-dsh-theme="harbor">` 打标，`packages/client/web/src/fork/themes.css` 承载 token 覆盖——owner 对本地安装的 dsh-market「夕港 / Harbor」皮肤的蓝白重染，markdown 与语法配色取自 owner 的 xbot 检出里的 "tokyo-night" 主题（代码面 `#161a2e`、链接青 `#7dcfff`）。配色同时声明在带标记的根元素**与其 body** 上：上游把 alias token 声明在 `body` 与 `body[data-ds-dark-theme]`，在应用子树里会盖过只声明在根上的块，导致面板仍是上游配色。另有四条限定在 `[data-chat-flow]` 内的规则补上没有 token 承载的对比度：两级标题（h1/h2 `#7aa2f7`，h3+ `#c8d3f5`）、行内代码芯片（`#9ece6a` 落在 `#2b3358` 上并带可见边框）、表头（`#1f2540`）。`?theme=default` 写入按浏览器的退出选择，`?theme=harbor` 清除它。上游 theme 插件只管 light/dark/system 偏好与正文字号、没有配色扩展点，因此配色放在 shell 而不是主题注册；皮肤的壁纸与运行时 hooks 不入库 |
| 无限动画的 reduced-motion 守卫 | C | 4 段 CSS 媒体块 | 上游在多数动画文件里已有 `@media (prefers-reduced-motion: reduce) { animation: none }`，但漏了四处无限动画：`ui-primitives/StateDot.cell`（运行态点阵追逐）、`ui-conversation/InputBar.pending`、`ui-conversation/TodoPanel.glyphProgress`、`ui-input-trigger/MenuView.skeletonBar`。守卫只在读者开启该偏好时生效，其余读者的渲染完全不变；StateDot 保留静态中间相 `opacity: 0.6`，让运行态仍然可读 |
| 部署默认下的运行中指示器 | C | 1 段 CSS + 1 个配色 token | 部署标记把动画压成一步完成，导致 `ui-primitives/StateDot` 的点阵追逐停在基础 `opacity: 0.15`，侧栏行旁的「运行中」指示器几乎看不见。该追逐是 Session 正在运行的唯一信号，因此 `StateDot.module.css` 在 `:root[data-reduce-motion]` 下保留它，同时部署配色把 `--dsw-static-deepseek-450`（追逐与默认文件类型图标所用的静态步进）从 `rgb(86 134 254)` 提到品牌浅蓝 `#8fbaff` |
| `frontend-static` 对内容哈希资产发 immutable 缓存头 | C | `packages/host/frontend-static/src/index.ts` 约 10 行 | dist 服务端此前对任何文件都只回 `content-type`，于是每次刷新都重新下载 shell 里带内容 hash 的 JS 与 CSS，而 `/plugins` 下的插件包早已是 immutable（线上实测：每次刷新的 shell JS 与 CSS 为 438 KiB gzip，占该次刷新的 448 KiB，解压后 1,352 KiB）。现在命中 Vite 内容哈希输出目录（`assets/`、`preview/`；`apps/web/vite.config.ts` 为每个产物名固定 `[hash]`）的请求带 `cache-control: public, max-age=31536000, immutable`；index 响应与稳定命名的根文件（`favicon.svg`、`manifest.webmanifest`、`preview.html`）保持原有头，重建的 bundle 或变更的 index 仍能到达浏览器。上游 `frontend-static` 没有缓存头配置项，因此这条规则是它服务函数内带标记的补丁；Electron/桌面宿主的 dist 处理（`apps/desktop-host`）保持上游的无头行为，因为本部署走 Web 面 |
| `ui-conversation`/`ui-chat` CSS overflow-anchor + 安全区 | C | ~40 行 | 滚动容器锚定；局部规则 |
| `ui-conversation` 打字重渲染收敛 | C | 2 个 null 叶子 + 3 处调用点 | `ConversationRoot` 订阅整个 input store 只是为了把 owner props 交给 dock，`ConversationSession` 订阅 input store 与持久化草稿只是为了播种与镜像草稿；这两个订阅让每敲一键都重渲染它们所在的列——header、已加载的 transcript 窗口（`conversation.view`）与 composer 栈。订阅现下沉到 `InputDockSlot` 与 `DraftMirror` 两个不渲染 DOM 的叶子；dock 仍拿到实时 owner props，播种 effect 保持原有的 mount-only 语义。实测（1,519 行 / 30,404 节点）：每键 JS 工作 3 ms → 1 ms，总延迟在测量机上两边都是 16 ms 一帧 |
| `api/session-controller/src/history.ts` 快路径注入 | C | `page()` 约 10 行，`follow` 约 60 行 + `packages/api/session-controller/tests/session-history-journal.host.spec.ts` 中 fork 的加宽切点断言 | 分页注入委托给 fork 自有 `src/page-boundary.ts`（边界游走、梯子、快路径计划）：`page()` 一次调用加 `paginate` 的委托。打开注入增加同步服务检查、带观测回落的窗口快照分支，以及第二个构造器回调（按 id 激活窗口化 Session）。两条快速路径都通过 `seekSurface` 辅助函数取得持久化方法，并绑定到 `ctx.get` 返回的 tracker 代理：未绑定的提取方法会以包装对象为 `this` 运行，在任何读取自身状态的提供方里抛错 |
| `api/session-controller/src/fork/open-window.ts` 冷打开窗口 | C | 约 110 行 + `history.ts` 分支 | 打开快照的窗口化读取：基于 `readIndexedSuffix`（`page-boundary.ts`，新增窗口化读取、可选计划字段 `throughSeq`/`windowFloor` 与回合截断检查）的读数计划、检查点门槛、闭合尾部证明与折叠。上游的 `follow` 折叠整段日志再裁剪，在最大会话上要花数秒 CPU 与数百 MB 堆；上游没有可委托的索引寻址面（fork 的 `seekable`/`messageCut`/`readFrom` 在自己的持久化后端上）。窗口化读取先调用 `seekable`，返回 false 时在任何切点查询或读取之前就退出。`history.ts` 只保留带标记的 try、观测回落与按 id 的激活交接 |
| `session-projection-cache/src/fork/checkpoint-read.ts` 检查点读取与写回 | C | 约 90 行 + `index.ts` 注册 | 窗口化打开需要按会话 id 读取已存检查点行，首次冷打开还要装上供下一次折叠的记录。上游的公有折叠面只返回视图值而非可播种的行，其 `hydratePrepared` 刻意不写任何东西；fork 增加一个 symbol 键注册的服务私有查找（WeakMap 不可行：cordis 交给调用方的是 tracker 代理而非注册对象）、`readCheckpoint` 读取，以及持久前缀写回。`readColdSessionLog` 报告 `durableEventCount`，使写回的行绝不越过已存日志末端（`observation.ts` 负责透传）|
| `session-persistence-sqlite` 整包 + 寻址面（`seekable`/`messageCut`/`readFrom`）+ 重写能力（`truncate`） | A（0.1.2-rc.1 起 fork 拥有） | 9 个 src 文件 + `delete-event-row.sql` | 上游按 JSONL-only 决策删除该包但为 out-of-tree 提供者保留接缝；fork 保留并移植到 handle-based `PersistenceBackend`/coordinator，采用上游最终 SCHEMA_VERSION 20（`ignorable` 列兼作 packed 行判别符；一次性 19→20 原地迁移；`seed_length` 承载继承切点）。`seekable(id)` 是快速路径的门槛：仅当前格式行返回 true，且只读元数据。它之所以存在，是因为 `loadStoredFrom` 的历史分支会用格式目录恢复整段日志再切片，而切片用的 `fromSeq` 来自存储物理空间，恢复出的序号已重排、不在该空间里——生产库绝大多数行正是这种，窗口计划在那里每次尝试都要读完整段日志却拿到空页。该包同时实现 seam 的可选重写能力：`SqliteSessionHandle.truncate`/`SqliteStore.truncateLog` 在一个 immediate 事务中丢弃切点起的全部已存事件，重写唯一跨切点的 packed 行以只保留其切点以下成员，递增一次 revision，失败即回滚；句柄游标随之重置，收缩守卫改按已存 revision 判定。本行登记的页缓存字段见[页缓存 Config 字段记录](.agents/notes/implemented/feature/2026-09-20-sqlite-page-cache-config.zh.md) |
| `session-persistence` seam：可选 `SessionHandle.truncate?` 重写能力 | C | `src/handle.ts` 约 20 行 + `src/index.ts` 类 JSDoc | `SessionHandleTruncateOptions { signal?, append? }` 与可选成员 `truncate?(toSeq, options?)`：丢弃 `toSeq` 起的全部已存事件，并在同一步持久落地 `append`（按 seq、起点恰为 `toSeq`），仅限写句柄；读句柄以 `SessionReadOnlyError` 拒绝，写所有权丢失时以 `SessionOwnershipLostError` 拒绝。无法重写已提交日志的后端省略该方法，需要重写的消费者响亮地拒绝（`session/rewrite-unsupported`）而不是把未满足的操作当作成功——fork 的 SQLite 后端实现了它，上游随产品交付的 JSONL 后端省略它。`index.ts` 类 JSDoc 为这条唯一截断路径放弃了绝对化的「绝不重写」表述 |
| `api/session-controller` 就地重写编排 | C | fork 模块 52 + 55 行；`commands.ts` +200/-6；`agent.ts` +62/-7；`index.ts` 中继守卫；`types.ts` 2 个 wire 字段 + 2 个错误码 | 纯函数切点校验在 fork 模块 `src/fork/rewrite-history.ts`（`resolveRewriteCut`：武装 seq 必须是已存的 `user/message`，子代理会话拒绝，切点为不晚于它的最后一个 `turn/start` 的 seq，并只向前越过该消息**自己**的准入 `agent/inbox/spliced` 事件，低于 `inheritedEventCount` 拒绝；随后 `planInboxRepair` 折叠保留前缀，为仍处于 pending 的消息返回一条取消 splice，由 `truncate` 的 `append` 落地）。`commands.ts` 保留标记的 `createSeeded` 注入、`prompt` 重写分支与两个编排方法（`rewriteHistory`：仅 queue、仅 live Agent idle、inbox 有排队拒绝、dispose → 写句柄截断 + 追加 `planInboxRepair` 算出的中和移除 → close → 丢弃该会话的投影缓存记录；`truncateStoredLog` 在 `handle.truncate` 未定义时响亮失败）。`agent.ts` 保留标记的句柄持有字段及 `disposeAgent`/`createSeeded` 与 resume/create 调用点改动。`types.ts` 增加 `SessionPromptRequest.rewriteFrom`/`SessionPromptValue.rewrote` 与 `session/rewrite-unavailable`（带类型化原因）/`session/rewrite-unsupported` 两个错误码。fork 模块 `src/fork/rewrite-hold.ts` 在整个重写窗口内扣住 Agent 拆除本会发布的 `api-session/removed` 公告：`prompt` 在 `rewriteHistory` 之前进入 hold、在 `resolveAgent` 结算后释放，`index.ts` 的 `session/disposed` 中继先询问 `commands.deferRemoval`；若窗口结束时没有活跃 Agent 则重新公告该移除——没有它，每个客户端都会在重写期间丢掉列表行与 selection，并渲染新会话画面 |
| `session-projection-cache` 重写失效 + 种子水线规则 | C | `index.ts` +52/-2（含类 JSDoc）| `SessionProjectionCache.discard(id)` 持久删除已存记录，并先清掉同一 id 上 live Session 的待写；重写之所以调用它，是因为缓存的身份校验看不出日志发生了倒退。种子读（`hydratePrepared`、`coldSnapshot`）另外拒绝水位在其将要折叠进的 durable prefix 之上的已存行（`rowsWithinDurablePrefix`）：缓存只可落后于日志、绝不可超前，否则从被删除事件折叠出的行会把已删除消息恢复成排队项并重放成新轮次。零 I/O 的列表读（`cachedSnapshot`）没有日志可对照，依赖写入方的丢弃。对只增的日志无行为变化：正常检查点的每一行都在其折叠所达的 durable prefix 之下 |
| `session-controller` 客户端 + `ui-conversation` 重写透传与窗口重建 | C | `sessions/session.ts` 约 35 行；`input/facade.ts` 约 30 行；`hub.ts`/`service.ts` 约 15 行；`contract/session.ts` + `contract/input.ts` 的 wire 字段 | `SessionPromptOptions.rewriteFrom` 随 prompt wire 请求上行；`ClientSession.prompt` 在 Host 回报 `rewrote === true` 时先 `resync()` 重建窗口再结算，武装提示词被接纳却没有 `rewrote` 时（与未重启的旧 Host 的版本偏差）结算为可见的 `session/rewrite-missed` 失败——绝不静默追加。composer 输入机（`ui-conversation`）在草稿旁持有 `InputState.rewriteFrom`/`InputActions.setRewriteFrom`：武装值随默认发送路径上行，随已提交草稿清空，失败发送时随草稿恢复。子代理寻址的续写分支忽略该选项 |
| `session-query-sqlite` 活动观察记忆化 | C | `index.ts` 内约 13 行 | 指纹 memo（`length:tailSeq:tailTime`）在 `src/fork/live-observation-memo.ts`；`index.ts` 只留字段与 3 行委托 |
| `client/ui-layout` AppFrame 移动端抽屉 | C | import + 组合 + 2 个 store 字段（`stores.ts`）+ `packages/client/ui-layout/tests/layout-store.client.spec.ts` 中的 store 断言 | **0.1.5-rc.2 状态：已重放** —— 上游 frame 在其 1024px 自动折叠以下只提供窄轨，故 fork 保留自己的手机断点（768px，与右侧栏自身的全屏阈值一致）：左栏变为固定覆盖抽屉，frame 只渲染一条流内轨道。详情面板退役——上游右侧栏在 768px 以下本就全屏覆盖并提供自己的退出入口。 | 视口机制 hook 与抽屉 chrome 在 `src/client/fork/mobile-shell.tsx`（自带 `MOBILE_VIEWPORT`，因上游 `columns.ts` 去掉了该常量）；AppFrame 的 diff 为 import、一次 `useMobileRegime` 调用、抽屉分支、单轨模板、`RightbarColumn` 的 `mobile` 属性（使其不占该轨道）与两处手柄守卫；`stores.ts` 增加 `mobile`/`drawerOpen` 及 `setMobile`/`setDrawerOpen` 与 `toggleSidebar` 的移动分支；抽屉标签是 fork 加入 `common` 字典的 `sidebar.open` 键；移动端 chrome 的 CSS（`.rightbarCol[data-mobile]`、`.scrim`、`.mobileDrawer`、`.mobileMenu`）在 `AppFrame.module.css` 内由一段分区标记覆盖（上游的 `.overlayLayer` 位于该段内） |
| 通用客户端品牌读作纯 `DSH` | C | 2 个字典值（`packages/client/locale/src/locales/en.ts`、`packages/client/locale/src/locales/zh.ts`）+ 2 处构建默认值 + 测试断言 | 上游通用品牌为 `DSH Local Build`，fork 读作 `DSH`（两个语言字典的 `brand.localBuild` 驱动侧栏品牌名与文档标题兜底，`apps/web/index.html` 与 `vite.config.ts` 的 `DEFAULT_CLIENT_TITLE` 承载静态默认值）。部署标题（`DSH_CLIENT_TITLE`）仍可覆盖，品牌下方构建版本行不变。这两个字典同时承载移动端抽屉的 `sidebar.open` 键与 Think 行的两个键，整体采纳上游字典会把它们一起删掉 |
| 对话头部移动端精简 | C | 两个 `max-width` 媒体块（767.98px 与 560px） | 抽屉断点以下，会话头部左边距让开 frame 固定的抽屉入口（≤767.98px 时 `padding-left: 56px`）。手机宽度下头部内边距与标签节奏收紧，会话标题面包屑保留 `min-width: 88px` 底线并经 `.crumb` 省略号截断，`.headerActions` 可收缩（`flex: 0 1 auto`、`min-width: 0`，刻意不加 `overflow: hidden`——那会把 job 徽标绝对定位的菜单裁到不可见），`.headerCorner` 归零其桌面 `-16px` 右外边距——该外边距是按桌面 28px 内边距配的，而手机内边距为 12px，使角落控件在 360/412/480/560px 下都越出视口 4px。上游该模块完全没有媒体查询，整块采纳会一次性删掉手机内边距与其上所有规则；`header-phone-styles.client.spec.ts` 会拒绝这种做法。同一个块刻意不碰 lineage slot：在子代理面包屑里该 slot 渲染子代理会话自己的标题（`switcher` 变体），且它是进入子代理目录的唯一手机路径——抽屉树与会话搜索都把子代理子节点过滤掉。真正让出手机 header 的元信息是 agent-preset 标签（见下方 `ui-agent-preset` 行）。session-log 那一半随 0.1.5-rc.2 退役：上游把该控件挪进头部的 28px 省略号菜单，已不再挤占手机头部 |
| `api/session-controller` `openWorkspacePath` 桌面门控 + `ui-chat` 文件打开路由 | C | 门控保持内联（5 行）；路由 = `packages/client/ui-chat/src/client/apply.ts` 中的标记+import+2 处调用，由 `packages/client/ui-chat/tests/apply-inject.client.spec.tsx` 钉住 | 打开 RPC 查询 `canOpenPath()` 并快速失败（内在一行式）；路由决策与拒绝文案映射在 `src/client/chat/fork/open-file-routing.ts` |
| `bundle/base` 沙箱能力 | C | 行 `disabled` 标志 + 执行器行名 | **0.1.5-rc.2 状态：部分恢复** —— 上游的 workspace-files 与 deliverables 条目需要 `sandboxPolicy` 服务，因此 sandbox 与 sandbox-policy 行重新启用并钉 `mode: danger-full-access`（本部署唯一模式）；执行器行仍为本地提供方，permission-presets 行保持禁用 |
| `client/modules` + `client/web` 延迟启动批次 | C | 3 个文件约 120 行 | `WebBootBatchPhase 'deferred'` + `Config.defer` 切分 + 两段式 boot；上游形态（增量线格式字段、空默认）；defer 名单是部署配置而非仓库状态 |
| `ui-workspace` 提升头部稳定 | C | 标记+import | `nextSessionOrderAccount`/`reconciledSessionOrder` 在 `src/client/fork/order-stability.ts`；WorkspaceBrowser 直接调用 |
| `ui-chat` 消息动作条 DOM 钩子 | C | `MessageIconActions.tsx` 1 个属性 | 动作条带 `data-message-actions`，第三方插件可在原生复制/分支按钮旁挂自己的动作。上游 0.1.5 删除了 `data-time-hover-root`，而 fork 的 `dsh-rewind` 正锚定它；没有稳定钩子时插件静默不渲染。该属性纯新增、不可见 |
| `ui-tool` 通用行的 `bodyContent` + `GenericToolCard` 导出 | C | 3 个上游文件共约 15 行（`ToolRow.tsx`、`GenericToolCard.tsx`、`client/index.ts`） | **保留** —— fork 的包外插件 `dsh-apollo` 保留所有工具共用的通用折叠行、只替换其展开体：`ToolRow` 新增可选的 `bodyContent: ReactNode`，以块级元素渲染为展开体的第一个子节点（在 Input/Output 卡片之外），取代默认的「参数」（INPUT）段——不格式化参数、不打印 INPUT 标签、不渲染空卡片——并让该行本身可展开；OUTPUT 段、卡片链、摘要与 Inspect 胶囊不变，不传该属性的行与上游渲染完全一致。`GenericToolCard` 透传同一属性，浏览器入口导出该组件与其 props 类型；这个值导出是对客户端导出纪律规则的有意例外，已获用户同意。重新施加：该属性、以 `inputBody` 替代 `cardBody`、展开体首子节点、`GenericToolCard` 的透传，以及两个入口导出 |
| `ui-chat` StatsLine 绘制后测量 | 已退役 | — | 上游在 0.1.5-rc.2 用两个图标胶囊（`StatsPills.tsx`）取代统计条并删除 `StatsLine.tsx`，绘制后省略号测量已无载体；该行仅作 runbook 存档 |
| `skill` 注册表目录限制 | C | `index.ts` 内 17 行 | 全部逻辑在 `src/fork/skill-restrict.ts`（编译、按作用域存储、链式过滤）；`index.ts` 只留 import、一个字段、两个标记的委托调用；allow/deny 互斥记录在模块 JSDoc |
| `subagent` 子代理 cwd + skillFilter | C | 接缝 + 5 处调用点（`child-agent.ts`、`continuation.ts`、`continuation-activation.ts`、`subagent-in-process-driver`、`tool-subagent`） | 逻辑在 `src/fork/child-scoping.ts`（`stampChildCwd`、`applyChildSkillFilter`）；贯穿 `childSessionMeta`/continuation/驱动的请求字段是保留的接缝；descriptor v3→4（同步遇上游 bump：字段取并集）；冷恢复的 cwd 权威仍在会话 header  **重放清单（2026-09-16 更正）**：接缝是**四处**调用点而非两处 —— `child-agent.ts`（`childSessionMeta` + `applyChildComposition`）、`continuation.ts`（冷恢复 meta + 两处 continuable 组合）、`packages/subagent/subagent/src/continuation-activation.ts`（首次激活组合的 `skillFilter` 字段，第三处组合点）、以及 `subagent-in-process-driver/src/index.ts`（`setup()` 把 `request.skillFilter` 交给 `applyChildComposition`；`agents.create()` 把 `request.cwd` 交给 `childSessionMeta`）。驱动那两行在 0.1.5-rc.2 同步时被静默丢弃、连「re-apply」提交本身也漏掉了，所以本行现在登记全部调用点；`child-isolation.spec.ts` 覆盖这四处。第五处是产出方：`tool-subagent/src/index.ts` 把自身实例配置校验过的 `cwd`/`skillFilter` 展开进它交给驱动的 start request（`src/fork/delegation-scope.ts`，登记在该包的别名面行内），`fork-delegation-alias.spec.ts` 覆盖三条 start 路径。 |
| `session-format-v0-to-v1` released-v0 子代理 descriptor 兼容 | C | `src/fork/subagent-descriptor-compat.ts` + 5 个上游文件各一处标记委托 | 冻结的 released-v0 边界只接受 descriptor 版本 3，而安装的 `@deepseek-ai/dsh-subagent` 向新 payload 打版本 4，已存的 2/3 payload 又早于版本 4 新增的组装输入；该模块拥有可选成员集合、当前版本与 2/3→4 升级，`dispositions.ts`、`migration.ts`、`payload-validation.ts`、`validation.ts` 与 `tests/relationships.spec.ts` 各保留一处标记委托 |
| `ui-chat` ChatView 重排稳定滚动锚点 | C | import + 1 个回调分支 + 3 处捕获/重持编辑、移除 1 个上游 effect | `chat/fork/scroll-anchor.ts` 在每次流列尺寸变化时（折叠塌陷、图片加载、展开）重新断言持有的读者行，并在滚动写入之后测量锚点使连续回调保持幂等，配 observed-top 台账保证读者输入归因正确；锚点行被隐藏时回退到其上方最近的存活可见行。ChatView 在每次离开底部的滚动采样上武装锚点，在每次前插补偿后重新持有读者行，在保存位置恢复时捕获恢复的行，并在 load-earlier 结算后保留锚点（上游清空 effect 已移除） |
| `api/session-controller` 流式快照节奏 | C | `session.ts` 1 行 + 注释 | 实时 chunk 呈现（`publishAssistantEntry` 的 `transient` 分支）改用 `markFrameDirty()` 标记会话快照，一个流式帧最多发布一次。帧调度器早已存在（`Notifier.markFrameDirty`）但生产代码零调用：每个 chunk 都走一次微任务冲刷，令每个 `useSession` 消费者逐 chunk 重渲染（实测 3 个 chunk 帧 → 3 次通知；现为 3 → 1，最终状态不变） |
| `api/session-controller` 失败会话窗口的重置恢复 | C | `manager.handleConnected` 内 9 行 | 载体重置会中止所有逻辑流；error 态会话经 `resync()` 重开，而不是冻结在最后一帧直到整页刷新 |
| `api/session-controller` 环境活动合并 | C | import + 字段 + `manager.ts` 内 1 处调用、空转身份契约 | 其他运行中会话的连续 `api-session/activity` 流按会话缓冲（最新时间戳胜出），由 `sessions/fork/coalesced-refresh.ts` 大约每秒冲刷一次；单独活动立即应用，保留同步 staging 契约。`applyMutation` 在变更不翻转任何字段时返回输入数组引用，`recordMutation` 在该引用不变时跳过脏冲刷，未变化的 status/activity 帧不再重建列表 |
| `api/session-controller` 客户端选择通知 + 快照身份 | C | `manager.ts` 内约 9 行 | 身份/稳定性逻辑（entry/items/subagents/jobs 缓存、内容相等复用前快照）在 `src/client/sessions/fork/snapshot-identity.ts`；选择仍走 `markDirty`，`open`/`openSubagent` 经 `followCurrent` 同步 stage |
| `api/gateway` Remote stream mux permessage-deflate | C | ~35 行 | `RemoteStreamMuxServer` 接受 `Config.websocketPerMessageDeflate`（默认关）的 `perMessageDeflate` 参数；RFC 7692 协商配 `threshold: 1024`，journal `opened` 整窗帧压缩、实时帧原样；mux 帧处理本身未动
| `ui-workspace` order store 引用稳定 | C | `packages/client/ui-workspace/src/client/stores.ts` 中标记+import+3 行守卫 | `sessionOrderChanged` 在 `src/client/fork/order-stability.ts`；store action 在 order 未变时保留旧数组引用（时间戳照常推进） |
| `bundle/base/cordis.patch.yml` 沙箱禁用 + 本地 provider 换挂 | C | 6 行 + 注释 | Fork 决策 2026-09-05（仅 danger-full-access 部署，见 FORK_CHANGES.md）：行 `sandbox`、`sandbox-policy`、`permission` 置 `disabled: true`；执行器行 `bash-sandbox`/`pwsh-sandbox`/`fs-sandbox` 保留 id 与平台 `!!js` 门，但改挂 `@deepseek-ai/dsh-bash-local`/`dsh-pwsh-local`/`dsh-fs-local` 取代沙箱包。同步重放：按文件内 fork 注释回退 name/flag；`permission` 必须跟随沙箱行（其构造器拒绝非约束执行器之上的组合） |
| 根 `vitest.config.ts` 沙箱套件禁用 | C | 2 份名单 + 展开 | `forkDisabledSandboxTests` 把 `packages/sandbox/*`、bash-sandbox、pwsh-sandbox、fs-sandbox 单元套件移出收集；`forkDisabledSandboxCoverageExclusions` 把对应源码移出 per-file 覆盖率门。恢复路径（恢复 base 行后删除名单）见文件内注释 |
| `vitest.e2e.config.ts` sandbox 族与 ACP escalation e2e 排除 | C | 2 份排除名单 | Fork 决策 2026-09-05（见 FORK_CHANGES.md）：sandbox 族 e2e 套件（`packages/sandbox/*`、bash-sandbox、pwsh-sandbox、fs-sandbox，`*.e2e.ts`）与 ACP escalation 套件（`apps/cli/tests/profiles/acp/tests/escalation.e2e.ts`，denial→escalation 流程；保留 keyless smoke 的拆文件提示见注释）探测的都是 fork 不再挂载的机制。恢复路径见文件内注释 |
| `vitest.web.config.ts` 权限面浏览器套件排除 | C | 1 份排除名单 | Fork 决策 2026-09-05（见 FORK_CHANGES.md）：`settings-chrome`、`permission-policy-context`、`access-confirmation`、`seeded-history`、`approval-composer` 浏览器套件断言的 Permission 设置行 / 访问模式选择器 / `/permission read-only` chip 均为 fork 不再挂载的面——approval-composer 重放 `snapshots/web/approval-composer/` 下录制的审批接管会话，但必须先经 Access chip 进入 Read Only 才能触及其审批接管主体，而无宿主 permission 服务的 `permissions` 投影时该 chip 不渲染。金样保持上游形态；恢复路径见文件内注释 |
| `vitest.expected.config.ts` 沙箱形态 expected 排除 | C | 1 份排除名单 | Fork 决策 2026-09-05（见 FORK_CHANGES.md）：`subagent-inheritance` 与 `image-offload` 钉死的 read-only 委派事件、sandbox-policy prompt 上下文、拒绝输出与 `Current DSH file policy` 句均非 fork 产出；`goal`、`headless`、`semantic-checkpoint`、`subagent-diagnostic`、`workspace-context-resume` 对整段归一化会话与上游组合录制的金样做全等比较——其 `permission/preset`、`sandbox/mode`、`approval/policy` 行与 sandbox:policy 运行时上下文消息在 fork 日志中从不出现（排除前该 lane 25 项测试中 12 项在 fork 上失败）。金样保持上游形态；恢复路径见文件内注释 |
| `vitest.snapshot.config.ts` ACP snapshot 语料排除 | C | 1 份排除名单 | Fork 决策 2026-09-05（见 FORK_CHANGES.md）：`snapshots/acp/acp.snapshot.ts` 重放上游录制的沙箱 denial→escalation→approval 语料（escalation 三件套及其 sidecar 关联的 cancel/approval 与 schema-pin 用例）；实测 8 个重放中 6 个在 fork 组合下失败，且套件 pin/清单不变式禁止按场景名单排除，故整个文件不再收集。恢复路径见文件内注释 |
| `apps/web/tests/shipped-composition.e2e.ts` 沙箱缺席断言 | C | 1 段断言 | Fork 决策 2026-09-05（见 FORK_CHANGES.md）：以 `ctx.get(...)` 解析为 `undefined` 的缺席断言取代上游 `sandboxPolicy`/`permissionPresets` 模式钉（approval 仍为 `ask`）。恢复路径见文件内注释 |
| `apps/cli/tests/windows-shell.spec.ts` 权限行断言 | C | 1 段断言 | Fork 决策 2026-09-05（见 FORK_CHANGES.md）：上游「permission 面永不移动」循环断言 sandbox/permission 行保持启用；fork 断言其为 `disabled: true`，而 ui-permission/fs-sandbox/approval 保持启用。恢复路径见文件内注释 |
| `apps/cli/tests/github-webhook-real.e2e.ts` permission 预设钉移除 | C | 1 个合取项 | Fork 决策 2026-09-05（FORK_CHANGES.md）：带 key 真测不再要求 `permission/preset read-only` 事件（唯一生产者 permission-presets 随沙箱行禁用）；标题 + webhook 来源 provenance 保留。沙箱行恢复时加回合取项 |
| `ui-jobs` job 徽标手机宽度 | C | `JobListAction.module.css` 中带标记 `@media (max-width: 560px)` 块约 25 行 | 手机收缩后的操作盒里，徽标文案（"9 background jobs"）折成 72px 的一列，把 header 行从 30px 撑到 78px，并在每次任务变化时重新折行。手机块让计数保持单行并带 `text-overflow: ellipsis`，给 `.trigger` 加 `max-width: 100%` 宽度约束（缩进 `.root` 里的 inline-flex 盒子否则把 shrink-to-fit 解成 nowrap 文案而溢出该行），并让 `.root`/`.trigger`/`.count` 收缩（`min-width: 0`）而不裁切——`.root` 是绝对定位 `.menu` 的包含块。纯计数 chip 需要新的 locale 键与随视口变化的文案，客户端 CSS 表达不了 |
| `ui-agent-preset` preset 标签手机移除 | C | `AgentPresetLabel.module.css` 中带标记 `@media (max-width: 560px)` 块约 10 行 | 会话 header 的 preset 标签在 560px 以下不渲染（`.label { display: none }`）：它是无控件的静态上下文，图标对每个 preset 都相同，而 `title` 携带的是 preset 描述而非名称，加宽度上限等于两者都看不到。给出 preset 名称的是设置页；新建会话处的座位（另一个组件 `AgentPresetSeat`）同样列出 preset，但其自身标签在菜单锚点没有空间时会省略成纯图标。上游在任何宽度都把图标与名称一起渲染 |
| `ui-settings-general` 设置弹窗 portal + 手机形态 | C | 新增 `src/client/fork/portal.tsx` + 模块顶部 1 处带标记 import 与 `SettingsRoot` 中 `SettingsPanel` 调用点的 1 处带标记包裹（面板自身函数体未改）+ 带标记 `@media (max-width: 560px)` 块约 70 行 + `packages/client/ui-settings-general/tests/settings-root.client.spec.tsx` 钉住挂载点 | 触发按钮位于 ui-layout 的侧栏，手机上渲染在 fork 的 transform 动画抽屉内；该抽屉是模态层 `position: fixed; inset: 0` 的包含块，于是 412×905 下该层解到 320px 抽屉（导航轨 188 + 内容列 131、控件被裁、遮罩只盖抽屉）而非视口。该层经 fork 模块挂载到 `document.body`；`react-dom`/`@types/react-dom` 按 ui-primitives 的声明进入该包 devDependencies。≤560px 时面板以单列铺满视口，导航变横向轨，关闭 36px，并带 `env(safe-area-inset-*)` 内边距。上游此处既无 portal 也无该模块的媒体规则，两条都是需重放的补丁；上游同等能力的引导弹窗（`ui-primitives` 的 `Modal`）本就 portal，若上游把设置弹窗迁到该 primitive，首选改为对齐上游。`ui-sidebar/src/client/SidebarRoot.tsx` 中一处带标记的注释修正记录：该面板不再是侧栏列的 fixed 后代 |
| `ui-chat` 折叠态 Think 摘要 | C | `ReasoningRow.tsx` 处替换一个表达式（删除上游两个取行辅助函数）+ 删 2 条 CSS 规则 + `token-format.ts` 新增 `formatExactCount` + 2 个 locale key + 从落定消息内嵌带时间戳流折出的 `reasoningSpans`（`AssistantChatData` 一个字段、Assistant Definition 一处折叠、经 `AssistantMarkdown`/`AssistantNodeView` 的一个 prop） | **保留** —— 折叠的 Think 行改为显示推文字符数（`1,234 字符` / `1,234 characters`）与落定的流式耗时（`· 12.3s`），取代上游「已结算取首行 / 流式取末行」的文本预览，让过程扫读不再夹带推文散文；展开内容保持上游形态。耗时取自落定消息本已内嵌的带时间戳流，因此不新增任何会话读取；运行中的块与没有记录到跨度的块只显示计数。`formatExactCount` 承载精确整数分组，`formatExactTokens` 委托给它；两个含 Think 行的 jsdom 规格、定义规格中的跨度用例、lifecycle-chrome 的钉住轮询、选择性压力断言，以及 19 个可比较的 `snapshots/web/**` golden 随之移动。整体采用上游 `ReasoningRow.tsx` 或其样式表会静默恢复预览与尾部钉住，删掉新增导出、契约字段或 locale key 会让某个调用点无法编译 |
| `subagent` 每实例模型别名面 + 每子代理作用域 | C | `src/index.ts` +138/-14（别名面 9 处标记点，其中 4 处上游表达式就地替换；另加守卫 3 处）+ fork 模块 `src/fork/model-alias.ts`（190 行）、`src/fork/delegation-scope.ts`（108 行）与 `src/fork/config-surface.ts`（65 行）+ `tests/fork-delegation-alias.spec.ts`（44 个测试） | **保留** —— 官方委派工具现自带别名面，本部署此前从包外插件 `dsh-subagent-dispatch` 取得它，因此它的 9 个工具面（通用 `subagent` 加 8 个角色工具）都以 `tool-subagent` 实例挂载。四个配置键，全部保持「省略即缺省」。`models` 是实例的别名表（`{alias, provider, model, reasoningEffort?}[]`）：存在时**替换**上游面向模型的 `provider`/`model`/`reasoning_effort` 参数为单一可选 `model` 别名，而这张表就是该实例的**授权面**——面外别名在调用期 fail loud 并枚举允许项，session 级 `subagent-model-selection` 策略对别名实例不参与。`defaultModel` 是调用省略 `model` 时使用的别名（必须在表内；缺省为表首项）。`cwd`（子代理工作区；加载期校验「非空绝对路径 + 已存在目录」）与 `skillFilter`（`allow`/`deny` 恰一个方向，全量校验：对象、已知键、字符串数组）随 start request 透传；进程内驱动把 `cwd` 落到子会话 header。两个别名/作用域模块都在加载期校验，配置错误在挂载时被拒而不是留到第一次委派。`models` 与 `modelSelectionSettings: true` 同在时挂载即 fail loud（两条模型授权路径语义冲突）；别名实例要求 provider 具备 `agentOptions` 能力（别名总要设置子路由）；解析出的 `{provider, model, reasoningEffort}` 在**调用期**对 live `llm` 适配器 preflight，故挂载期不需要 `llm`。省略 `models` 时上游工具面与路由解析保持逐字节一致：该包 126 个既有上游用例零改动，与 44 个新用例一同全绿（6 文件 / 170 测试）。重放：三个 `src/fork/` 模块整体拷贝 + 9 处标记点——两处 import、四个配置键声明、保持省略语义的 schema 键及其 `default(undefined)`（否则 Schemastery 会物化 `models: []`，被读成「已配置别名但一个都不允许」）、`apply()` 内两个解析器与互斥检查、provider 能力守卫、描述后缀、别名参数分支、路由解析/preflight 分支，以及两处请求展开——另加守卫三处：`src/index.ts:44` 的 import、`:200` 的派生键集、`:388` 的 `apply()` 首行调用。**2026-09-16 补记 —— 顶层未知键守卫**：`src/fork/config-surface.ts`（65 行：`allowedConfigKeys(dict)` + `assertKnownConfigKeys(config, allowed)`）补上本次迁移打开的静默透传缺口。Cordis 经 Schemastery 的**非严格** object schema 解析插件配置（`vendor/schemastery/src/index.ts`：`Schema.resolve(data, schema, options, strict = false)`，以及 `Schema.extend('object', ...)` 的 `if (!strict) merge(result, data)`），未声明的顶层键被直接并入解析结果；包外 `dsh-subagent-dispatch` 自带 `assertKnownKeys`，迁移把这条保证丢了，于是 preset 行把 `models:` 拼成 `modelz:` 时改为挂载上游面——没有 `model` 参数、`defaultModel` 成为死键、子代理继承父路由——且毫无声响。守卫把 fail-loud 恢复到 `apply()` 的首条语句（`src/index.ts:388`，在 `models`×`modelSelectionSettings` 互斥检查之前），消息点名未知键并列出允许集。允许集是**派生**而非重述：Schemastery 的 object schema 把成员表挂在 schema 对象上，故 `Config.dict` 运行时可达，键集即 13 个已声明键——上游 9 个（`provider`、`toolName`、`modelSelectionSettings`、`enableRunInBackground`、`backgroundMode`、`agentOptions`、`persona`、`toolFilter`、`maxDepth`）加 fork 4 个（`models`、`defaultModel`、`cwd`、`skillFilter`）——模块内的字面量列表仅作 `dict === undefined` 的兜底，因此上游往 `Config` 加键即自动放宽接受集，无需第二份清单。同一个拼写错误的另一半是孤立 `defaultModel`：`models` 缺席时别名面不存在而该键成为死键，故 `src/fork/model-alias.ts:76-86` 现在抛出消息 "tool-subagent: `defaultModel` is configured without `models` — add the alias table or remove `defaultModel`"。证据：`tests/fork-delegation-alias.spec.ts` 内守卫的 5 个用例（挂载期拒绝未知键并点名允许集、`apply()` 绕过 Schemastery 时同样拒绝、派生集被证明等于 `Config.dict` 的键且字面量兜底完整、仅含上游键的配置照常挂载且不物化任何 fork 字段，以及 `defaultModel` 无 `models`），本行计数随之 178→190 行、38→44 用例、6 文件 164→170 测试；`npx tsc -b tsconfig.host.json` exit 0 |
| `tool-agent-team` 非成员请求渲染 | C | `src/index.ts` 内 6 行 | `team:policy` 系统提示段用会抛错的 `membership()` 解析归属，于是被 `maybeInstall` 创建期竞态放行的 agent（委派深度 2，子代理 descriptor 晚于 `agent/created`）在下一次请求渲染时失败：整轮以 `reason.kind = 'error'` 收尾，运行结算为 `stopReason=error`，父侧遂对「其实已经跑过」的子代理报 `subagent run failed`。现在非成员经 `tryMembership` 渲染为空段，并由 `tests/tool-team.spec.ts` 的回归测试钉住。 |
| fork 差异面校验器 `scripts/verify-fork-surface.ts` + `scripts/fork-surface-baseline.txt` | A | 1 个脚本 + 1 份冻结基线 | 本 fork 自有的登记校验器：只读盘点，证明 `packages/`、`apps/`、`vendor/` 下的每个 fork 标记都能从本清单的某一行到达、每个 tier C 行点名的文件仍带标记、没有行指向不存在的路径，且本文件与其中文对照保持相同的标题结构与行数。基线之外的发现会让运行失败（`--json` 供机器消费，`--update-baseline` 在人工复核后重新冻结）；基线只允许冻结被接受的**匹配器局限**（按角色、整包或上下文登记、而非按校验器可识别的路径登记的行），绝不冻结真实的标记或登记缺口。在 runbook 第 3 步（走清单之前）与 lefthook pre-push 的 `fork surface` job 中运行。上游永远不会拥有这两个文件，同步时原样搬移 |
| 顶层 `deliberations/` 圆桌记录 | 已退役 | — | masked-tool-payload 审议记录（19 个文件，2026-09-05 作为纯文档 Tier-A 增量收编，承载 0.1.2-alpha.1 吸收的圆桌与工作日志）在仓库内零引用——私有知识库保留原始 crate——2026-09-19 消费者审计遂删除副本：fork 树不携带任何无引用的增量 |
| `ui-conversation` composer 仅组合键提交 | C | 5 个带标记文件 + 金句：`input/editor/keymap.ts` 的 Enter 守卫、`skeleton/InputBar.tsx` 的 3 处标记编辑（常量 `'enter'` 手势、删除空草稿 steer 分支、连同 `canSteerQueue` 派生删除 steer placeholder 臂）、`client/locales.ts` 的 `settings.enter.description` 值与删除的 `placeholder.steerQueue` 键（中英）、`apps/web/tests/steering.e2e.ts` 的 3 个 `describe.skip`（两个取反组合键场景与整队列 steer 场景）及其 Control+Enter 转换、`apps/web/tests/subagent-interrupt-ui.e2e.ts` 的默认 placeholder 等待、以及 `queued-image` / 设置对话框金句文案 | **保留** —— 上游 composer 按普通 Enter 提交、在 Cmd/Ctrl 组合键上反转投递模式、并从空草稿组合键 steer 整个队列，且没有任何能把 Enter 降级为换行的设置；所有者要求杜绝误发送。普通 Enter 与 Shift+Enter 插入原生换行；仅组合键提交，解析为 Send 按钮的模式（busyEnter 偏好，绝不取反）；空草稿 steer 已删除，因为连按两次组合键会把刚排队的消息 steer 进运行中的回合（[note](.agents/notes/implemented/feature/2026-09-19-composer-chord-only-submission.zh.md)）。提交策略、composer wire contract、输入 facade 与 hub、以及提交 machine 保持上游：`accelerated` 手势保留直测单元，`steerQueue` 保留 `service-orchestration` 覆盖；QueueDock 逐行 Steer 是剩余的队列进回合路径。重新施加：keymap 守卫位于 IME 守卫与菜单仲裁之后、`preventDefault()` 之前（null 事件合成 Enter 仍提交）；InputBar 的删除把分支、提示臂与派生连同两份字典键一起移除；`steering.e2e.ts` 的三个 describe 在录制 fixture 上钉住上游手势，仅当本行退役时才恢复（需重录） |

| `session-persistence-sqlite` 会话日志线程池解压（`asyncCodec`） | A | `src/compression.ts` 的 `ScannedRows`/`StoredRowScan`/`scanRowsOnThreadPool`/`HYDRATION_WINDOW = 8`（约 100 行）+ `src/store.ts` 的扫描选择（1 个字段、2 处 await 的冷读扫描）+ `src/index.ts` 的 `Config` 字段 + `tests/compression-async.spec.ts` + `tests/async-codec.spec.ts` + `tests/page-cache-async-composition.spec.ts` | **保留** —— 本包为 fork 自有，该开关除自身文件外不增加任何合并面。线上 61k 事件的大会话冷开要耗 36.4 CPU·秒，其中大头是本模块里同步的 `zstdDecompressSync`/`zstdCompressSync`，全程阻塞事件循环。开启 `asyncCodec: true` 后，两处冷读扫描（`loadStoredLog`、`loadStoredFrom`——唯一在事务之外读的两处）把每个压缩数据列按有界窗口交给 libuv 池上的 `zlib.zstdDecompress`，随后对解压后的行跑**同一个** `scanRows`，因此解码前缀与全部归类（普通数据、撕裂尾、已提交损坏、畸形行）与同步编解码一致；池解不开的列保留压缩原值，交给扫描自己的逐行解码归类。写事务期间持有的解码（`physicalSpanFrom` 的追加尾部、`commitRepair`、`truncateLog`）保持同步：在该 store 唯一的连接上，`begin` 与 `commit` 之间出现 await，会让嵌套操作的 `rollback` 终止写方的事务。压缩在两种状态下都保持 `zstdCompressSync`——异步压缩器写出的是不带 Frame_Content_Size 的流式帧，无法复现一次性 `ZSTD_compress2` 的字节——因此开与关写出的行仍逐字节相同。默认关闭；消费者是本部署的 `session-persistence-sqlite` 配置行，它在 `cacheSizeKib` 旁携带该开关。决策记录见[异步编解码记录](.agents/notes/implemented/feature/2026-09-20-async-session-codec.zh.md) |

## 优化方案（按优先级）

1. **DONE — 从 `history.ts` 抽出分页核心** — 把 `nthMessageCut`、`turnAlignedCut`、`paginateSuffix` 和 `tryIndexedPage` 的纯函数部分移入 fork 自有模块（如 `src/page-boundary.ts`，上游永远不会有的文件）。`history.ts` 只留约 10 行注入点：import、`page()` 里的 `tryIndexedPage` 调用、`paginate` 对共享游走的委托。上游重构 `page()` 时冲突面是十行而不是两百行。
2. **DONE — 把 `messageCut` 移出上游持久化接口** — fork 自有服务（独立包或 `session-query-sqlite` 内的扩展）暴露索引切点；`history.ts` 的快路径已经是可选能力发现（`SeekablePersistence` duck-typing）。上游的 `SessionPersistence` 抽象和 coordinator 回归原状；SQL 随 fork 服务走。
3. **改动时即写合并 runbook，而非同步时补** — 每个 C/D 层改动在本清单记下注入点；同步重放从头到尾照单执行。
4. **保持 D 层 diff 紧凑并带标记** — 滚动归因块带原理解释注释；上游若自行修复同一钳位 bug，该 diff 缩为零（在上游发布中关注）。
5. **DONE — AppFrame 移动端 shell** — 最大面积。现实上保持 patch（布局是上游核心组件），但把 fork 的视口机制放进 `columns.ts` 式的叶子模块，让 AppFrame 的 diff 保持 import-and-delegate 形态。
6. **PAGE_MESSAGES** — 一行；若常变可改 build 时 env（`DSH_CLIENT_PAGE_MESSAGES`）。

## 同步流程（runbook）

1. 通读 fork 基线 tag 到目标 tag 之间的全部 release note，列出其中点名的协议、会话格式与 API 破坏性变更，并规划对应适配。
2. 在 worktree `git fetch upstream && git merge upstream/master`。
3. 先跑 `npx tsx scripts/verify-fork-surface.ts`（只读，约 0.5 秒），把失败项清干净再逐行核对清单。它会点名：带标记却没有清单行登记的 fork 补丁、被 C 层行点名却已丢失标记的文件、以及中英结构漂移；新出现的结果就是真实的同步信号——补登记该面（或补回标记），不要往 `scripts/fork-surface-baseline.txt` 里加行来消音。
4. **平价复核（AGENTS.md 政策）**：对照新 tag 逐行核对清单；上游已提供等价功能时，删除该 fork 行并采用上游版本（退役记入 `FORK_CHANGES.md`）；保留行更新「上游为何不能满足」的理由。
5. A 层：无操作（上游没有这些文件）；只解决 `pnpm-workspace`/tsconfig 聚合与 `cordis.patch.yml`。
6. B 层：重放开关/常量；预期平凡的上下文冲突。
7. C 层：按本清单逐块重放局部补丁；跑所属包的测试套件。
8. D 层：重放注入点（完成上面第 4 步后，它们是 history.ts/persistence 仅剩的 diff），fork 自有模块整体拷贝。
9. 跑 `pnpm run test:gui`、session-controller 套件、`DSH_SNAPSHOT=replay pnpm run test:web`；仅在有意输出变化时刷新金样。
10. 在 `FORK_CHANGES.md` 记录本次同步。
