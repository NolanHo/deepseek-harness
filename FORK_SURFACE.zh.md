# Fork patch surface — 上游差异面清单与合并 runbook

[English](FORK_SURFACE.md) | 中文

fork 与上游的维护契约：每处差异要么是 fork 自有模块（零合并成本）、配置开关、小型行为补丁，要么是语义级核心修改。本文件盘点每个差异面、其隔离层级、以及上游同步时的重放流程。任何触及上游自有文件的改动都要同步更新本清单。

## 为什么需要这份文件

0.1.2-alpha.1 同步（上游 1079 提交）解决了 123 个冲突。痛点集中在上游自有文件中的 fork 语义。下面的清单把每个差异面分类，让下一次同步变成机械的重放清单，而不是考古挖掘。

## fork 模块约定

凡超过一行的 fork 逻辑都住在 `<pkg>/src/fork/`（client 面：`<pkg>/src/client/<area>/fork/`）的 fork 自有模块里。上游文件只带一个标记注入点——一个 import 加一个调用——前面配 `// Fork patch (FORK_SURFACE.md): ...` 注释。同步操作 = 原样搬移所有 `fork/` 目录 + 重放下方登记的注入点。内在一行式改动（常量、单 hook 互换、CSS 块、配置字段）按政策保持内联；抽取它们只会增加间接层而不会缩小合并面。

## 双路径

双路径指同一行为两边都改了——同一文件、同一算法，或同一结果的两套实现。双路径一律归上游：删除 fork 版本、采用上游实现，并在 `FORK_CHANGES.md` 记录退役，即使 fork 版本略优。保留的 fork 版本必须写明上游无法服务的生产消费者或未修复的已复现缺陷。

### 已解决与保留的双路径

| 双路径 | fork 侧 | 结论 |
| --- | --- | --- |
| TurnProcess 折叠标签 | `ui-chat/src/client/chat/fork/turn-process-summary.ts` + 视图 import | **已退役** —— 上游在同一视图内联构建同样的折叠标签；模块及其 `message.turnProcess.collapsed` 键删除，视图恢复上游实现 |
| 历史分页 | `session-controller/src/fork/page-boundary.ts` + `history.ts` 注入 | **保留——对上游 `paginate` 的 fork 修改（2026-09-26）** —— 上游在带 `turnWindow` 时展宽到同时满足两个下限的 `turn/start`，但其消息停止点切在第 max 条计数消息（或该消息的来源组头），于是下限落在长轮次内部的请求会让页首成为没有 `turn/start` 的 `assistant/message`——`session/page` 与 `follow` 首屏均已复现。fork 修改把该停止点当作锚点，继续回溯到拥有它的 `turn/start`，因此切点要么是轮次开头，要么（回溯在任何轮次开头之前先到日志开头时）是日志开头（`exhausted`）；`maxMessages` 由上限变为下限。两条读路径仍由 `SessionHistoryController.windowCut()` 取切点，该方法按请求绑定该 `paginate`，因此无论是否存在缓存记录，索引读取与观测路径端出同一页。除该修改外仍归 fork 所有的是索引物理读取——`page-boundary.ts` 的 `SeekablePersistence`/`readIndexedSuffix`/`readIndexedPage` 与 `history.ts` 的注入 |
| 冷开窗口 | `session-controller/src/fork/open-window.ts` + `history.ts` 分支 | **保留** —— 上游折叠整段日志再裁剪（最大会话要数秒 CPU 与数百 MB 堆）；fork 通过自有 seek 面只读一个索引窗口 |
| 回流稳定滚动锚点 | `ui-chat/src/client/chat/fork/scroll-anchor.ts` + 6 处 ChatView 注入 + `overflow-anchor: none` | **保留** —— 上游的 ResizeObserver 只重跟尾部、结算 effect 清空锚点，且其台账把原生锚定的写入误判为读者移动；fork 实测阅读行上方展开 8,460 px 而补偿为零 |
| 移动端 frame 组合 | `ui-layout/src/client/fork/mobile-shell.tsx` + AppFrame 与 store 字段 | **保留** —— 上游 frame 在 1024 px 以下只折叠成窄轨且没有手机形态，手机宽度无法重新展开左栏 |
| 沙箱组合 | `bundle/base/cordis.patch.yml` 执行器替换 + `permission` 禁用 | **保留** —— 上游的约束型执行器需要 bwrap 或 Landlock；本容器内核早于 Landlock 且 `bwrap` 探测失败，采用上游组合会让每条 bash 调用 fail-close。在支持 Landlock 的内核上需重新评估 |
| 活动与重建抖动 | `sessions/fork/coalesced-refresh.ts`、`sessions/projection-store.ts`、`session-query-sqlite/src/fork/live-observation-memo.ts`、空转 identity 契约 | **保留** —— 上游对每个活动事件立即应用、每次重建都新造 subagent/job 投影与快照对象、每次搜索克隆并哈希全部挂载会话；每个保留的 fork 模块都修复了已复现缺陷。0.1.7-rc.1 同步解决了本行在 0.1.5-rc.2 标记的局部重叠：`snapshot-identity.ts` 与 `ui-workspace` 的 `order-stability.ts` 已删除，因为上游现在在 `manager.buildListSnapshot` 内联构建 entry identity，并由 `tree.ts` 的 `reconcileManualOrder`/`orderByRecency` 派生提升顺序。2026-09-25 的补充覆盖控制帧路径（活动合并器从未看到它）：`projection-store.ts` 报告被接受的帧其已发布值是否真的变了（值相等时只推进序号水位，保留缓存值及其引用、不通知），`manager.handleControlFrame` 仅在真变化时标脏，`service.projectList` 对字段全等的行沿用已发布的行对象，并在 ids、行、顺序、phase 与投影都未变时完全跳过 `list.set`；同时把 `refreshProjections` 的在途探测从 values 身份改到水位，因为静默落地不再移动该身份。改动前实测：手机档客户端在**不打开任何会话**时于 310 秒窗口内烧掉约 300 秒 JS（496 个长动画帧、阻塞 280.9 秒、样式与布局 13 ms），全部归因于这条列表/树/状态重建链 |
| 打开文件路由 | `ui-chat/src/client/chat/fork/open-file-routing.ts` | **保留** —— 路由优先已安装的 `dsh-better-sidebar`，再回落上游右侧栏 |
| 通用品牌 `DSH` | 语言字典 + `apps/web` 默认值 | **保留** —— 本部署的品牌选择；上游为 `DSH Local Build` |

## 隔离层级

- **A 层 — fork 自有包**：上游永远不会有的文件。零合并成本。
- **B 层 — 配置开关**：上游文件中的一个常量或经校验的 `Config` 字段。重放分钟级。
- **C 层 — 小型行为补丁**：每个上游文件约 40 行以内、局部块。冲突小、可解。
- **D 层 — 语义级核心修改**：上游自有算法内的 fork 行为。这是合并成本所在；尽量压缩注入面。

## 当前清单（相对 fork 基线 tag dsh-v0.1.7-rc.1 的上游）

| 差异面 | 层级 | 规模 | 性质 |
| --- | --- | --- | --- |
| `packages/web/web-search-{academic,bocha,brave,zhihu}` | A | 20 文件 | fork 搜索提供方，完全隔离 |
| `.agents/notes`、文档、快照、测试更新 | A | — | 随源更新；与对应源一起重放。`apps/web/tests/preset-migration.snapshot.ts` 把上游激活后的事实尾部重新对齐到 fork 组装：`permission` 行保持禁用，因此激活不落 `permission/preset`、`sandbox/mode`、`approval/policy` 任何一条事实。Web 车道其余按 fork 组装重新对齐之处：`apps/web/tests/shipped-composition-auto.e2e.ts` 从 `shipped-composition.e2e.ts` 拆出并被 `vitest.web.config.ts` 排除，因为其中每个用例都驱动宿主 `permissionPresets` 服务把 Session 切到 Auto，而 `permission` 行被禁用使 `auto-review` 条目停在该服务上、生产者永不激活（已在 `tsconfig.host.json` 与 `apps/web/tsconfig.json` 登记）；`shipped-composition.e2e.ts` 断言仍启用的 `sandbox-policy` 行为 `danger-full-access`；`apps/web/tests/support.ts` 承载共用的 `optOutDeploymentTheme` 退出选择：写入其查询所拥有的主题键，因为宿主的 token 重定向会在启动前剥掉查询串；`sidebar-terminal.e2e.ts` 与 `thinking-markdown.e2e.ts` 经该退出选择启动；`trajectory-virtualization.e2e.ts` 按 fork 的 8 条消息页大小翻页；`chat-long-interactions.e2e.ts` 在折叠展开后重新钉住转写尾部；`remote-welcome.e2e.ts` 断言宿主发布的可信 authority 的持久确认；`details-session-lifecycle.e2e.ts` 把自动全屏块保持在 fork 768 px 移动态之上；`present.e2e.ts` 在正文提及运行期间撤下裸原生打开，使提及进入侧栏预览、而交付卡保留其定位与应用打开，`snapshots/web/present/ui.expected.md` 去掉该行不再渲染的 Access 芯片；`plan-control-row.e2e.ts` 扫到 fork 更窄控制行之下 |
| `session.ts` PAGE_MESSAGES 8（上游 50） | B | 8 行，外加 `apps/web/tests/stats-paged-history.e2e.ts` 的 `loadEarlierUntil` 辅助函数（按 8 条消息一页翻完整个日志） | 客户端页大小 |
| `client/connection` browserAuth 开关 | B | ~50 行 | 配置字段后的可选认证关闭 |
| `ui-chat` TurnProcess 折叠标签 + 时长 | 已退役（双路径） | — | 上游 0.1.5-rc.2 在 `TurnProcessNodeView` 内联构建同一折叠标签；fork 的 `src/client/chat/fork/turn-process-summary.ts`（分类计数 + 墙钟时长 + 折叠前缀）已删除，视图恢复为上游实现，fork 自有的 `message.turnProcess.collapsed` 字典键随之移除。时长仍在轮次页脚的用量详情中可见。`TurnProcessNodeView.tsx` 与 `locale.ts` 现与上游完全一致。 |
| `ui-chat` 部分历史下的 Turn Process 折叠 | 已退役 | — | **已退役** —— 上游 0.1.7-rc.1 不再给 `processWindowReady` 加 `historyIncomplete` 门（改为读窗口自身的 `turnStarted`/`turnClosed` 标志），因此已关闭的回合在展示窗口载入后即折叠；fork 被删除的 prop、`ChatNodeSeat.tsx` 的门控编辑与两个部分历史用例一并删除，`ChatView.tsx` 与上游一致 |
| `ui-chat` TurnProcess 折叠控件胶囊样式 | C | `TurnProcessNodeView.module.css` 内约 25 行 CSS | 控件渲染为贴合标签的圆角胶囊，带 hover 面，chevron 与标签留出间距——但仅在控件可操作时如此：运行中的回合使控件恒为 disabled（`turnProcessAlwaysOpen`），没有可折叠内容的回合也永不启用，因此 disabled 态保留标签的度量、去掉边框与底色，读作状态文本而不是按不动的按钮。上游渲染的是通栏分隔行、chevron 跟在末尾。标记与文案都是上游的、类名不变，因此整体采用上游样式表会静默恢复成通栏分隔线 |
| `ui-chat` ChatView 读者输入归因 | 已退役 | — | 上游自己的修复已落地（observed-top 几何台账覆盖全部输入设备，无需监听器）；fork 的设备标记补丁在 0.1.2-rc.1 同步时移除 |
| 部署级 reduced-motion 默认 | C | 1 个 shell 模块（`packages/client/web/src/fork/reduce-motion.ts`）+ `packages/client/web/src/boot.ts` 中 1 处启动调用 + 1 段 CSS | 本部署**默认无动效**：每次启动给 `<html data-reduce-motion>` 打标，`packages/client/web/src/base.css` 把动画压成一步完成。过渡保持作者时长：右侧栏面板机制需要可观测的 `transform` 过渡，一刀切 1 ms 会让它的 `transitionrun` 探针报 "panel transform transition is unavailable"，并让 `sidebar-right.e2e.ts` 的 7 个用例失败。`?reduce-motion=0` 写入按浏览器的退出选择，`?reduce-motion=1` 清除它。该标记不改变浏览器全局的 `prefers-reduced-motion`，其他站点保持各自动效 |
| 部署主题配色 | C | 1 个 shell 模块（`packages/client/web/src/fork/deployment-theme.ts`）+ 1 个样式表（`packages/client/web/src/fork/themes.css`）+ `packages/client/web/src/boot.ts` 中 1 处启动调用 + 4 条元素规则 | 本部署默认渲染暮蓝配色：每次启动给 `<html data-dsh-theme="harbor">` 打标，`packages/client/web/src/fork/themes.css` 承载 token 覆盖——owner 对本地安装的 dsh-market「夕港 / Harbor」皮肤的蓝白重染，markdown 与语法配色取自 owner 的 xbot 检出里的 "tokyo-night" 主题（代码面 `#161a2e`、链接青 `#7dcfff`）。配色同时声明在带标记的根元素**与其 body** 上：上游把 alias token 声明在 `body` 与 `body[data-ds-dark-theme]`，在应用子树里会盖过只声明在根上的块，导致面板仍是上游配色。另有四条限定在 `[data-chat-flow]` 内的规则补上没有 token 承载的对比度：两级标题（h1/h2 `#7aa2f7`，h3+ `#c8d3f5`）、行内代码芯片（`#9ece6a` 落在 `#2b3358` 上并带可见边框）、表头（`#1f2540`）。`?theme=default` 写入按浏览器的退出选择，`?theme=harbor` 清除它。上游 theme 插件只管 light/dark/system 偏好与正文字号、没有配色扩展点，因此配色放在 shell 而不是主题注册；皮肤的壁纸与运行时 hooks 不入库 |
| 无限动画的 reduced-motion 守卫 | C | 2 段 CSS 媒体块 | 上游在多数动画文件里已有 `@media (prefers-reduced-motion: reduce) { animation: none }`，但漏了仍在的两处无限动画：`ui-conversation/InputBar.pending`（`packages/client/ui-conversation/src/client/skeleton/InputBar.module.css`）与 `ui-input-trigger/MenuView.skeletonBar`（`packages/client/ui-input-trigger/src/client/MenuView.module.css`）。守卫只在读者开启该偏好时生效，其余读者的渲染完全不变。0.1.7-rc.1 同步把另外两半退役：上游用自带的 spinner 关键帧与 reduced-motion 弧取代了 `ui-primitives` 的 StateDot 点阵追逐，`ui-conversation/TodoPanel` 的动画字形现在就是 `StateDot` |
| 部署默认下的运行中指示器 | C | 1 个配色 token | 部署标记把动画压成一步完成，而上游 0.1.7-rc.1 用自带的 spinner 关键帧与 reduced-motion 弧取代了 `StateDot` 的点阵追逐，因此 fork 的 `:root[data-reduce-motion]` 保活块与追逐的静态中间相透明度随追逐一并删除。配色这一半保留：`packages/client/web/src/fork/themes.css` 把 `--dsw-static-deepseek-450` 从 `rgb(86 134 254)` 提到品牌浅蓝 `#8fbaff`，即默认文件类型图标经 `--dsh-file-type-default-color` 所用的静态步进 |
| `frontend-static` 对内容哈希资产发 immutable 缓存头 | C | `packages/host/frontend-static/src/index.ts` 约 10 行 | dist 服务端此前对任何文件都只回 `content-type`，于是每次刷新都重新下载 shell 里带内容 hash 的 JS 与 CSS，而 `/plugins` 下的插件包早已是 immutable（线上实测：每次刷新的 shell JS 与 CSS 为 438 KiB gzip，占该次刷新的 448 KiB，解压后 1,352 KiB）。现在命中 Vite 内容哈希输出目录（`assets/`、`preview/`；`apps/web/vite.config.ts` 为每个产物名固定 `[hash]`）的请求带 `cache-control: public, max-age=31536000, immutable`；index 响应与稳定命名的根文件（`favicon.svg`、`manifest.webmanifest`、`preview.html`）保持原有头，重建的 bundle 或变更的 index 仍能到达浏览器。上游 `frontend-static` 没有缓存头配置项，因此这条规则是它服务函数内带标记的补丁；Electron/桌面宿主的 dist 处理（`apps/desktop-host`）保持上游的无头行为，因为本部署走 Web 面 |
| `ui-conversation`/`ui-chat` CSS overflow-anchor + 安全区 | C | ~40 行 | 滚动容器锚定；局部规则 |
| `ui-conversation` 打字重渲染收敛 | C | 2 个 null 叶子 + 3 处调用点，位于 `packages/client/ui-conversation/src/client/skeleton/ConversationContent.tsx` 与 `packages/client/ui-conversation/src/client/skeleton/DefaultConversationViews.tsx` | `ConversationRoot` 订阅整个 input store 只是为了把 owner props 交给 dock，`ConversationSession` 订阅 input store 与持久化草稿只是为了播种与镜像草稿；这两个订阅让每敲一键都重渲染它们所在的列——header、已加载的 transcript 窗口（`conversation.view`）与 composer 栈。订阅现下沉到 `InputDockSlot`（在 `ConversationContent.tsx` 中）与 `DraftMirror`（在 `DefaultConversationViews.tsx` 中）两个不渲染 DOM 的叶子；dock 仍拿到实时 owner props，播种 effect 保持原有的 mount-only 语义。实测（1,519 行 / 30,404 节点）：每键 JS 工作 3 ms → 1 ms，总延迟在测量机上两边都是 16 ms 一帧 |
| `ui-conversation` settle 隐藏加上限 | C | 1 个 fork 模块 （`packages/client/ui-conversation/src/client/skeleton/settle-hide.ts`）+ `packages/client/ui-conversation/src/client/skeleton/ConversationMainPanel.tsx` 里 1 处带标记调用点 | `.root[data-phase='settling'] .composerSeat { visibility: hidden }` 属上游且无上限，而 `settling` 的两个输入都是不保证会落定的异步事实：continuable 子会话的 `parentAvailable` 只由**成功**的 `subagents.list` 读取写入（两个失败分支最多保留旧值、不通知任何人；重新点选子会话刷新的是子会话自己的目录；挂起的读取会占住 manager 的 single-flight 条目），而 `Session.doOpen` 等待日志首帧没有超时、对非 remote 异常直接 rethrow 并把 `openState` 留在 `loading`。于是恢复进 continuable 子会话的页面会把 composer 隐藏一整个页面生命周期——2026-09-22 在部署实例上复现为 `settling` + 136px 座位 `visibility: hidden`、133 轮转写正常渲染、重新点选不重试、只有刷新才恢复。本 fork 用 `useSettleHide`/`SETTLE_HIDE_LIMIT_MS = 5000` 给一段连续 pending 期加上限，phase 到期回落 `hero`/`active`，正常停靠的 composer 回来。消费者：本部署的桌面 GUI。验证：`pnpm exec vitest run packages/client/ui-conversation` 加上 Agent Note 里的隔离实例检查。决策记录：[note](.agents/notes/implemented/bug-fix/2026-09-22-bounded-settle-hide.zh.md) |
| `api/session-controller/src/history.ts` 快路径注入 | C | `page()` 约 10 行，`follow` 约 60 行 | 分页注入委托给 fork 自有 `src/fork/page-boundary.ts`（索引后缀读取、其稠密与终点证明、128/4096 余量梯子），并把请求自己的页规则交给每个读取计划：`windowCut()` 返回按请求的 `beforeSeq`、`maxMessages` 与 `turnWindow` 绑定后的上游 `paginate`（含 fork 的轮次对齐切点修改）。打开注入增加同步服务检查与带观测回落的窗口快照分支，并在交付该快照后不激活 Session：只读打开既不读整段日志也不挂载它，两者都推迟到第一次显式解析时支付。两条快速路径都通过 `seekSurface` 辅助函数取得持久化方法，并绑定到 `ctx.get` 返回的 tracker 代理：未绑定的提取方法会以包装对象为 `this` 运行，在任何读取自身状态的提供方里抛错。无法挂载的会话也不再在打开时报告 `api-session/error`：窗口化路径把该失败推迟到第一次发送消息时暴露，只有观测路径的 `promote` 仍立即报告。两条快路径都改用 `windowCut()` 切页、不再自算页面下界，因此各自端出的页面与观测路径一致；`IndexedPagePlan.seedMessages`（`request.turnWindow?.minMessages ?? request.maxMessages ?? DEFAULT_MAX_MESSAGES`）现在只给索引读取播种子；`maxMessages` 是共享 `paginate` 会向外展宽到轮次开头的消息下限，而 `exhausted` 的走查（窗口内该下限之下没有轮次开头）把该窗口标记为未证明，快路径因此在深余量上重试，或返回 `undefined` 交给观测路径，而不是端出该切片 |
| `api/session-controller/src/fork/open-window.ts` 冷打开窗口 | C | 约 110 行 + `history.ts` 分支 | 打开快照的窗口化读取：基于 `readIndexedSuffix`（`page-boundary.ts`，新增窗口化读取、可选计划字段 `seedMessages`/`throughSeq`/`windowFloor` 与只做证明的接受判定）的读数计划，切点由请求自己的 `windowCut()` 规则给出；随后是检查点门槛、闭合尾部证明与折叠。上游的 `follow` 折叠整段日志再裁剪，在最大会话上要花数秒 CPU 与数百 MB 堆；上游没有可委托的索引寻址面（fork 的 `seekable`/`messageCut`/`readFrom` 在自己的持久化后端上）。窗口化读取先调用 `seekable`，返回 false 时在任何切点查询或读取之前就退出。`history.ts` 只保留带标记的 try 与观测回落，并交付窗口快照而不激活 Session |
| `session-projection-cache/src/fork/checkpoint-read.ts` 检查点读取与写回 | C | 约 90 行 + `index.ts` 注册 | 窗口化打开需要按会话 id 读取已存检查点行，首次冷打开还要装上供下一次折叠的记录。上游的公有折叠面只返回视图值而非可播种的行，其 `hydratePrepared` 刻意不写任何东西；fork 增加一个 symbol 键注册的服务私有查找（WeakMap 不可行：cordis 交给调用方的是 tracker 代理而非注册对象）、`readCheckpoint` 读取，以及持久前缀写回。`readColdSessionLog` 报告 `durableEventCount`，使写回的行绝不越过已存日志末端（`observation.ts` 负责透传）|
| `session-persistence-sqlite` 整包 + 寻址面（`seekable`/`messageCut`/`readFrom`）+ 重写能力（`truncate`） | A（0.1.2-rc.1 起 fork 拥有） | 9 个 src 文件 + `delete-event-row.sql` + `select-events-from-through.sql` + `select-max-seq.sql` | **0.1.5-rc.2 STATUS: adapted** —— src/handle.ts（每会话句柄、live write-behind、撕裂尾截断、进程内单写者）与 src/restore.ts（legacy 行经 `dsh-session-format-catalog` 恢复、读取时 v0→v3 迁移、写打开时 publish）；`preparedSessionCacheSize` 已移除（上游删除了 prepare/borrow seam），legacy 行仍保留 packed-row 解码，schema 20 + 19→20 迁移 + `seed_length` 不变；包套件 13 文件 / 176 测试中 175 通过、1 跳过，upstream 契约为 21/21 memory + 20/20 file-backed。 | 上游按 JSONL-only 决策删除该包但为 out-of-tree 提供者保留接缝；fork 保留并移植到 handle-based `PersistenceBackend`/coordinator，采用上游最终 SCHEMA_VERSION 20（`ignorable` 列兼作 packed 行判别符；一次性 19→20 原地迁移；`seed_length` 承载继承切点）。`seekable(id)` 是快速路径的门槛：仅当前格式行返回 true，且只读元数据。它之所以存在，是因为 `loadStoredFrom` 的历史分支会用格式目录恢复整段日志再切片，而切片用的 `fromSeq` 来自存储物理空间，恢复出的序号已重排、不在该空间里——生产库绝大多数行正是这种，窗口计划在那里每次尝试都要读完整段日志却拿到空页。该包同时实现 seam 的可选重写能力：`SqliteSessionHandle.truncate`/`SqliteStore.truncateLog` 在一个 immediate 事务中丢弃切点起的全部已存事件，重写唯一跨切点的 packed 行以只保留其切点以下成员，递增一次 revision，失败即回滚；句柄游标随之重置，收缩守卫改按已存 revision 判定。本行登记的页缓存字段见[页缓存 Config 字段记录](.agents/notes/implemented/feature/2026-09-20-sqlite-page-cache-config.zh.md) |
| `session-persistence` seam：可选 `SessionHandle.truncate?` 重写能力 | C | `src/handle.ts` 约 20 行 + `src/index.ts` 类 JSDoc | `SessionHandleTruncateOptions { signal?, append? }` 与可选成员 `truncate?(toSeq, options?)`：丢弃 `toSeq` 起的全部已存事件，并在同一步持久落地 `append`（按 seq、起点恰为 `toSeq`），仅限写句柄；读句柄以 `SessionReadOnlyError` 拒绝，写所有权丢失时以 `SessionOwnershipLostError` 拒绝。无法重写已提交日志的后端省略该方法，需要重写的消费者响亮地拒绝（`session/rewrite-unsupported`）而不是把未满足的操作当作成功——fork 的 SQLite 后端实现了它，上游随产品交付的 JSONL 后端省略它。`index.ts` 类 JSDoc 为这条唯一截断路径放弃了绝对化的「绝不重写」表述。`pnpm run gen-cordis-api` 会把该段落抄进生成的 `packages/extensions/tool-cordis/src/api-catalog.ts` 服务描述，因此该生成文件携带的是生成文本形式的标记，其中不存在任何手写补丁 |
| `api/session-controller` 就地重写编排 | C | fork 模块 52 + 55 行；`commands.ts` +200/-6；`agent.ts` +62/-7；`index.ts` 中继守卫；`types.ts` 2 个 wire 字段 + 2 个错误码 | 纯函数切点校验在 fork 模块 `src/fork/rewrite-history.ts`（`resolveRewriteCut`：武装 seq 必须是已存的 `user/message`，子代理会话拒绝，切点为不晚于它的最后一个 `turn/start` 的 seq，并只向前越过该消息**自己**的准入 `agent/inbox/spliced` 事件，低于 `inheritedEventCount` 拒绝；随后 `planInboxRepair` 折叠保留前缀，为仍处于 pending 的消息返回一条取消 splice，由 `truncate` 的 `append` 落地）。`commands.ts` 保留标记的 `createSeeded` 注入、`prompt` 重写分支与两个编排方法（`rewriteHistory`：仅 queue、仅 live Agent idle、inbox 有排队拒绝、dispose → 写句柄截断 + 追加 `planInboxRepair` 算出的中和移除 → close → 丢弃该会话的投影缓存记录；`truncateStoredLog` 在 `handle.truncate` 未定义时响亮失败）。`agent.ts` 保留标记的句柄持有字段及 `disposeAgent`/`createSeeded` 与 resume/create 调用点改动。`types.ts` 增加 `SessionPromptRequest.rewriteFrom`/`SessionPromptValue.rewrote` 与 `session/rewrite-unavailable`（带类型化原因）/`session/rewrite-unsupported` 两个错误码。fork 模块 `src/fork/rewrite-hold.ts` 在整个重写窗口内扣住 Agent 拆除本会发布的 `api-session/removed` 公告：`prompt` 在 `rewriteHistory` 之前进入 hold、在 `resolveAgent` 结算后释放，`index.ts` 的 `session/disposed` 中继先询问 `commands.deferRemoval`；若窗口结束时没有活跃 Agent 则重新公告该移除——没有它，每个客户端都会在重写期间丢掉列表行与 selection，并渲染新会话画面 |
| `session-projection-cache` 重写失效 + 种子水线规则 | C | `index.ts` +52/-2（含类 JSDoc）| `SessionProjectionCache.discard(id)` 持久删除已存记录，并先清掉同一 id 上 live Session 的待写；重写之所以调用它，是因为缓存的身份校验看不出日志发生了倒退。种子读（`hydratePrepared`、`coldSnapshot`）另外拒绝水位在其将要折叠进的 durable prefix 之上的已存行（`rowsWithinDurablePrefix`）：缓存只可落后于日志、绝不可超前，否则从被删除事件折叠出的行会把已删除消息恢复成排队项并重放成新轮次。零 I/O 的列表读（`cachedSnapshot`）没有日志可对照，依赖写入方的丢弃。对只增的日志无行为变化：正常检查点的每一行都在其折叠所达的 durable prefix 之下。0.1.7-rc.1 合并保留 fork 的三参数 `hydratePrepared(session, events, durableEventCount)` 写回与 `rowsWithinDurablePrefix`；`cachedSnapshot` 与 `cachedPredecessorTitle` 是上游自有的两参数与一参数面 |
| `session-controller` 客户端 + `ui-conversation` 重写透传与窗口重建 | C | `sessions/session.ts` 约 35 行；`input/facade.ts` 约 30 行；`hub.ts`/`service.ts` 约 15 行；`contract/session.ts` + `contract/input.ts` 的 wire 字段 | `SessionPromptOptions.rewriteFrom` 随 prompt wire 请求上行；`ClientSession.prompt` 在 Host 回报 `rewrote === true` 时先 `resync()` 重建窗口再结算，武装提示词被接纳却没有 `rewrote` 时（与未重启的旧 Host 的版本偏差）结算为可见的 `session/rewrite-missed` 失败——绝不静默追加。composer 输入机（`ui-conversation`）在草稿旁持有 `InputState.rewriteFrom`/`InputActions.setRewriteFrom`：武装值随默认发送路径上行，随已提交草稿清空，失败发送时随草稿恢复。子代理寻址的续写分支忽略该选项 |
| `session-query-sqlite` 活动观察记忆化与每请求活动观察额度 | C | `index.ts` 内约 15 行、fork memo 模块，以及两个调用方各一处分配（`api/session-controller` 的 `list.ts` 与 `tool-session-query` 的 `operations.ts`） | `src/fork/live-observation-memo.ts` 的 memo 以 Session 实例与其 O(1) 日志长度（`session.seq`）为键，因此未变化的已挂载会话会被直接复用、不再物化事件；fork 之前的键（`length:tailSeq:tailTime`）只有在 `session.snapshotEvents()` 之后才能算出，也就是在读之后。现在 `recompute` 是唯一的事件读取；`index.ts` 只留字段、委托与 `_chargeLiveObservation`，每次观测按经校验的 `maxLiveObservedEvents` Config 上界（默认 100,000）计入 `exec.liveObservationBudget`，两个调用方各按请求分配一份 |
| `session-query-sqlite` 请求级对账、已持久化冷读额度与 rowid 区段删除文档 | B + C | 一个 `Config` 字段（`maxPersistedObservedEvents`，默认 `SESSION_QUERY_SQLITE_DEFAULT_MAX_PERSISTED_OBSERVED_EVENTS = 5000`）、`packages/session-query/session-query/src/types.ts` 的 `SessionSearchReconciliationBudget` 字段、`index.ts` 约 150 行（带 `_reconciliations` WeakMap 的 `_reconcileOnce`、`_persistedObservationBudget`、带跨尝试读取复用、最新优先候选顺序与 `truncated` 返回的 `_observeStable`，以及 `_deleteSessionDocuments`/`_recordDocumentRange`）、`schema.ts` 的 `persisted_doc_ranges` 与 `live_doc_ranges` 两张表，以及两个调用方各一处分配（`api/session-controller` 的 `list.ts`、`tool-session-query` 的 `operations.ts`） | **保留（2026-09-26 新增）** —— 已复现的缺陷，活动观察与排序文档额度都未覆盖的另一半读放大。一次 `session/search` 请求里三个乘数叠加：controller 的分页循环每页对账一次（`SEARCH_PROVIDER_CALL_LIMIT` = 100 次调用）、`_observeStable` 每趟都重新列出 store 并冷读索引修订未命中的每个已持久化会话、且在快照变动时重试，而占主导的是：每个被重建索引的会话都用 `DELETE FROM persisted_docs WHERE session_id = ?` 删除旧文档，`session_id` 是 `UNINDEXED` 因此 `EXPLAIN QUERY PLAN` 报 `SCAN persisted_docs VIRTUAL TABLE`。在本部署派生索引的副本上（96,177 个文档）实测：删除一个会话的 3,440 个文档读 213-374 MB，`rowid BETWEEN` 同样扫描，只有 `rowid = ?` 是 docid 查找（每行 0.02 MB；同样 3,440 行 18.8 MB / 101 ms）。于是单个会话的重建索引代价是 O(整个索引) 而非 O(该日志)，生产上重开搜索实测 `session/search {query:'卡顿'}` 为 115 秒 / 140 GB。在隔离实例上复现（独立 `DSH_HOME`、6,111 个会话 / 2,010,364 事件的 store 副本、三个挂载会话 70,100 + 31,944 + 25,250 事件、一个已持久化会话每 3 秒追加、全新派生索引）：master 上首个请求读 **10,349 MB / 34.7 秒（56.3 CPU·秒）**，第二个 **29,908 MB / 37.3 秒（54.3 CPU·秒）**（成本随索引增长），整段请求期间 1.3 秒的页面 ping 全部超时；对 33 个会话 / 144,994 事件子集的冷读一趟为 27.5 秒 / 4,367 MB。修复让一次对账服务一个调用方请求（提供方把这一趟记忆在 `exec.reconciliationBudget` 上，每页使用该 memo 的持久化绑定与索引代），把每次持久化冷读计入 `maxPersistedObservedEvents`，达到上界即停止冷读并提交已读内容（其余由后续请求建索引，追赶因此收敛而不是让每次搜索都失败），重试复用同一绑定下修订未变的已读日志，优先索引最新的会话，并通过 `persisted_doc_ranges`/`live_doc_ranges` 记录的 rowid 区段删除会话文档（区段表出现之前已建的索引会付一次扫描）。同实例同负载、5,000 事件默认值实测：首个请求 **199.4 MB / 13.5 秒**（19.3 CPU·秒、22 个会话），随后两个 **52.9 MB / 3.5 秒** 与 **67.8 MB / 3.5 秒**，窄查询 `session-query` **60.8 MB / 3.1 秒**返回 5 条命中、其热重复 **60.4 MB / 3.0 秒**（持续追加会话自身的日志即「+ 一条日志」超出量），宽查询 `the` 在 **314 毫秒 / 0.31 MB** 内以 `gateway/bad-request` 拒绝，子集冷读一趟 **126.3 MB / 4.24 秒**且只读一个会话而非五个——首个请求读量比 master 少 52 倍，之后每个请求比 master 的第二个少 440 倍。该夹具的稳态请求仍要付那个持续追加的会话，即额度文档写明的「+ 一条日志」超出量。同步后重新应用：`Config` 字段及其校验与默认值；`SessionSearchReconciliationBudget` 类型及其 `exec` 字段；`_reconcileOnce`、`_reconciliations` WeakMap、`_persistedObservationBudget` 与两个调用方的 `reconciliationBudget` 分配；`_observeStable` 内以（绑定身份, 修订）为键的 `read` memo、每次 `readColdSessionLog` 前的 `spent >= bound` 准入检查、`truncated` 计数、最新优先候选排序，以及 truncated 时跳过的稳定性重试；`_deleteSessionDocuments`、`_recordDocumentRange` 与两张区段表及其 `DERIVED_USER_TABLES` 条目。回归：`packages/session-query/session-query-sqlite/tests/sqlite.spec.ts`——对八个 300 事件 JSONL 会话的文件式索引设 `maxPersistedObservedEvents: 600`，首个请求以 289 KiB 索引两个会话、四次请求收敛到全部八个（master 首个请求读 1.0 MiB）；在每对 list 之间移动快照的追加者返回已读会话，master 抛 `SESSION_QUERY_PERSISTENCE_FAILED`；32 个会话中重建一个已更改会话读 1.07 MB，master 读 9.5 MB；两页游标遍历只列一次语料，master 重新对账并抛 `SESSION_QUERY_STALE_CURSOR`；`packages/api/session-controller/tests/session-search.host.spec.ts` 断言两页携带同一份对账额度。验证：`NODE_ENV=test npx vitest run packages/api/session-controller packages/session-query` |
| `client/ui-layout` AppFrame 移动端抽屉 | C | import + 组合 + 2 个 store 字段（`stores.ts`）+ `packages/client/ui-layout/tests/layout-store.client.spec.ts` 与 `packages/client/ui-layout/tests/app-frame.client.spec.tsx` 中的断言 | **0.1.5-rc.2 状态：已重放** —— 上游 frame 在其 1024px 自动折叠以下只提供窄轨，故 fork 保留自己的手机断点（768px，与右侧栏自身的全屏阈值一致）：左栏变为固定覆盖抽屉，frame 只渲染一条流内轨道。详情面板退役——上游右侧栏在 768px 以下本就全屏覆盖并提供自己的退出入口。抽屉的「当前会话」监听改从列表行的保留绑定 `mainView`（`retainedBy.mainView`）派生会话 id，因为上游 0.1.7-rc.1 移除了 fork 早先读取的 `SessionListState.current` 字段。 | 视口机制 hook 与抽屉 chrome 在 `src/client/fork/mobile-shell.tsx`（自带 `MOBILE_VIEWPORT`，因上游 `columns.ts` 去掉了该常量）；AppFrame 的 diff 为 import、一次 `useMobileRegime` 调用、抽屉分支、单轨模板、`RightbarColumn` 的 `mobile` 属性（使其不占该轨道）与两处手柄守卫；`sidebar` slot 的 owner share（`src/client/index.ts`）新增 `mobile` 标记，由抽屉分支置真、桌面分支置假；`stores.ts` 增加 `mobile`/`drawerOpen` 及 `setMobile`/`setDrawerOpen` 与 `toggleSidebar` 的移动分支；抽屉标签是 fork 加入 `common` 字典的 `sidebar.open` 键；移动端 chrome 的 CSS（`.rightbarCol[data-mobile]`、`.scrim`、`.mobileDrawer`、`.mobileMenu`）在 `AppFrame.module.css` 内由一段分区标记覆盖（上游的 `.overlayLayer` 位于该段内） |
| `ui-sidebar` 手机抽屉设置席位 | C | `packages/client/ui-sidebar/src/client/SidebarRoot.tsx`（带标记的条件渲染）+ `packages/client/ui-sidebar/src/client/SidebarRoot.module.css`（带标记的 `.drawerLogoRow` 块）+ `packages/client/ui-sidebar/src/client/contract/slots.ts`（席位 JSDoc）+ `packages/client/ui-sidebar/tests/sidebar-root.client.spec.tsx` + `packages/client/ui-sidebar/tests/sidebar-styles.client.spec.ts` + 由 `gen-client-catalog` 生成的 `packages/extensions/cordis-client-runner/src/client/slot-catalog.ts`（逐字嵌入带标记的 owner props 文本） | **保留** —— 上游只在栏页脚渲染 `sidebar.settings` 席位；在 fork 的手机抽屉中，这条 42px 通栏行紧贴会话列表、正落在拇指停留处，在该处滚动或点选会话都会进入 Settings。frame 的 `mobile` 标记把该席位移入抽屉品牌行、紧随品牌之后，并以紧凑图标呈现（`{ wide: false }`）；页脚只保留 `sidebar.footer.action`。品牌行从行首开始排布、折叠按钮留在行尾，因此品牌旁的空白不再是 New Session 的点击区。紧凑席位不携带连接状态控件，该控件属于宽版页脚席位。 |
| 通用客户端品牌读作纯 `DSH` | C | 2 个字典值（`packages/client/locale/src/locales/en.ts`、`packages/client/locale/src/locales/zh.ts`）+ 2 处构建默认值 + 测试断言 | 上游通用品牌为 `DSH Local Build`，fork 读作 `DSH`（两个语言字典的 `brand.localBuild` 驱动侧栏品牌名与文档标题兜底，`apps/web/index.html` 与 `vite.config.ts` 的 `DEFAULT_CLIENT_TITLE` 承载静态默认值）。部署标题（`DSH_CLIENT_TITLE`）仍可覆盖，品牌下方构建版本行不变。这两个字典同时承载移动端抽屉的 `sidebar.open` 键与 Think 行的两个键，整体采纳上游字典会把它们一起删掉 |
| 对话头部移动端精简 | C | 两个 `max-width` 媒体块（767.98px 与 560px） | 抽屉断点以下，会话头部左边距让开 frame 固定的抽屉入口（≤767.98px 时 `padding-left: 56px`），且两个块都预留与抽屉按钮 `top` 相同的顶部安全区内边距（≤767.98px 为 `padding-top: calc(10px + env(safe-area-inset-top))`，560px 块并入 `padding` 简写）——否则在有状态栏内边距的设备上，固定按钮会比标题行低整整一条状态栏。手机宽度下头部内边距与标签节奏收紧，会话标题面包屑吸收该行的剩余宽度、没有宽度底线，并经 `.crumb` 省略号截断；纯计数 chip 的数字保持单行（各自 `.countCompact` 声明 `white-space: nowrap`，否则被压缩的计数会在数字内部断行，叠成两行的计数便压到尾部控件上）；`.headerActions` 保持内容宽度（`flex: 0 0 auto`，故 chip 不会被压到小于自身固有宽度），且刻意不加 `overflow: hidden`——那会把 job 徽标绝对定位的菜单裁到不可见；`.headerCorner` 归零其桌面 `-16px` 右外边距——该外边距是按桌面 28px 内边距配的，而手机内边距为 12px，使角落控件在 360/412/480/560px 下都越出视口 4px。上游该模块完全没有媒体查询，整块采纳会一次性删掉手机内边距与其上所有规则；`header-phone-styles.client.spec.ts` 会拒绝这种做法。同一个块刻意不碰 lineage slot：在子代理面包屑里该 slot 渲染子代理会话自己的标题（`switcher` 变体），且它是进入子代理目录的唯一手机路径——抽屉树与会话搜索都把子代理子节点过滤掉。真正让出手机 header 的元信息是 agent-preset 标签（见下方 `ui-agent-preset` 行）。session-log 那一半随 0.1.5-rc.2 退役：上游把该控件挪进头部的 28px 省略号菜单，已不再挤占手机头部 |
| `api/session-controller` `openWorkspacePath` 桌面门控 + `ui-chat` 文件打开路由与提及门控 | C | 门控保持内联（5 行）；路由与提及门控 = `packages/client/ui-chat/src/client/apply.ts` 中的 import + 2 处调用（`routeOpenFile`、`nativeGatedMentions`）+ 3 处标记，由 `packages/client/ui-chat/tests/apply-inject.client.spec.tsx` 钉住 | 打开 RPC 查询 `canOpenPath()` 并快速失败（内在一行式）；路由决策与原生门控的提及包装在 `src/client/chat/fork/open-file-routing.ts` |
| `bundle/base` 沙箱能力 | C | 行 `disabled` 标志 + 执行器行名 | **0.1.5-rc.2 状态：部分恢复** —— 上游的 workspace-files 与 deliverables 条目需要 `sandboxPolicy` 服务，因此 sandbox 与 sandbox-policy 行重新启用并钉 `mode: danger-full-access`（本部署唯一模式）；执行器行仍为本地提供方，permission-presets 行保持禁用。web-app 组合里的 `ui-permission` 客户端行随之禁用（`packages/bundle/web-app/cordis.patch.yml`）：该路由由宿主服务提供，客户端半边只会让每次启动记一条 `/api/permissionPresets/catalog` 404；两行同时启停。 |
| `client/modules` + `client/web` 延迟启动批次 | C | `packages/client/modules/src/client/entries.ts`（带标记的 Loader 条目对账）+ `packages/client/modules` 中的 boot manifest/解析器切分 + `packages/client/web/src/boot.ts` 的两段式 shell 启动 | `WebBootBatchPhase 'deferred'` + `Config.defer` 切分 + 两段式 boot；上游形态（增量线格式字段、空默认）；defer 名单是部署配置而非仓库状态。shell 这一半带标记注入（`activateDeferred`、挂载前 roster 过滤）；manifest/解析器那一半不带 fork 标记，标记普查看不出那里的回退——请按相对基线 tag 的 diff 重新施加 |
| `ui-workspace` 提升头部稳定 | 已退役 | — | **已退役** —— fork 的 `nextSessionOrderAccount`/`reconciledSessionOrder` 补丁及其 `src/client/fork/order-stability.ts` 模块已删除：上游 0.1.7-rc.1 自己经 `src/client/tree.ts` 的 `reconcileManualOrder`/`orderByRecency` 派生提升头部，`WorkspaceBrowser` 与 store 直接调用这两个函数 |
| `ui-chat` 消息动作条 DOM 钩子 | C | `MessageIconActions.tsx` 1 个属性 | 动作条带 `data-message-actions`，第三方插件可在原生复制/分支按钮旁挂自己的动作。上游 0.1.5 删除了 `data-time-hover-root`，而 fork 的 `dsh-rewind` 正锚定它；没有稳定钩子时插件静默不渲染。该属性纯新增、不可见 |
| `ui-tool` 通用行的 `bodyContent` + `GenericToolCard` 导出 | C | 3 个上游文件共约 15 行（`ToolRow.tsx`、`GenericToolCard.tsx`、`client/index.ts`） | **保留** —— fork 的包外插件 `dsh-apollo` 保留所有工具共用的通用折叠行、只替换其展开体：`ToolRow` 新增可选的 `bodyContent: ReactNode`，以块级元素渲染为展开体的第一个子节点（在 Input/Output 卡片之外），取代默认的「参数」（INPUT）段——不格式化参数、不打印 INPUT 标签、不渲染空卡片——并让该行本身可展开；OUTPUT 段、卡片链、摘要与 Inspect 胶囊不变，不传该属性的行与上游渲染完全一致。`GenericToolCard` 透传同一属性，浏览器入口导出该组件与其 props 类型；这个值导出是对客户端导出纪律规则的有意例外，已获用户同意。重新施加：该属性、以 `inputBody` 替代 `cardBody`、展开体首子节点、`GenericToolCard` 的透传，以及两个入口导出 |
| `ui-chat` StatsLine 绘制后测量 | 已退役 | — | 上游在 0.1.5-rc.2 用两个图标胶囊（`StatsPills.tsx`）取代统计条并删除 `StatsLine.tsx`，绘制后省略号测量已无载体；该行仅作 runbook 存档 |
| `skill` 注册表目录限制 | C | `index.ts` 内 17 行 | 全部逻辑在 `src/fork/skill-restrict.ts`（编译、按作用域存储、链式过滤）；`index.ts` 只留 import、一个字段、两个标记的委托调用；allow/deny 互斥记录在模块 JSDoc |
| `subagent` 子代理 cwd + skillFilter | C | 接缝 + 5 处调用点（`child-agent.ts`、`continuation.ts`、`continuation-activation.ts`、`subagent-in-process-driver`、`tool-subagent`） | 逻辑在 `src/fork/child-scoping.ts`（`stampChildCwd`、`applyChildSkillFilter`）；贯穿 `childSessionMeta`/continuation/驱动的请求字段是保留的接缝；descriptor v3→4（同步遇上游 bump：字段取并集）；冷恢复的 cwd 权威仍在会话 header  **重放清单（2026-09-16 更正）**：接缝是**四处**调用点而非两处 —— `child-agent.ts`（`childSessionMeta` + `applyChildComposition`）、`continuation.ts`（冷恢复 meta + 两处 continuable 组合）、`packages/subagent/subagent/src/continuation-activation.ts`（首次激活组合的 `skillFilter` 字段，第三处组合点）、以及 `subagent-in-process-driver/src/index.ts`（`setup()` 把 `request.skillFilter` 交给 `applyChildComposition`；`agents.create()` 把 `request.cwd` 交给 `childSessionMeta`）。驱动那两行在 0.1.5-rc.2 同步时被静默丢弃、连「re-apply」提交本身也漏掉了，所以本行现在登记全部调用点；`child-isolation.spec.ts` 覆盖这四处。第五处是产出方：`tool-subagent/src/index.ts` 把自身实例配置校验过的 `cwd`/`skillFilter` 展开进它交给驱动的 start request（`src/fork/delegation-scope.ts`，登记在该包的别名面行内），`fork-delegation-alias.spec.ts` 覆盖三条 start 路径。 |
| `session-format-v0-to-v1` released-v0 子代理 descriptor 兼容 | C | `src/fork/subagent-descriptor-compat.ts` + 5 个上游文件各一处标记委托 | 冻结的 released-v0 边界只接受 descriptor 版本 3，而安装的 `@deepseek-ai/dsh-subagent` 向新 payload 打版本 4，已存的 2/3 payload 又早于版本 4 新增的组装输入；该模块拥有可选成员集合、当前版本与 2/3→4 升级，`dispositions.ts`、`migration.ts`、`payload-validation.ts`、`validation.ts` 与 `tests/relationships.spec.ts` 各保留一处标记委托 |
| `session-format-v3-to-v4` released-v3 描述符世代兼容 | C | `packages/session/session-format-v3-to-v4/src/fork/subagent-descriptor-compat.ts` + `src/index.ts` 一处标记导出与 `src/facts.ts` 一处标记委托 | 冻结的 released-v3 边界只解释描述符版本 1 至 3，其他世代一律降级为 unknown 模式的目录成员；而安装的 `@deepseek-ai/dsh-subagent` 会给新载荷打上版本 4。该模块拥有被解释的世代集合（`CURRENT_SUBAGENT_DESCRIPTOR_VERSION = 4`、`isInterpretedDescriptorVersion`），使已经列出该子会话的父会话保住它携带的模式与标签。`src/index.ts` 在 `historicalChildCatalogSource` 旁再导出 `childCatalogFact`，供数据库支撑的父会话自行校验单个子会话的 fact，而不因某个子会话的不可用描述符拒绝整个父会话；`src/facts.ts` 把版本判断委托给该模块。与上方 released-v0 兼容行同构；消费者：本部署 `session-persistence-sqlite` 的 V4 恢复（其 child-catalog 读取导入 `childCatalogFact`） |
| `subagent` released descriptor identity 兼容 | C | `packages/subagent/subagent/src/fork/released-descriptor-identity.ts` + `packages/subagent/subagent/src/projection.ts` 一处标记委托 | 安装的 descriptor 世代是 4（见上方 per-child `cwd`/`skillFilter` 行），而恢复出来的历史子会话保留它写入时的世代（2 或 3）——正是上方 released-v3 目录行已经为父会话成员关系解释过的世代。严格折叠只认安装版本，因此这类子会话给不出 identity，会话控制器的地址栅栏会对一个父目录自己已经列出的子会话回答 `subagent descriptor is corrupt`。`foldReleasedDescriptorIdentity` 把 released 载荷盖上安装版本后交给 `foldSubagentDescriptor` 校验，因此 identity 出自同一套 schema，未声明成员或损坏字段仍然建立不了 identity；`foldSubagentDescriptor` 对 resume 组装保持严格，`subagent` projection 的 `stateVersion` 从 2 提到 3，使缓存为 `null` 哨兵的行重新折叠。消费者：本部署被恢复的 released 子会话，由该包的 released-descriptor-identity 规格与会话控制器冷会话规格覆盖 |
| `ui-chat` ChatView 重排稳定滚动锚点 | C | `chat/fork/scroll-anchor.ts` + 标记注入位于上游抽出的读者 hooks：`packages/client/ui-chat/src/client/chat/use-chat-viewport.ts`、`packages/client/ui-chat/src/client/chat/use-chat-reading.ts`、`packages/client/ui-chat/src/client/chat/use-chat-scroll.ts` | `chat/fork/scroll-anchor.ts` 在每次流列尺寸变化时（折叠塌陷、图片加载、展开）重新断言持有的读者行，并在滚动写入之后测量锚点使连续回调保持幂等，配 observed-top 台账保证读者输入归因正确；锚点行被隐藏时回退到其上方最近的存活可见行。ChatView 在每次离开底部的滚动采样上武装锚点，在每次前插补偿后重新持有读者行，在保存位置恢复时捕获恢复的行，并在 load-earlier 结算后保留锚点（上游清空 effect 已移除）。0.1.7-rc.1 合并把这些注入移进了上游抽出的读者 hooks：`use-chat-viewport.ts` 保留重排重断言与回调分支，`use-chat-reading.ts` 保留离开底部的武装、前插重持与保存位置捕获，`use-chat-scroll.ts` 保留 load-earlier 结算 |
| `api/session-controller` 流式快照节奏 | C | `session.ts` 1 行 + 注释 | 实时 chunk 呈现（`publishAssistantEntry` 的 `transient` 分支）改用 `markFrameDirty()` 标记会话快照，一个流式帧最多发布一次。帧调度器早已存在（`Notifier.markFrameDirty`）但生产代码零调用：每个 chunk 都走一次微任务冲刷，令每个 `useSession` 消费者逐 chunk 重渲染（实测 3 个 chunk 帧 → 3 次通知；现为 3 → 1，最终状态不变） |
| `api/session-controller` 失败会话窗口的重置恢复 | C | `manager.handleConnected` 内 9 行 | 载体重置会中止所有逻辑流；error 态会话经 `resync()` 重开，而不是冻结在最后一帧直到整页刷新 |
| `api/session-controller` 环境活动合并 | C | import + 字段 + `manager.ts` 内 1 处调用、空转身份契约 | 其他运行中会话的连续 `api-session/activity` 流按会话缓冲（最新时间戳胜出），由 `sessions/fork/coalesced-refresh.ts` 大约每秒冲刷一次；单独活动立即应用，保留同步 staging 契约。`applyMutation` 在变更不翻转任何字段时返回输入数组引用，`recordMutation` 在该引用不变时跳过脏冲刷，未变化的 status/activity 帧不再重建列表 |
| `api/session-controller` 客户端选择通知 + 快照身份 | 已退役 | — | **已退役** —— fork 的 `src/client/sessions/fork/snapshot-identity.ts` 及其规格已删除：上游 0.1.7-rc.1 在 `manager.buildListSnapshot` 内联构建 entry identity，客户端 session-controller 也不再声明 `SubagentCatalogSnapshot` 类型（`ui-subagent` 中的同名类型属该包自有）。选择仍走 `markDirty` 通知，`open`/`openSubagent` 仍经 `followCurrent` 同步 stage |
| `api/gateway` Remote stream mux permessage-deflate | C | ~35 行 | `RemoteStreamMuxServer` 接受 `Config.websocketPerMessageDeflate`（默认关）的 `perMessageDeflate` 参数；RFC 7692 协商配 `threshold: 1024`，journal `opened` 整窗帧压缩、实时帧原样；mux 帧处理本身未动
| `ui-workspace` order store 引用稳定 | 已退役 | — | **已退役** —— `sessionOrderChanged` 位于已删除的 `src/client/fork/order-stability.ts`，其 `stores.ts` 三行守卫随模块一并不存在：store 现在直接采用上游 `reconcileManualOrder` 派生的顺序，`WorkspaceBrowser` 调用的也是同一个函数。0.1.7-rc.1 同步采用上游辅助函数，而未重新施加该守卫 |
| `bundle/base/cordis.patch.yml` 沙箱禁用 + 本地 provider 换挂 | C | 6 行 + 注释 | Fork 决策 2026-09-05（仅 danger-full-access 部署，见 FORK_CHANGES.md）：行 `sandbox`、`sandbox-policy`、`permission` 置 `disabled: true`；执行器行 `bash-sandbox`/`pwsh-sandbox`/`fs-sandbox` 保留 id 与平台 `!!js` 门，但改挂 `@deepseek-ai/dsh-bash-local`/`dsh-pwsh-local`/`dsh-fs-local` 取代沙箱包。同步重放：按文件内 fork 注释回退 name/flag；`permission` 必须跟随沙箱行（其构造器拒绝非约束执行器之上的组合） |
| 根 `vitest.config.ts` 沙箱套件禁用 | C | 2 份名单 + 展开 | `forkDisabledSandboxTests` 把 `packages/sandbox/*`、bash-sandbox、pwsh-sandbox、fs-sandbox 单元套件移出收集；`forkDisabledSandboxCoverageExclusions` 把对应源码移出 per-file 覆盖率门。恢复路径（恢复 base 行后删除名单）见文件内注释 |
| `vitest.e2e.config.ts` sandbox 族与 ACP escalation e2e 排除 | C | 2 份排除名单 | Fork 决策 2026-09-05（见 FORK_CHANGES.md）：sandbox 族 e2e 套件（`packages/sandbox/*`、bash-sandbox、pwsh-sandbox、fs-sandbox，`*.e2e.ts`）与 ACP escalation 套件（`apps/cli/tests/profiles/acp/tests/escalation.e2e.ts`，denial→escalation 流程；保留 keyless smoke 的拆文件提示见注释）探测的都是 fork 不再挂载的机制。恢复路径见文件内注释 |
| `vitest.web.config.ts` 权限面浏览器套件排除 | C | 1 份排除名单 | Fork 决策 2026-09-05（见 FORK_CHANGES.md）：`settings-chrome`、`permission-policy-context`、`access-confirmation`、`seeded-history`、`approval-composer` 浏览器套件断言的 Permission 设置行 / 访问模式选择器 / `/permission read-only` chip 均为 fork 不再挂载的面——approval-composer 重放 `snapshots/web/approval-composer/` 下录制的审批接管会话，但必须先经 Access chip 进入 Read Only 才能触及其审批接管主体，而无宿主 permission 服务的 `permissions` 投影时该 chip 不渲染。金样保持上游形态；恢复路径见文件内注释 |
| `vitest.expected.config.ts` 沙箱形态 expected 排除 | C | 1 份排除名单 | Fork 决策 2026-09-05（见 FORK_CHANGES.md）：`subagent-inheritance` 与 `image-offload` 钉死的 read-only 委派事件、sandbox-policy prompt 上下文、拒绝输出与 `Current DSH file policy` 句均非 fork 产出；`goal`、`headless`、`semantic-checkpoint`、`subagent-diagnostic`、`workspace-context-resume` 对整段归一化会话与上游组合录制的金样做全等比较——其 `permission/preset`、`sandbox/mode`、`approval/policy` 行与 sandbox:policy 运行时上下文消息在 fork 日志中从不出现（排除前该 lane 25 项测试中 12 项在 fork 上失败）。金样保持上游形态；恢复路径见文件内注释 |
| `vitest.snapshot.config.ts` ACP snapshot 语料排除 | C | 1 份排除名单 | Fork 决策 2026-09-05（见 FORK_CHANGES.md）：`snapshots/acp/acp.snapshot.ts` 重放上游录制的沙箱 denial→escalation→approval 语料（escalation 三件套及其 sidecar 关联的 cancel/approval 与 schema-pin 用例）；实测 8 个重放中 6 个在 fork 组合下失败，且套件 pin/清单不变式禁止按场景名单排除，故整个文件不再收集。恢复路径见文件内注释 |
| `apps/web/tests/shipped-composition.e2e.ts` 沙箱缺席断言 | C | 1 段断言 | Fork 决策 2026-09-05（见 FORK_CHANGES.md）：以 `ctx.get(...)` 解析为 `undefined` 的缺席断言取代上游 `sandboxPolicy`/`permissionPresets` 模式钉（approval 仍为 `ask`）。恢复路径见文件内注释 |
| `apps/cli/tests/windows-shell.spec.ts` 权限行断言 | C | 1 段断言 | Fork 决策 2026-09-05（见 FORK_CHANGES.md）：上游「permission 面永不移动」循环断言 sandbox/permission 行保持启用；fork 断言其为 `disabled: true`，而 ui-permission/fs-sandbox/approval 保持启用。恢复路径见文件内注释 |
| `apps/cli/tests/github-webhook-real.e2e.ts` permission 预设钉移除 | C | 1 个合取项 | Fork 决策 2026-09-05（FORK_CHANGES.md）：带 key 真测不再要求 `permission/preset read-only` 事件（唯一生产者 permission-presets 随沙箱行禁用）；标题 + webhook 来源检查保留。沙箱行恢复时加回合取项 |
| `ui-jobs` job 徽标手机宽度 | C | `.countCompact` 默认规则 + `JobListAction.module.css` 中带标记 `@media (max-width: 560px)` 块约 21 行 + `JobListAction.tsx` 中 1 处带标记的 span | 手机收缩后的操作盒里，徽标文案（"9 background jobs"）折成 72px 的一列，把 header 行从 30px 撑到 78px，并在每次任务变化时重新折行；随后的省略号修复只让文案截断成 "4 bac…"，仍占着会话标题需要的宽度（412×915 实测：标题 88px、lineage chip 108px、job 徽标 65px）。现在手机块隐藏 `.count` 并渲染组件内的 `.countCompact`——仅任务计数，保留运行中圆点与菜单箭头——触发行 `aria-label` 仍为完整本地化计数。`.countCompact` 声明 `white-space: nowrap`，因此被压缩的徽标不会把数字断开，否则会把计数叠成两行列并撑高手机 header 行。`.trigger` 保留 `max-width: 100%` 约束（若操作盒再次被压缩，`.root` 里的 inline-flex 触发器否则会把 shrink-to-fit 解成内容的 min-content 而溢出该行），`.root`/`.trigger` 继续 `min-width: 0` 且不裁切，因为 `.root` 是绝对定位 `.menu` 的包含块。纯计数 chip 需要把数字放进独立 span，CSS 才能隐藏文案；本地化文案仍是可访问名称 |
| `ui-subagent` 头部 lineage chip 手机计数 | C | `SubagentHeaderLineage.tsx` 中 1 处带标记的 span + `SubagentHeaderLineage.module.css` 的 `.countCompact` 默认规则与 `@media (max-width: 560px)` 块 | lineage chip 的完整计数文案（"32 subagents"）在手机头部截断为 "32 su…"，并占着会话标题需要的宽度（实测同 `ui-jobs` 行）。手机块隐藏 `.count` 并渲染 `.countCompact`——仅子代理计数——触发行保留完整本地化 `aria-label`，`switcher` 变体（面包屑里子代理会话自己的标题）不受影响。`.countCompact` 声明 `white-space: nowrap`，因此被压缩的 chip 不会把数字断开，否则会把计数叠成两行列并撑高手机 header 行。上游在任何宽度都渲染完整文案，整块采纳其模块会恢复拥挤行；`subagent-header-lineage-phone-styles.client.spec.ts` 会拒绝这种做法 |
| `ui-agent-preset` preset 标签手机移除 | 已退役 | — | **已退役** —— 上游 0.1.7-rc.1 在 `AgentPresetLabel.module.css` 里交付了同一手机端结果：`@container (max-width: 540px) { .label { display: none } }`，因此 fork 带标记的 `@media (max-width: 560px)` 块被删除 |
| `ui-settings-general` 设置弹窗 portal + 手机形态 | C | 新增 `src/client/fork/portal.tsx` + 模块顶部 1 处带标记 import 与 `SettingsRoot` 中 `SettingsPanel` 调用点的 1 处带标记包裹（面板自身函数体未改）+ 带标记 `@media (max-width: 560px)` 块约 70 行 + `packages/client/ui-settings-general/tests/settings-root.client.spec.tsx` 钉住挂载点 | 触发按钮位于 ui-layout 的侧栏，手机上渲染在 fork 的 transform 动画抽屉内；该抽屉是模态层 `position: fixed; inset: 0` 的包含块，于是 412×905 下该层解到 320px 抽屉（导航轨 188 + 内容列 131、控件被裁、遮罩只盖抽屉）而非视口。该层经 fork 模块挂载到 `document.body`；`react-dom`/`@types/react-dom` 按 ui-primitives 的声明进入该包 devDependencies。≤560px 时面板以单列铺满视口，导航变横向轨，关闭 36px，并带 `env(safe-area-inset-*)` 内边距。上游此处既无 portal 也无该模块的媒体规则，两条都是需重放的补丁；上游同等能力的引导弹窗（`ui-primitives` 的 `Modal`）本就 portal，若上游把设置弹窗迁到该 primitive，首选改为对齐上游。`ui-sidebar/src/client/SidebarRoot.tsx` 中一处带标记的注释修正记录：该面板不再是侧栏列的 fixed 后代 |
| `ui-chat` 折叠态 Think 摘要 | C | `ReasoningRow.tsx` 处替换一个表达式（删除上游两个取行辅助函数）+ 删 2 条 CSS 规则 + `token-format.ts` 新增 `formatExactCount` + 2 个 locale key + 从落定消息内嵌带时间戳流折出的 `reasoningSpans`（`AssistantChatData` 一个字段、Assistant Definition 一处折叠、经 `packages/client/ui-chat/src/client/chat/AssistantMarkdown.tsx`/`AssistantNodeView` 的一个 prop） | **保留** —— 折叠的 Think 行改为显示推文字符数（`1,234 字符` / `1,234 characters`）与落定的流式耗时（`· 12.3s`），取代上游「已结算取首行 / 流式取末行」的文本预览，让过程扫读不再夹带推文散文；展开内容保持上游形态。耗时取自落定消息本已内嵌的带时间戳流，因此不新增任何会话读取；运行中的块与没有记录到跨度的块只显示计数。`formatExactCount` 承载精确整数分组，`formatExactTokens` 委托给它；两个含 Think 行的 jsdom 规格、定义规格中的跨度用例、lifecycle-chrome 的钉住轮询、选择性压力断言，以及 19 个可比较的 `snapshots/web/**` golden 随之移动。整体采用上游 `ReasoningRow.tsx` 或其样式表会静默恢复预览与尾部钉住，删掉新增导出、契约字段或 locale key 会让某个调用点无法编译 |
| `subagent` 每实例模型别名面 + 每子代理作用域 | C | `src/index.ts` +138/-14（别名面 9 处标记点，其中 4 处上游表达式就地替换；另加守卫 3 处）+ fork 模块 `src/fork/model-alias.ts`（190 行）、`src/fork/delegation-scope.ts`（108 行）与 `src/fork/config-surface.ts`（65 行）+ `tests/fork-delegation-alias.spec.ts`（44 个测试） | **保留** —— 官方委派工具现自带别名面，本部署此前从包外插件 `dsh-subagent-dispatch` 取得它，因此它的 9 个工具面（通用 `subagent` 加 8 个角色工具）都以 `tool-subagent` 实例挂载。四个配置键，全部保持「省略即缺省」。`models` 是实例的别名表（`{alias, provider, model, reasoningEffort?}[]`）：存在时**替换**上游面向模型的 `provider`/`model`/`reasoning_effort` 参数为单一可选 `model` 别名，而这张表就是该实例的**授权面**——面外别名在调用期 fail loud 并枚举允许项，session 级 `subagent-model-selection` 策略对别名实例不参与。`defaultModel` 是调用省略 `model` 时使用的别名（必须在表内；缺省为表首项）。`cwd`（子代理工作区；加载期校验「非空绝对路径 + 已存在目录」）与 `skillFilter`（`allow`/`deny` 恰一个方向，全量校验：对象、已知键、字符串数组）随 start request 透传；进程内驱动把 `cwd` 落到子会话 header。两个别名/作用域模块都在加载期校验，配置错误在挂载时被拒而不是留到第一次委派。`models` 与 `modelSelectionSettings: true` 同在时挂载即 fail loud（两条模型授权路径语义冲突）；别名实例要求 provider 具备 `agentOptions` 能力（别名总要设置子路由）；解析出的 `{provider, model, reasoningEffort}` 在**调用期**对 live `llm` 适配器 preflight，故挂载期不需要 `llm`。省略 `models` 时上游工具面与路由解析保持逐字节一致：该包 126 个既有上游用例零改动，与 44 个新用例一同全绿（6 文件 / 170 测试）。重放：三个 `src/fork/` 模块整体拷贝 + 9 处标记点——两处 import、四个配置键声明、保持省略语义的 schema 键及其 `default(undefined)`（否则 Schemastery 会物化 `models: []`，被读成「已配置别名但一个都不允许」）、`apply()` 内两个解析器与互斥检查、provider 能力守卫、描述后缀、别名参数分支、路由解析/preflight 分支，以及两处请求展开——另加守卫三处：`src/index.ts:44` 的 import、`:200` 的派生键集、`:388` 的 `apply()` 首行调用。**2026-09-16 补记 —— 顶层未知键守卫**：`src/fork/config-surface.ts`（65 行：`allowedConfigKeys(dict)` + `assertKnownConfigKeys(config, allowed)`）补上本次迁移打开的静默透传缺口。Cordis 经 Schemastery 的**非严格** object schema 解析插件配置（`vendor/schemastery/src/index.ts`：`Schema.resolve(data, schema, options, strict = false)`，以及 `Schema.extend('object', ...)` 的 `if (!strict) merge(result, data)`），未声明的顶层键被直接并入解析结果；包外 `dsh-subagent-dispatch` 自带 `assertKnownKeys`，迁移把这条保证丢了，于是 preset 行把 `models:` 拼成 `modelz:` 时改为挂载上游面——没有 `model` 参数、`defaultModel` 成为死键、子代理继承父路由——且毫无声响。守卫把 fail-loud 恢复到 `apply()` 的首条语句（`src/index.ts:388`，在 `models`×`modelSelectionSettings` 互斥检查之前），消息点名未知键并列出允许集。允许集是**派生**而非重述：Schemastery 的 object schema 把成员表挂在 schema 对象上，故 `Config.dict` 运行时可达，键集即 13 个已声明键——上游 9 个（`provider`、`toolName`、`modelSelectionSettings`、`enableRunInBackground`、`backgroundMode`、`agentOptions`、`persona`、`toolFilter`、`maxDepth`）加 fork 4 个（`models`、`defaultModel`、`cwd`、`skillFilter`）——模块内的字面量列表仅作 `dict === undefined` 的兜底，因此上游往 `Config` 加键即自动放宽接受集，无需第二份清单。同一个拼写错误的另一半是孤立 `defaultModel`：`models` 缺席时别名面不存在而该键成为死键，故 `src/fork/model-alias.ts:76-86` 现在抛出消息 "tool-subagent: `defaultModel` is configured without `models` — add the alias table or remove `defaultModel`"。证据：`tests/fork-delegation-alias.spec.ts` 内守卫的 5 个用例（挂载期拒绝未知键并点名允许集、`apply()` 绕过 Schemastery 时同样拒绝、派生集被证明等于 `Config.dict` 的键且字面量兜底完整、仅含上游键的配置照常挂载且不物化任何 fork 字段，以及 `defaultModel` 无 `models`），本行计数随之 178→190 行、38→44 用例、6 文件 164→170 测试；`npx tsc -b tsconfig.host.json` exit 0 |
| `tool-agent-team` 非成员请求渲染 | 已退役 | — | **已退役** —— 上游 0.1.7-rc.1 用静态 `POLICY` 常量渲染 `team:policy` 系统提示段，并把角色与名称移入 teammate 系统提醒，因此 fork 的 `tryMembership` 渲染与 `tests/tool-team.spec.ts` 里的回归测试一并删除。`tryMembership` 只作为上游 `maybeInstall` 的创建期门继续存在 |
| fork 差异面校验器 `scripts/verify-fork-surface.ts` + `scripts/fork-surface-baseline.txt` | A | 1 个脚本 + 1 份冻结基线 | 本 fork 自有的登记校验器：只读盘点，证明 `packages/`、`apps/`、`vendor/` 下的每个 fork 标记都能从本清单的某一行到达、每个 tier C 行点名的文件仍带标记、没有行指向不存在的路径，且本文件与其中文对照保持相同的标题结构与行数。基线之外的发现会让运行失败（`--json` 供机器消费，`--update-baseline` 在人工复核后重新冻结）；基线只允许冻结被接受的**匹配器局限**（按角色、整包或上下文登记、而非按校验器可识别的路径登记的行），绝不冻结真实的标记或登记缺口。在 runbook 第 3 步（走清单之前）与 lefthook pre-push 的 `fork surface` job 中运行。上游永远不会拥有这两个文件，同步时原样搬移 |
| 顶层 `deliberations/` 圆桌记录 | 已退役 | — | masked-tool-payload 审议记录（19 个文件，2026-09-05 作为纯文档 Tier-A 增量收编，承载 0.1.2-alpha.1 吸收的圆桌与工作日志）在仓库内零引用——私有知识库保留原始 crate——2026-09-19 消费者审计遂删除副本：fork 树不携带任何无引用的增量 |
| `ui-conversation` composer 仅组合键提交 | C | 5 个带标记文件 + 金句：`packages/client/ui-conversation/src/client/input/editor/keymap.ts` 的 Enter 守卫、`packages/client/ui-conversation/src/client/input/editor/view-binding.ts` 的组合键分支与已删除的空草稿 steer、`skeleton/InputBar.tsx` 的 3 处标记编辑（常量 `'enter'` 手势、删除空草稿 steer 分支、连同 `canSteerQueue` 派生删除 steer placeholder 臂）、`client/locales.ts` 的 `settings.enter.description` 值与删除的 `placeholder.steerQueue` 键（中英）、`apps/web/tests/steering.e2e.ts` 的 3 个 `describe.skip`（两个取反组合键场景与整队列 steer 场景）及其 Control+Enter 转换、`apps/web/tests/subagent-interrupt-ui.e2e.ts` 的默认 placeholder 等待、以及 `queued-image` / 设置对话框金句文案 | **保留** —— 上游 composer 按普通 Enter 提交、在 Cmd/Ctrl 组合键上反转投递模式、并从空草稿组合键 steer 整个队列，且没有任何能把 Enter 降级为换行的设置；所有者要求杜绝误发送。普通 Enter 与 Shift+Enter 插入原生换行；仅组合键提交，解析为 Send 按钮的模式（busyEnter 偏好，绝不取反）；空草稿 steer 已删除，因为连按两次组合键会把刚排队的消息 steer 进运行中的回合（[note](.agents/notes/implemented/feature/2026-09-19-composer-chord-only-submission.zh.md)）。提交策略、composer wire contract、输入 facade 与 hub、以及提交 machine 保持上游：`accelerated` 手势保留直测单元，`steerQueue` 保留 `service-orchestration` 覆盖；QueueDock 逐行 Steer 是剩余的队列进回合路径。重新施加：keymap 守卫位于 IME 守卫与菜单仲裁之后、`preventDefault()` 之前（null 事件合成 Enter 仍提交）；InputBar 的删除把分支、提示臂与派生连同两份字典键一起移除；`steering.e2e.ts` 的三个 describe 在录制 fixture 上钉住上游手势，仅当本行退役时才恢复（需重录） |

| `session-persistence-sqlite` 会话日志线程池解压（`asyncCodec`） | A | `src/compression.ts` 的 `ScannedRows`/`StoredRowScan`/`scanRowsOnThreadPool`/`HYDRATION_WINDOW = 8`（约 100 行）+ `src/store.ts` 的扫描选择（1 个字段、2 处 await 的冷读扫描）+ `src/index.ts` 的 `Config` 字段 + `tests/compression-async.spec.ts` + `tests/async-codec.spec.ts` + `tests/page-cache-async-composition.spec.ts` | **保留** —— 本包为 fork 自有，该开关除自身文件外不增加任何合并面。线上 61k 事件的大会话冷开要耗 36.4 CPU·秒，其中大头是本模块里同步的 `zstdDecompressSync`/`zstdCompressSync`，全程阻塞事件循环。开启 `asyncCodec: true` 后，两处冷读扫描（`loadStoredLog`、`loadStoredFrom`——唯一在事务之外读的两处）把每个压缩数据列按有界窗口交给 libuv 池上的 `zlib.zstdDecompress`，随后对解压后的行跑**同一个** `scanRows`，因此解码前缀与全部归类（普通数据、撕裂尾、已提交损坏、畸形行）与同步编解码一致；池解不开的列保留压缩原值，交给扫描自己的逐行解码归类。写事务期间持有的解码（`physicalSpanFrom` 的追加尾部、`commitRepair`、`truncateLog`）保持同步：在该 store 唯一的连接上，`begin` 与 `commit` 之间出现 await，会让其他操作的 `BEGIN` 在解码期间全部以嵌套事务错误失败。压缩在两种状态下都保持 `zstdCompressSync`——异步压缩器写出的是不带 Frame_Content_Size 的流式帧，无法复现一次性 `ZSTD_compress2` 的字节——因此开与关写出的行仍逐字节相同。默认关闭；消费者是本部署的 `session-persistence-sqlite` 配置行，它在 `cacheSizeKib` 旁携带该开关。决策记录见[异步编解码记录](.agents/notes/implemented/feature/2026-09-20-async-session-codec.zh.md) |

| `subagent` catalog listing 冷读上限（`coldReadConcurrency`、`coldReadBudget`） | C | `packages/subagent/subagent/src/index.ts`（经校验的 `Config` 及其两个 schema 键与默认值、解析后的 `listingLimits`、唯一花掉它们的 listing 调用点 `listDescendants`）+ `packages/subagent/subagent/src/list-children.ts`（`SubagentListingLimits` 与 `ColdReadJob` 类型、提到预算选择之前的投影缓存档、`selectColdReads`、`observeColdIdentity`）+ `packages/subagent/subagent/tests/list-children.spec.ts`（`SubagentRuntime cold-read bounds` 块的 7 个用例） | **保留** —— 上游对每个非 live 子会话都发起一次身份读取，只用一个硬编码的并发常量 4 兜底；于是投影缓存为冷时，一个有 422 个子会话的 workspace 每一遍都要为每个非 live 子会话解码一份完整存储日志：在与生产同构的隔离实例上实测，首次 GUI 加载每一遍跑 423 次这样的解码、共 4–5 遍，把单核钉在 100% 约 8 分钟，期间所有 RPC 与流挂起、客户端超时并反复重连。缓存热时该路径零读取，因此这是一项尾部风险：任何缓存失效（新的 `DSH_HOME`、任一投影单元 stateVersion 变化、rewrite discard、storages 丢失）都会把它变回冷态。上游没有单次 listing 的读取上限，也没有可设置它的字段——只有那个硬编码并发常量——因此两个上限都属于 fork：`coldReadConcurrency`（默认 4，即移植过来的常量）限制在飞读取数，`coldReadBudget`（默认 64）限制单次 listing 发起的读取数。预算计的是**真实**会话读取：能由投影缓存回答的候选在进入选择之前就已解析，因此既不花预算也不占并发槽——这正是重复 listing 单调推进、新建子会话不会因为队首被缓存服务而永久 `unavailable` 的原因。超出预算的候选保留其 corpus 位置，产出既有的可重试 `{kind:'diagnostic',reason:'unavailable'}`；不新增 wire 词汇，取消语义不变。验证：`npx vitest run packages/subagent/subagent/tests/list-children.spec.ts` —— 该块逐次 listing 统计观察次数、用挂起的读取钉住并发峰值、在缓存填充后重新 listing，并证明直接构造的插件也套用随产品交付的默认值。其标记落在 `Config` 声明**之下**，因为生成的配置目录钉住的就是该声明的行号。决策记录：[note](.agents/notes/implemented/bug-fix/2026-09-21-subagent-listing-cold-read-bound.zh.md)。0.1.7-rc.1 合并把两个上限收缩到后代遍历：上游的 `listChildren` 改由父目录投影作答、不做任何冷读，因此不传上限，只有 `listDescendants` 花掉它们 |
| `api/gateway` Remote stream 载波生命周期诊断 | C | `packages/api/gateway/src/index.ts`（`diagnosticsSlowMs` 这个 `Config` 字段及其 schema 边界与默认值、stderr sink 常量、mux 构造处的诊断参数）+ `packages/api/gateway/src/stream-server.ts`（`RemoteStreamDiagnosticSink` 与 `RemoteStreamDiagnostics` 类型、诊断常量、accept 时刻/心跳致死/已开逻辑流三项跟踪，以及四处上报点）+ `packages/api/gateway/tests/stream-server.host.spec.ts`（7 个用例）与 `packages/api/gateway/tests/gateway-stream.host.spec.ts`（2 个用例） | **保留** —— 生产环境浏览器侧 mux WebSocket 每约 15 秒重建一次并持续数小时，而这条路径此前零日志，于是「心跳 terminate」「ready/首帧迟发（事件循环被别处占用）」「对端或代理关闭」三者无法区分；traefik 记录同一 route 24 小时内 468 条连接、寿命中位 14.5 秒、峰值每小时 90–135 条。上游 mux 既无载波生命周期输出，也没有可注入的 sink，因此 fork 增加一个可选的 `RemoteStreamDiagnostics`（sink + `slowMs`），输出四类单行诊断：首帧慢于阈值（endpoint + elapsedMs）、事件循环令心跳 tick 迟到（driftMs，最多每 5 秒一条）、心跳 terminate（missed 计数）、以及每次 socket 关闭一行（code、截断到 120 字符的 reason、socket 存活时长、已开逻辑流数、是否心跳所致）。sink 写 `process.stderr`，因为本仓库没有注册任何 console exporter——cordis 内置 exporter 只写内存环形缓冲——`ctx.logger` 的行到不了运维日志，而本部署由 supervisor 把 stderr 折进服务日志；sink 保持可注入，供确有 exporter 的组合改投 logger。健康流量只产出每次关闭那一行，任何一行都不携带 stream payload 或 Session 值。验证：`npx vitest run packages/api/gateway/tests/stream-server.host.spec.ts packages/api/gateway/tests/gateway-stream.host.spec.ts` —— 用例钉住每一类行、阈值边界、5 秒限流，以及组合后的 Host 上的 stderr 路由。其标记落在 `Config` 声明**之下**，因为生成的配置目录钉住的就是该声明的行号。决策记录：[note](.agents/notes/implemented/feature/2026-09-21-remote-stream-carrier-diagnostics.zh.md) |

| `session-persistence-sqlite` 保留已解码日志（`decodedLogCacheBytes`） | A | `src/index.ts` 的 `Config` 字段（`decodedLogCacheBytes` schema 键及其 JSDoc 标记）+ `src/store.ts`（`DecodedLogCacheEntry`、带字节计数的 `decodedLogs` LRU 映射、`loadStoredLog` 中经 revision 校验的复用、`memoizeDecodedLog`/`forgetDecodedLog`/`commitSessionMutation`、`materializeHeader` 的 revision 推动、`close()` 的清空）+ `src/compression.ts`（`DecodedTextMeasurement`、`ScannedRows.decodedBytes`、`decodeRow` 与 `decodeScalarRow` 中的 `Buffer.byteLength` 计费）+ `src/restore.ts`（共享 header 所需的 header 冻结：当前格式 codec 会把解码出的 header 展开成新的未冻结对象，而 header 记录的字段全是原始值，浅冻结即完整）+ `tests/decoded-log-cache.spec.ts`（24 用例）+ `tests/packed-charge.spec.ts` + `tests/hit-no-event-read.spec.ts` + `tests/resources/sql/insert-schema-19-multibyte-events.sql` + `tests/resources/sql/delete-session-row.sql` + `tests/resources/sql/update-session-cwd.sql` + `tests/decoded-text.ts` | **保留** —— 本包为 fork 自有，因此本次改动不会在其自身文件之外新增合并面。上游没有等价物：每一次整日志读取都会重新解压、解析、校验并冻结存储行，于是恢复会话、重连客户端或重新列 catalog 每次都会重建一份相同的对象图。在本部署那条 66,736 事件的会话上，命中仍要付整行扫描时的重复读取实测冷读约 3.5 秒、保留后约 0.30 秒（约 11×），两次命中返回同一个冻结对象（`identitySame: true`）；把判定提前到事件行查询之前后，命中只读一行会话行、不再读事件行（合成 66,000 事件会话上中位 80.6 ms → 0.025 ms、p95 0.342 ms，冷读不变）；该会话的保留日志被计费 252.2 MB 解码后 JSON 文本。store 按连接保留整份已解码日志，判定依附于读取本来就要付的那一步：命中要求会话行的 revision 来自同一个 `readTransaction`、且在查询事件行之前就判定——命中只 prepare 一条 `select-session`、不 prepare 事件行语句，复用之前也没有 await——因此任何连接或进程经本提供方的写入都会未命中，而不会返回数据库已不再持有的行。每条本地写路径都在自己的事务内推动该 revision，并在提交后删除该会话的条目（header 物化也在其中，因为它重写了命中判定所依据的那一行）；上限按每条日志解码后 JSON 文本的 UTF-8 `Buffer.byteLength` 计费，按最久未使用淘汰，且永不保留比整体上限还大的日志。默认 `0` 即已发布行为、不保留任何内容；部署在自己的 profile 行里与 `cacheSizeKib`、`asyncCodec` 并列设定它。限制写在包 README 中（计费是被保留图的下界、带外 SQL 损坏在淘汰前可能被掩盖）。它与上方的 `asyncCodec` 行组合：那个开关把未命中仍要付的解码挪到 libuv 池，这一条让命中直接跳过解码。验证：`npx vitest run packages/session/session-persistence-sqlite/tests/decoded-log-cache.spec.ts`（24 用例）、`npx vitest run packages/session/session-persistence-sqlite/tests/packed-charge.spec.ts`、`npx vitest run packages/session/session-persistence-sqlite/tests/hit-no-event-read.spec.ts`，以及包套件（`npx vitest run packages/session/session-persistence-sqlite/tests` → 13 文件通过，176 中 175 通过 / 1 跳过）。决策记录：[note](.agents/notes/implemented/feature/2026-09-21-decoded-log-cache.zh.md) |

| `ui-workspace` 会话行悬浮卡片移除 | C | 5 个带标记文件共变更约 290 行（删除 231 / 新增 62）：`packages/client/ui-workspace/src/client/rows/Rows.tsx`（`HoverCard` 返回、`SessionHoverContent`、`hoverTimeLabel`）、`packages/client/ui-workspace/src/client/rows/Rows.module.css`（`.hoverStatus`）、`packages/client/ui-workspace/src/client/locales.ts`（两个字典的 `time.ago`）、`packages/client/ui-workspace/tests/rows.client.spec.tsx`（3 条卡片用例与 4 条行用例中的卡片部分）、`apps/web/tests/workspace-management.e2e.ts`（停留并复制用例），以及该包 README 双语对 | **保留** —— 所有者移除了侧栏会话行的悬浮卡片：行本身已呈现标题、紧凑相对时间与状态点，卡片只是重复行内容并随指针在会话列表中移动。上游的卡片（完整标题、套「…前」模板的相对时间、每个实时状态一行状态文案，以及整卡复制标题）被删除而非隐藏，因为隐藏会留下按文件 100% 覆盖率门禁持续索要用例的代码（[note](.agents/notes/implemented/simplification/2026-09-22-session-hover-card-removal.zh.md)）。Workspace 行的卡片属于上游并保留，因此 `HoverCard` 与 `.hoverContent`／`.hoverTitle`／`.hoverPath`／`.hoverTime` 规则仍有消费者。同步后重放：`SessionNodeItem` 在标记处直接返回行元素而非 `HoverCard` 包装；删除 `SessionHoverContent` 与 `hoverTimeLabel`；删除 `.hoverStatus` 块；两个字典去掉 `time.ago`；行 spec 去掉卡片用例；workspace-management 浏览器 spec 去掉停留并复制用例。去掉包装同时去掉套在每个会话行外的 `display: block` span；`.groupSection > * + *` 继续提供 2 px 行距，行自身的拖拽标记本就相对行定位 |

| `ui-workspace` 会话行 `⋯` 菜单扩展位 | 已退役 | — | **已退役** —— 上游 0.1.7-rc.1 为这个座位交付了真正的 slot：`sidebar.workspaces.session.menu.item` 与 `sidebar.workspaces.session.row.action` 是所有行（含本包自己的 Pin/Rename/Fork/Archive 动作）都注册进去的 `list` slot，因此 fork 的 `sessionRowMenu` 注册表模块、四处标记注入与 `tests/session-row-menu.client.spec.ts` 均已删除。消费者迁移尚未完成：本部署挂载的树外插件 `dsh-session-snooze` 仍在其自有的 `packages/session-snooze/src/client/index.tsx` 中注入已删除的 `sessionRowMenu` 服务，必须改为经 `sidebar.workspaces.session.menu.item` 注册其菜单行 |
| `packages/guard/turn-continuation` guard 插件 | A | 1 个包（`src/index.ts` + `tests/turn-continuation.spec.ts`）+ 其 README 双语对与 Agent Note | **fork 自有**，于 2026-09-20 加入；上游没有这个包，同步时逐字采用即可。插件在轮次以配置的原因结束后再开一个轮次：`continueOn`（默认 `['max-tokens']`）与 `maxConsecutive`（默认 `2`）是经校验的 `Config` 字段，`continueOn` 出现可继续原因（`max-tokens`、`error`）之外的条目或 `maxConsecutive` 非整数会让加载失败，`maxConsecutive: 0` 无需卸载即可关闭继续。预算以确切 Agent 为键存放在 `WeakMap` 中，统计每个人类轮次内的连续继续次数，且仅当 `agent/inbox/claimed` 消息的 `source.kind` 为 `user` 时回填；每次继续都是一条持久 `user/message`，来源为 `{kind: 'plugin', plugin: 'turn-continuation'}`，继续授权保持进程本地。消费者：本部署自有的 profile 行启用它——没有任何随仓库发布的 profile 挂载它，因此同步后请确认那一行（它在本仓库之外）；该行消失时本行随之退役。决策记录：[note](.agents/notes/implemented/feature/2026-09-20-turn-continuation-policy.zh.md) |
| `api/gateway` 日志帧节流 | C | 1 个 fork 模块（`packages/api/gateway/src/client/fork/stream-frame-pacing.ts`，96 行）+ `packages/api/gateway/src/client/journal-stream.ts`（`consume()`）中 1 处标记 import 与 1 处标记调用 + `packages/api/gateway/tests/stream-frame-pacing.client.spec.ts`（10 用例：7 单元 + 3 集成）+ gateway 客户端编译面中的一行 | **保留（2026-09-25 新增）** —— 实测缺陷：手机档客户端（412×915、DPR 2.625、`Emulation.setCPUThrottlingRate(4)`）连续流式 228 秒产生 1352 个长动画帧、阻塞 164.6 秒（占墙钟 72%），最长单帧 2927 ms，而同期样式与布局合计仅 61 ms。机制：`StreamInbox` 是无界双端队列，队列非空时其 `next()` 在已 resolve 的 promise 上兑现，于是一个 WebSocket 消息任务就通过微任务把整条下游链路——帧解码、会话折叠、conversation assembler、store 发布——抽干且不让出事件循环，渲染器永远轮不到。fork 记录已消费的帧数，在一批预算用尽后（`FRAME_PACING_TIME_BUDGET_MS = 4`、`FRAME_PACING_FRAME_BUDGET = 32`）等待一个与流的 `AbortSignal` 竞速的 `MessageChannel` 宏任务（无定时器回退：所有受支持 host 都提供 `MessageChannel`）：帧序、终止帧与错误帧、游标状态、`options.failed` 与 dispose 静默都保持不变；时间预算始终生效，因此「一条消息一帧」的慢流每帧回到事件循环一次，而单轮仍只承载一帧自身的工作量加上预算内能塞进的廉价帧。消费者：本部署的 Web GUI。上游在该循环里没有让出点，因此待上游自行给帧消费设界后本行退役。同步后重放：复制 `src/client/fork/stream-frame-pacing.ts`，再把标记 import 与 `consume()` 顶部那处标记 `await afterFrame(this.signal)` 调用加回。验证：`npx vitest run packages/api/gateway`（10 文件 / 379 用例全绿）、`npx vitest run packages/api/session-controller`（46 文件 / 960 用例全绿）、`tsc -b` on the gateway client compiler face，以及去掉注入调用后新规格的 3 条集成用例变红 |

| `api/session-controller` + `session-query` + `session-persistence` 的 Session 列表行范围 | D | 12 个上游自有源文件约 900 行加 2 个 SQL 资源：`packages/api/session-controller/src/index.ts`（`list` Remote 与 `listScopeOf`）、`packages/api/session-controller/src/{list.ts,types.ts}`、`packages/api/session-controller/src/client/sessions/{manager.ts,service.ts}`、`packages/session-query/session-query/src/{corpus.ts,index.ts,types.ts}`、`packages/session/session-persistence/src/index.ts`（`SessionPersistenceListSelection` 接缝与共享谓词 `matchesListSelection`）、`packages/session/session-persistence-jsonl/src/index.ts`、`packages/client/ui-session/src/client/index.ts`、`packages/client/ui-conversation/src/client/skeleton/ConversationSession.tsx`；fork 自有的 `session-persistence-sqlite` 新增 `packages/session/session-persistence-sqlite/resources/sql/select-listed-sessions.sql` 与 `packages/session/session-persistence-sqlite/resources/sql/select-parented-sessions.sql`，由 `packages/session/session-persistence-sqlite/src/store.ts` 路由并在 `packages/session/session-persistence-sqlite/src/sql.ts` 注册 | **保留（2026-09-25 加入）** —— 实测缺陷：侧栏首屏会列出每一个已存储会话，生产部署上 6,015 行、线上载荷 2.58 MB，因为 `session/list` 只有一种不分页、不过滤的形态而客户端整包拉取。上游 0.1.7-rc.1 仍只有这一种形态、也没有存储侧行选择，因此整个范围归 fork 所有：`scope`/`parentSessionId` 线协议字段及其 `SessionListScope` 翻译、经 `session-query` 进入 `SessionPersistence.list` 的选择、SQL 行选择，以及客户端按需读取子会话的一整套（向上走祖先、保留行、成员权威、重连重读）。2026-09-26 的部分观测扫描加入了防止部分观测驱动状态的围栏：`readChildren` 丢弃来自被取代 Host 世代的响应（`childReadGeneration`，由 `handleConnected` 推进），并把删除帧晚于响应的子会话挡在合并之外（`removedChildIds`，遇新增或新世代清除）；`UiSession` 的状态协调把离开的 Host-list 成员与已存储但从未列入 Host 列表的投影行区分开（`listBefore` 快照），因此目录派生的子会话在世代重置后仍保留 running/unread 状态；`ui-subagent` lineage 切换器在打开树时启动惰性的父目录读取，恢复的 subagent 地址不再停在无人解决的提示上。同步后按四层重放：(1) 保留 session-controller `list` Remote 里的 `listScopeOf` 与 `SessionListRequest` 字段；(2) 保留查询 `corpus.ts` 中 `listSessions(signal, scope)` 把选择下传 `persistence.list(options)` 的路径，并删掉 corpus 若再长出的记录级过滤——谓词已移入接缝；(3) 保留 `SessionPersistenceListSelection`、`matchesListSelection`、JSONL 的 header 与待落盘行过滤，以及两个 SQL 资源与其 `SQL_RESOURCES` 条目；(4) 客户端保留 `refreshList` 的 `{ scope: 'listed' }` 请求、`loadChildren`/`readChildren`/`ancestorParent`/`retainedChildIds`/`retainScopedChildren`/`forgetRetainedChild`/`knownChildParents`，以及 `UiSession` 的 `observedRunning`/`presentBefore` 守卫，外加下方专门条目登记的部分观测围栏（manager 的 `childReadGeneration`/`removedChildIds`、`listBefore` 快照与切换器的打开时读取）。验证：`NODE_ENV=test npx vitest run packages/api/session-controller packages/session-query packages/session/session-persistence-sqlite packages/session/session-persistence-jsonl packages/client/ui-session packages/client/ui-subagent`；新增规格为 `packages/session-query/session-query/tests/session-list-scope.spec.ts`、`packages/session/session-persistence-sqlite/tests/session-list-scope.spec.ts`、`packages/session/session-persistence-jsonl/tests/list-scope.spec.ts`、session-controller manager 的 `scoped children` 块与 ui-session 的范围外状态用例 |
| `api/session-controller` 客户端会话列表重建闸门 | C | `client/sessions/projection-store.ts` 的变化感知写入 + 按键投递的 `subscribeAny` 通道 + manager store 监听器里按键门控的标脏 + `client/sessions/fork/coalesced-refresh.ts` 的 `LIST_IRRELEVANT_PROJECTION_KEYS`/`affectsSessionList`/`reconcileListState` + `client/sessions/manager.ts` 的 `mergeChildren`/`mergeSummaryRows` 单趟子行合并 + `client/sessions/service.ts` 中 1 处标记调用 + 对应规格 | **保留（2026-09-25 加入，2026-09-26 扩展）** —— 实测缺陷：手机档客户端**不打开任何会话**时在 310 秒窗口内烧掉约 300 秒 JavaScript（496 个长动画帧、阻塞 280.9 秒、样式与布局 13 ms），原因是每个已附加会话的每个控制帧都无条件标脏列表，且每次重建都为每个会话新造行并总是 `list.set`。`projection-store.ts` 报告被接受的帧其已发布值是否真的变了（值相等时只推进序号水位、保留缓存值及其引用、不通知任何人），`subscribeAny` 按变更逐个投递变更键使 manager 能按键决策；manager 的 store 监听器只在 `affectsSessionList(key)` 为真时标脏列表——deny list 豁免 `sessionStats`、`contextPressure`、`contextBreakdown`（其唯一读者是会话级 `useProjection` 绑定），未列出的键仍重建，`tokenUsage` 不进豁免集因为 subagent lineage 从行 map 读它——`handleControlFrame` 不再自行标脏（决策归 store 通道）；`reconcileListState` 对字段全等的行沿用已发布的行对象、并在 ids/行/顺序/phase/投影都未变时返回已发布的状态对象，从而跳过 `list.set`。2026-09-26 扩展把一次子会话读取改为单趟合并：`readChildren` 把整个响应交给 `mergeChildren`，经 `mergeSummaryRows` 一次 O(rows+children) 合并（一次 mutation log 写入、一次标脏、一次 `updateParentAvailability`）替代每个子行一次全数组重写。实测：200 个只带 `sessionStats` 的帧（20 会话 × 10 次变化值）从 22.1 ms / 10 次重建降到 0.6 ms / 0 次重建，一次 2,244 子行读取从 89.0 ms 降到 8.9 ms（生产上报 99 ms）。消费者：本部署的 Web GUI。同步后重放：复制 `client/sessions/fork/coalesced-refresh.ts`，再把 `projection-store.apply` 里带标记的变化感知返回、`subscribeAny` 的按键投递、manager store 监听器里按键门控的标脏、`handleControlFrame` 对该通道的委托、`refreshProjections` 的水位探测、`mergeChildren`/`mergeSummaryRows` 单趟合并及其 `readChildren` 调用、以及 `service.projectList` 的 `reconcileListState` 调用加回。验证：`npx vitest run packages/api/session-controller`、`npx vitest run packages/client/ui-session packages/client/ui-workspace packages/client/ui-sidebar`，以及 session-controller 两个编译面的 `tsc -b` |
| `settings` 旧版文档导入韧性 | D | `packages/settings/settings/src/index.ts` 约 200 行（`importLegacyDocument` 及其 `normalizedLevelKeys`/`valueSchema`/`declaresLevel`、`writeAtomic`、`importedTarget`、`importedSections`、`discardMarker` 辅助函数），外加 `packages/settings/settings/tests/legacy-import.spec.ts` 与 `packages/settings/settings/tests/{configuration.spec.ts,configuration-fixture.ts}` 的更新 | **保留（2026-09-25 加入）** —— 已复现缺陷：上游在写入任何 section 之前就改名 `settings.yaml`，一个被运行中组装拒绝的 section 便消耗掉整份文档；滞留的生产文档里 `reasoningEfforts` 拒绝了 YAML 解析器为裸 `off` 生成的布尔键 `false`，其后整份模型／provider 列表留在改名后的副本里，永不再试。fork 在文档仍在其原路径时写入每个 section，只把被拒绝的 section 留在旧路径等待下次启动，在改名之前把完成的 section 名单记入 `.imported-sections` 边车文件，使改名失败不会重复写入；被消费的文档移向第一个空闲的 `.imported[.N]` 名而不是覆盖既有副本；重试文档与边车都经同目录临时文件原子写入；被 schema 拒绝的 section 会再试一次，把字面 `false`／`true` 键改写为该条目配置 schema 声明的等级名。上游没有等价顺序与重试，因此该行在 upstream 把导入做成事务后退役。同步后重放：移植 `importLegacyDocument` 及其辅助函数；`packages/settings/settings/tests/legacy-import.spec.ts` 是验收规格 |
| `ui-chat` 有界转写挂载窗口 | C | 1 个 fork 模块（`packages/client/ui-chat/src/client/chat/fork/mounted-window.ts`）+ 标记注入位于 `packages/client/ui-chat/src/client/chat/ChatView.tsx`、`packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx`、`packages/client/ui-chat/src/client/chat/ChatGroupSeat.tsx`、`packages/client/ui-chat/src/client/chat/use-chat-scroll.ts` 与 `packages/client/ui-chat/src/client/chat/use-chat-reading.ts`，以及 `packages/client/ui-chat/src/client/chat/use-chat-viewport.ts` 里的读者手势事件（顶到挂载窗口头部的手势不产生滚动，因此不会发出 scroll 事件） | **保留（2026-09-25 加入）** —— 实测：转写面此前挂载全部已加载历史行，于是每帧的 DOM 与渲染工作随会话增长（手机档客户端：连续流式 228 秒产生 1352 个长动画帧、阻塞 164.6 秒；打开 231 行的会话在 8.7 秒窗口里花掉 6.87 秒脚本时间）。窗口围绕读者挂载 `MOUNTED_ROW_LIMIT = 50` 个常驻 order key，每次揭示上移 `REVEAL_ROW_STEP = 25`，轮次流式期间保留常驻尾部短桩；保存的锚点可解析（普通节点 key、`["key","part"]` 对，以及经其首个内嵌成员 key 解析的 `group:<json>`），无法解析的（`call:<id>`）留在已挂载底部且会话滚动记忆保持不变；跳转落地后重新布防 fork 的回流保持。已知残留：冻结窗口的解析结果按 entries 身份加 head 缓存、并只对该 50 key 切片做复验，因此若顺序重写把某个常驻 key 移到窗口之上而该切片逐字节不变，就可能复用一次完整游走存下的解析结果（探针形状记在 `FORK_CHANGES.md`）。头部控制行挂载（2026-09-26 加入）：折叠把成员行藏在轮次的 `turn-process` 控制行之后，因此一个携带某轮成员行却没有其控制行的窗口等于什么都不渲染；末步停在工具调用、没有最终回答的已完成轮次没有可见回答行，当它的过程行数超过 50 key 切片时控制行落在头部之上——转写面显示零个可见行，静默发生且没有揭示路径（没有已保存行的读者无法移动窗口头部，分页只会前插更早的历史）。`planMountedWindow` 接收常驻的升序 `controls` 索引，把轮次控制行与每一个携带其隐藏行的切片一起挂载——两个条件性额外 key：头部轮次的控制行（当它常驻于窗口头部之上时），以及短桩挂载期间尾部切片自身的控制行（否则长于短桩切片的折叠轮次会挂载成员行而控制行位于其上方）。Turn 对齐头部（2026-09-26 加入、2026-09-26 收窄）：派生的开场头部与每一次**显式**揭示步都吸附到拥有目标行的轮次开头（`turnAlignedHead`，并用 `frozenAt` 钉住，使 `windowAt` 的尾部归一化不能把它拉回），因为落在轮次内部的头部会挂载该轮次靠后的行却没有其控制行与提示词；读者自己的行仍钳制该步，而拥有该步必须保留之行的轮次开头比它高出超过 `MOUNTED_ROW_LIMIT` 行时该步拒绝移动、改由 `pageEarlier()` 服务读者。读者自己的连续手势正是这类边界之一：被挂载头部挡住的手势会揭示下一个 Turn 对齐步，因为把头部留在轮次内部会让 `planMountedWindow` 把该轮次的控制行挂载到窗口之上，而这次插入会在没有 scrollport 写入可吸收的情况下移动阅读线。窗口持有的读者行就是该步必须保留的行，因此无法在它之下移动的步会拒绝（改由显式控件分页），而指针按下不是滚动手势、永不步进。连续遍历（2026-09-26 加入）：窗口围绕读者保存的行**只采纳一次**，此后只由显式步进、跳转或交还尾部来移动；每次渲染重新派生会让整片切片随每个落定的采样滑动，并在阅读线之上挂载行，而 `use-chat-scroll` 的标记回流会以写入 scrollport 来补偿——手势进行中这读起来就是「位置被重算并把读者推回去」（浏览器泳道 `chat-scroll-contract` 改动前的轨迹：`scrollTop` 2006 → 2036 → 2170 → … → 2652，每步一次写入，读者最终钉在挂载顶部）。手势步进的闸门是手势本身即可满足的几何条件（`top <= REVEAL_AT_HEAD_PX` 且 `movedByReader`；不再看阅读策略或已保存行），揭示一个 Turn 对齐步且不写 scrollport 位置，并通过 `ChatReading.settleStep()` 重读读者位置，使保存的行与回流保持跟随该步放到阅读线上的行；`touchmove` 加入读者意图，手机端在挂载头部持续拖动也能继续步进，而指针按下或页内查找揭示永不步进。消费者：本部署的 Web GUI。上游挂载整个已加载窗口。同步后重放：复制该 fork 模块并加回标记注入，包括 `ChatView.tsx` 里供给规划器的 `controls` memo、`use-chat-viewport.ts` 里的读者手势事件（含 `touchmove`）、`planMountedWindow` 里的短桩控制行、为选定边界保留的 Turn 对齐派生（`turnAlignedHead`/`revealHead`/`frozenAt`），以及手势步进（`stepAtHead`/`gestureSteps`/`ChatReading.settleStep`）。验证：`NODE_ENV=test npx vitest run packages/client/ui-chat`（合并 tip 上 44 文件 / 665 用例全绿，其中锚点不变量门禁 19 用例、Turn 对齐规格 11 用例）、该模块语句/分支/函数/行 100%，以及 `chat-scroll-contract`、`chat-long-interactions`、`stats-paged-history`、`preview-boot` 四条泳道与 `complex-history.perf.ts` |

| `api/session-controller` 列表面投影白名单 | D | `packages/api/session-controller/src/list.ts` 的 `LIST_PROJECTION_KEYS` + `listProjectionValues`（`hintsOf` 投影）+ `packages/api/session-controller/src/types.ts` 的 `SessionProjectionHints` 契约 JSDoc + `tests/session-list-projection-selection.host.spec.ts` | **保留（2026-09-26 加入）** —— 实测缺陷：默认 `session/list` 响应把 92.4% 的字节花在消费者全部位于 Session-open 路径的投影上，因为 `hintsOf` 把每个已注册 wire 键都放进每一行。列表构建器现在把每个 block 投影到列表面会读的键（`sessionListMetadata`、`title`、`subagentCatalog`、`schedule`、`subagentTiming`、`tokenUsage`、`subagent`、`agentPreset`、`agentTeam`、`modelSelection`）；重型 open-plane 键（`turnOutline`、`inbox`、`todos`、`imageLimits` 及其余）不再为一次列举过线，且一个不携带任何保留键的 block 会整体省略其 `projections`。裁剪放在列表构建器，因为它是每个列表行与 `api-session/added` 帧都要经过的唯一收窄点，冷热皆然，未来调用方无法绕过；Session-open 路径（follow 快照、control baseline、`session/projections` RPC）保持全量投影，持久化/缓存层保持全量。保留集是审计过的读者集——每个键都指名其列表面消费者（workspace tree、subagent lineage、sidebar chat 标签、preset 芯片、Team 面板），`tokenUsage`/`subagentTiming` 保留是因为冷子会话的 lineage 指标从行 map 读它们。2026-09-26 更正：`modelSelection` 属于该读者集——Team 面板的成员行经 `projectionsBySession[member.id]?.values` 读它，客户端的重建闸门也早已把它归为列表相关键——因此在此恢复，选择规格改为从仓库自身的列表面读者派生键集，而不再依赖手工维护的名单。实测：最大单行省 ≥37,786 B（整个 `turnOutline` 值消失）；每行还省去重复的 `imageLimits` 常量（约 200 B × 223 行 ≈ 44 KB）。消费者：本部署的 Web GUI。同步后重放：保留 `list.ts` 的 `LIST_PROJECTION_KEYS`/`listProjectionValues` 与 `hintsOf` 投影，以及 `types.ts` 的契约 JSDoc。验证：`npx vitest run packages/api/session-controller`（选择规格钉住保留集、重型键泄漏、open 路径完整性，以及从源码派生的读者集） |

| `api/session-controller` + `ui-session` + `ui-subagent` 部分观测围栏 | D | 三个上游自有客户端文件约 70 行：`packages/api/session-controller/src/client/sessions/manager.ts` 的 `childReadGeneration` 计数与 `removedChildIds` 集合（`readChildren` 的围栏、`mergeChildren` 的删除过滤、`handleSessionAdded`/`handleSessionRemoved` 的标记、`handleConnected` 的推进）、`packages/client/ui-session/src/client/index.ts` 的 `listBefore` 快照（状态协调对已存储与已列入的区分），以及 `packages/client/ui-subagent/src/client/SubagentHeaderLineage.tsx`（`CatalogDropdown`）的打开时投影读取 | **保留（2026-09-26 加入）** —— 实测缺陷类：按范围收敛的会话客户端以碎片方式观测子会话与投影，于是三条路径会让陈旧或缺失的碎片驱动已被更新信息纠正过的状态。重连前的 `readChildren` 响应把陈旧行合并到新读之上、其失败还清掉新读的 memo（世代围栏现在两者都丢弃）；与在途读取竞速的删除帧被先于它计算的响应覆盖（`removedChildIds` 把该行挡在合并之外）；目录派生的子会话在世代重置清空投影 store 时丢掉已观测到的 running/`completionUnread` 状态（`listBefore` 把离开的 Host-list 成员与已存储但从未列入列表的投影行区分开）；恢复的 subagent 地址让 lineage 切换器中缺失的父目录永久停在加载态（现在打开该树会读取它）。上游没有任何这些围栏：它只看 store 快照协调状态、启动子会话读取不带世代。同步后重放：恢复 manager 里的 `childReadGeneration`/`removedChildIds` 及其检查点、`UiSession` 中 `presentBefore` 旁的 `listBefore` 快照，以及切换器打开时的 `refreshProjection` 调用。验证：`NODE_ENV=test npx vitest run packages/api/session-controller packages/client/ui-session packages/client/ui-subagent`——分支 tip 为 53 文件 / 1094 用例；回归用例为 manager 的世代/删除块、status-controller 的仅目录用例与切换器的缺失父目录用例 |

| `ui-workflow-run` 工作流面板成员解析 | C | `packages/client/ui-workflow-run/src/client/WorkflowRunPanel.tsx` 内联约 35 行（`catalogIdIndexes` `WeakMap`、`catalogChildIds` 与重写的 `navigableMembers` 成员判定，带有 `Fork patch (FORK_SURFACE.md)` 标记）+ 对应规格里的计数 Proxy 用例 | **保留（2026-09-26 加入）** —— 实测缺陷：面板的成员 selector 在每次 Session-store 通知时重跑，且每个成员各自扫描父会话的 `subagentCatalog`，因此一次调用成本为 O(成员数 × 目录大小)——2,244 条目目录下 56 个已挂载面板实测每帧 4.4 ms，200 条目 × 30 成员两趟渲染计得 11,301 次目录元素读取。fork 现在按目录身份建一次索引（`catalogIdIndexes`，按目录数组身份为键的 `WeakMap`），并用 `childIds.has(member.childId)` 判成员，因此一趟目录遍历覆盖整个 selector 调用；计数/状态语义不变（状态查找键改为 `member.childId`，与原 `child.id` 值等价）。消费者：本部署 Web GUI 的工作流运行面板。上游为每成员重扫目录。同步后重放：恢复 `catalogIdIndexes`/`catalogChildIds` 与 `navigableMembers` 的成员判定（标记就在索引上方）。验证：规格的计数用例在扫描实现下失败（11,301 次读取）、在索引实现下通过（200 次）；`NODE_ENV=test npx vitest run packages/client/ui-workflow-run`——合并 tip 上 2 文件 / 37 用例全绿 |

| `session-query-sqlite` 排序文档预算 | B + C | 一个 `Config` 字段（`maxRankedDocuments`，默认 `SESSION_QUERY_SQLITE_DEFAULT_MAX_RANKED_DOCUMENTS = 5000`）加 `packages/session-query/session-query-sqlite/src/index.ts` 中只读 postings 的 `_assertRankableBreadth` 探针及其两个调用点（约 25 行），再加 `packages/session-query/session-query/src/config.ts` 的 `SESSION_QUERY_SEARCH_TOO_BROAD` 码、`packages/session-query/tool-session-query/src/service-boundary.ts` 的映射项，以及 `packages/api/session-controller/src/list.ts` 的 `gateway/bad-request` 映射（约 8 行） | 上游的排序 CTE 在 `LIMIT`/`OFFSET` 生效之前就为每个匹配物化 `highlight()` 与基于 `replace()` 的出现次数，因此一次搜索调用的工作量是 O(匹配集 x 文档文本)。2026-09-26 在本部署 store 的副本上实测（318,335 个文档；`agent` 与 `the` 分别命中 35,872 与 92,802 个文档）：当时部署的构建上，一次 `session/search` 请求读了 3.08 GB / 300 s（660 CPU·秒）且从未返回；卡死的生产进程在 20 分钟内对 `session-query.sqlite` 及其 WAL 做了 1.44 TB 的 4 KiB 离散页读且无写入；这样阻塞单线程的请求会连带停止响应信号与 inspector。探针只流式读取至多 `maxRankedDocuments + 1` 个 rowid（只读 postings，不读文档文本），在 reconcile 之前、排序查询之前执行，并对更宽的匹配集报 `SESSION_QUERY_SEARCH_TOO_BROAD`（映射为 `gateway/bad-request`，客户端看到有界的拒绝而非服务器故障）。重新施加：`Config` 字段及其校验、`_reconcile` 旁的探针、`searchSessions`/`searchEvents` 中 `_reconcile` 之前的一次 `assertNotAborted` 加探针调用、错误码成员、工具映射项、控制器映射分支。消费者：本部署的 Web 搜索框与 `session_query` 工具；实测效果由「3.08 GB / 660 CPU·秒 且永不完成」变为「288 KB / 0.41 CPU·秒 即返回拒绝」。回归：`packages/session-query/session-query-sqlite/tests/sqlite.spec.ts` 围绕约 3.8 MiB 语料上的两次搜索读取 `/proc/self/io`，断言低于 1 MiB（未打补丁的引擎为 16 MB 以上）。**2026-09-26 follow-up，每请求上界：** 上面的探针只限定每次排序 CALL 的匹配文档数，但一次调用方请求会按页重跑整轮排序 —— `packages/api/session-controller/src/list.ts` 把游标当作 OFFSET 最多传 `SEARCH_PROVIDER_CALL_LIMIT`（100）次 —— 因此未超上限的匹配集仍会每页重排一次。在本改动起点（master tip）的隔离实例上，对着本部署 store 与索引的副本（318,335 个文档、1,438 个已持久化 Session）实测：`session/search {query:'搜索'}`（2,271 个匹配文档、27.5 MB 存储文本、1,264 个 Session）读 **114.8 MB / 1.19 s**，同一查询的 20 页游标遍历读 **1,276.2 MB**（每页 57-79 MB、0.52 s）。引擎现在按「规范化请求 + 语料库代」只排序一次，缓存不含文档文本的 identity 列，并用切分该排序结果的方式服务续页游标；只有本页自己的行会重读存储文本，`SESSION_QUERY_SEARCH_BUDGET_EXHAUSTED` 会拒绝任何会让调用方请求累计排序超过 `maxRankedDocuments` 个文档的重排（每个请求一个额度，所有页共享）。改动后实测：请求 **58.4 MB / 0.78 s**，20 页遍历 **63.1 MB / 约 3.2 s**（首页 57.8 MB，后续页各约 0.26 MB）；超上限的拒绝仍为 205 KB。重新施加的补充项：`_rankedSessions`/`_rankedEvents` 排序缓存、`_slicePage` 与 `_markedTextOf`（FTS5 的 `highlight()` 必须在同一条查询里带 MATCH 约束才能标出匹配）、`_chargeRankedDocuments`、`packages/session-query/session-query/src/types.ts` 的 `exec.rankedDocumentBudget` 字段、`searchSessions`/`searchEvents` 每次排序处的计入、控制器与 `session_query` 工具中每请求一个额度，以及第二个错误码成员及其控制器/工具映射。回归：`sqlite.spec.ts` 新增两例（约 3.8 MiB 语料的六页遍历只计入一次排序且读量低于 16 MiB 天花板；活跃事件推进语料库代后重试以预算错误码被拒且读量低于 1 MiB），以及控制器一例断言两页携带同一额度 —— 这些在 master 上全部失败（`budget.spent` 为 0，期望 480 与 60） |
## 优化方案（按优先级）

1. **DONE — 从 `history.ts` 抽出分页核心** — 索引读取计划（可寻址后缀读取、其稠密与终点证明、余量梯子）落在 fork 自有的 `src/fork/page-boundary.ts`，上游永远不会有的文件。`history.ts` 只留一小段注入点：import、`tryIndexedPage`/`tryWindowedOpen` 调用，以及按请求绑定上游 `paginate` 的 `windowCut()`。上游重构 `page()` 时冲突面是这段注入，而不是两百行。
2. **DONE — 把 `messageCut` 移出上游持久化接口** — fork 自有服务（独立包或 `session-query-sqlite` 内的扩展）暴露索引切点；`history.ts` 的快路径已经是可选能力发现（`SeekablePersistence` duck-typing）。上游的 `SessionPersistence` 抽象和 coordinator 回归原状；SQL 随 fork 服务走。
3. **改动时即写合并 runbook，而非同步时补** — 每个 C/D 层改动在本清单记下注入点；同步重放从头到尾照单执行。
4. **保持 D 层 diff 紧凑并带标记** — 滚动归因块带原理解释注释；上游若自行修复同一钳位 bug，该 diff 缩为零（在上游发布中关注）。
5. **DONE — AppFrame 移动端 shell** — 最大面积。现实上保持 patch（布局是上游核心组件），但把 fork 的视口机制放进 `columns.ts` 式的叶子模块，让 AppFrame 的 diff 保持 import-and-delegate 形态。
6. **PAGE_MESSAGES** — 一行；若常变可改 build 时 env（`DSH_CLIENT_PAGE_MESSAGES`）。

## 同步流程（runbook）

1. 通读 fork 基线 tag 到目标 tag 之间的全部 release note，列出其中点名的协议、会话格式与 API 破坏性变更，并规划对应适配。
2. 在 worktree `git fetch upstream && git merge upstream/master`。
3. 先跑 `npx tsx scripts/verify-fork-surface.ts`（只读，约 0.5 秒），把失败项清干净再逐行核对清单。它会点名：带标记却没有清单行登记的 fork 补丁、被 C 层行点名却已丢失标记的文件、以及中英结构漂移；新出现的结果就是真实的同步信号——补登记该面（或补回标记），不要往 `scripts/fork-surface-baseline.txt` 里加行来消音。
4. **平价复核（AGENTS.md 政策）**：对照新 tag 逐行核对清单；上游已提供等价功能时，删除该 fork 行并采用上游版本（退役记入 `FORK_CHANGES.md`）；保留行更新「上游为何不能满足」的理由。
5. A 层：无操作（上游没有这些文件）；只解决 `pnpm-workspace`/tsconfig 聚合与 `cordis.patch.yml`。
6. B 层：重放开关/常量；预期平凡的上下文冲突。
7. C 层：按本清单逐块重放局部补丁；跑所属包的测试套件。
8. D 层：重放注入点（完成上面第 4 步后，它们是 history.ts/persistence 仅剩的 diff），fork 自有模块整体拷贝。
9. 跑 `pnpm run test:gui`、session-controller 套件、`DSH_SNAPSHOT=replay pnpm run test:web`；仅在有意输出变化时刷新金样。
10. 在 `FORK_CHANGES.md` 记录本次同步。
